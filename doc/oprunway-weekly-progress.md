# OpRunway 周进展

周与周之间用 markdown 分节（`## 截至 <日期>`）；每周进展内部用纯文字 + 序号、不做 md 排版。倒序，最新在上，新的一周在下面这节上面另起。

## 截至 2026-07-03

1. 核心架构定型（workflow / skill / agent 形式）：整套 OpRunway 拆成一个顶层的验收流程编排（workflow）+ 几个功能模块（skill，大致按生成测试用例、NPU 上跑测、性能对比分工，数量待定）+ 一个公共组件（acc-common，管统一数据格式和自动判定）+ 几个分工的智能体（agent：解析任务书、跑测、独立评审），并针对不同算子仓设计了三种接入方式。

2. 规则、设计、约束都审定成了正式准则：上面这套架构连同配套的验收口径、约束和流程规则，经过人工逐条审核确认，目前 17 篇（共 21 篇设计文档）已定为项目的"事实级"准则、其余 4 篇在审；这些是审定过的准则，不是 AI 随手写的草稿。

3. 核心路线验证可行：能自动把算子放到昇腾 NPU 上跑精度和性能测试；过程中还用 Codex 做对抗审计，当场揪出真实 bug。


