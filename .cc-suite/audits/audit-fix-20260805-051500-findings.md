# Audit Findings

**Run**: audit-fix 20260805-051500 | **Scope**: `/Users/ll/.claude/plans/purrfect-tumbling-raccoon.md`（实施计划，按「规格将被执行」审）| **Audit type**: full (9-dim)
**Model**: gpt-5.6-sol | **Effort**: high | **Sandbox**: read-only | **Audit thread**: 019fd058-f096-74c2-8f49-16622e980f00
**Status values**: open | fixed | not-fixed | partial | regressed | skipped

| # | File | Line | Severity | Dimension | Finding | Suggested fix | Status | Round |
|---|------|------|----------|-----------|---------|---------------|--------|-------|
| 1 | plan.md | 140-146 | Critical | Correctness & Reliability | W2 让“不识别/未探测任务书 caseset”回退到 `generated`，只把新 kind 塞入 `task_pr_gaps`。现有 `validator.py:613-618` 只消费 dtype gap，`validate_acceptance_state.py:63` 也只注册两种 dtype finding；这些新 gap 会被静默忽略，最终可得到干净 pass。 | 建立中央 gap registry；`taskdoc_caseset_unrecognized/not_probed/ambiguous` 必须由 validator 和验收门共同硬校，未知 kind 直接 BLOCKED，且加入 run_workflow 终态测试。 | fixed | 1 |
| 2 | plan.md | 216-217 | Critical | Correctness & Reliability | `golden_unavailable` 只定义为“逐条挂账”，没有规定失败 case 是否仍保留在完整 caseset、如何进入 evidence、validator 和验收门。若实现为跳过异常 case，169 条可被缩成 164 条并通过。 | 异常 case 必须保留身份并使 Task1/Task2 确定性 BLOCKED；禁止仅写自由文本 gap 后删除 case。 | fixed | 1 |
| 3 | plan.md | 119-138 | Critical | Correctness & Reliability | caseset 识别没有要求 `op_name` 与当前 spec/DUT 一致，也没有核 input/output 数量、名称、dtype、shape 与 spec。唯一链接指向错误算子的 caseset 时仍可能以 `case_source=taskdoc` 验当前 DUT。真实 JSON 确实含 `op_name`、完整 IO 描述和 attr 描述。[公开 caseset](https://gitcode.com/cann/cann-ops-competitions/blob/master/04_tasks/01_community-task-2026/docs/202607/self_test_case/gaussian_blur/gaussian_blur_cases.json) | 在规范化前逐 case 对账 op 身份、IO/attr schema、输出描述和 prototype；任何漂移或歧义 BLOCKED。 | fixed | 1 |
| 4 | plan.md | 80-90,183-184 | Critical | Correctness & Reliability | W0 明定 CPU 替代不能宣称原 GPU 真值条款已验收，W3 却在自带 CPU golden 被引用时取消 `device_downgrade` 和“原口径未验收”gap，只记一个当前裁决链不消费的 `taskdoc_internal_conflict`。实际任务书同时写 OpenCV GPU 与 CPU，这条路径会把未解决冲突升级为干净精度通过。[任务书](https://gitcode.com/cann/cann-ops-competitions/blob/master/04_tasks/01_community-task-2026/docs/202607/GaussianBlur_task_doc.md) | 用户 CPU 政策可以决定本轮执行真值，但原 GPU 条款仍须进入机器强制 gap；内部冲突必须 `needs_review/BLOCKED` 或由用户逐轴明确裁定。 | fixed | 1 |
| 5 | plan.md | 84-89 | Critical | Correctness & Reliability | “`tolerance_source=="taskdoc_case"` 或 expect_func 指向 CPU”被当作阈值与 CPU 真值同源；共处一个文件或相互引用不证明阈值是按 CPU↔NPU 误差预算制定的。该豁免会掩盖 CPU/GPU 固有差异。 | 只有任务书明确声明阈值适用于该 CPU oracle 才算同源；否则始终标原 GPU 口径未验收。 | fixed | 1 |
| 6 | plan.md | 212-217 | Critical | Correctness & Reliability | 新的逐 case `err_threshold` 没有规定长度、顺序、类型、有限性和非负性。Python JSON 默认还能接受非标准 NaN/Infinity；无限阈值会让任意有限误差通过。 | 严格拒绝 NaN/Inf、bool、负数、错误长度和未知顺序；复用 `precision_policy._checked_tol` 语义，并由 validator 从受摘要绑定的 taskdoc case 重新派生 canonical policy。 | partial（err_threshold 确切长度与各位置语义仍未定义） | 1 |
| 7 | plan.md | 164-173 | High | Correctness & Reliability | 直接搬运任务书 golden 无法满足当前引擎接口。`gen_cases.py:515-587` 要求 `golden_fn(inputs, attrs)`、`GOLDEN_SOURCE`、`GOLDEN_PROVENANCE`、`GOLDEN_CONTRACT`；实际文件只有 `gaussian_blur_golden(self,kx,ky,sigmaX,sigmaY)`，且顶层直接 import numpy/cv2。[公开 golden](https://gitcode.com/cann/cann-ops-competitions/blob/master/04_tasks/01_community-task-2026/docs/202607/self_test_case/gaussian_blur/gaussian_blur_golden.py) | 原始脚本冻结为只读、内容寻址输入；另生成通用适配 wrapper，明确函数签名、返回值和契约元数据，并由 `check_golden --load` 验证。 | fixed | 1 |
| 8 | plan.md | 119-129,205-217 | High | Correctness & Reliability | 计划没有定义任务 case attr 到 DUT ABI 的映射。任务 case 使用 `ksize_x/ksize_y/sigmaX/sigmaY`，而当前 spec 使用 `ksize:[x,y]/sigma_x/sigma_y/border_type`，且任务 case没有 `border_type`。 | 从 prototype、任务书和 header 三源生成显式映射；缺失的 `border_type` 必须有权威默认值或询问用户，不能猜。 | fixed | 1 |
| 9 | plan.md | 172-173,216 | High | Security & Risk | 计划会执行任务书仓发布的任意 Python。`check_golden.py:15-35` 明示它在同一解释器执行整个模块，挡不住 `os._exit`、C 扩展退出或状态篡改；AST 证明“函数存在”不证明安全。 | 在无 token、无网络、只读输入、受限输出目录、资源限额的独立容器/进程执行；不要让被执行模块与判定模块同进程。 | fixed | 1 |
| 10 | plan.md | 122-129 | High | Security & Risk | “顶层 case 键全集覆盖”没有递归到 `input_desc/output_desc/attr_desc`。真实 `data_path` 位于嵌套 input/output 对象；`case_name` 和 `expect_func` 也未限制 `..`、绝对路径、重复名或路径分隔符。 | 定义递归严格 schema；case ID 从受控内容摘要派生；expect_func 仅允许同目录 basename；所有路径经 safe_path，拒绝软链、重复 ID 和逃逸。 | partial（未明确拒绝危险 case_name / 禁止其参与路径构造） | 1 |
| 11 | plan.md | 119-145 | High | Correctness & Reliability | 所有被链接目录都可能落 `not_a_caseset_dir`，而“不认就挂 gap”会把普通设计文档目录也误报成缺失 caseset；反之，多算子任务书的多个合法 caseset 会被全局 `caseset_ambiguous` 拒绝。 | 先以链接上下文和 `op_name` 建立 per-op 候选，再按当前 spec 选择；只对明确声称为自测 caseset 的候选失败挂账。 | fixed | 1 |
| 12 | plan.md | 99-106 | High | Security & Risk | “有界递归=深度 2”并不限制广度、总文件数、单文件大小、总字节、分页或重定向；任务书链接可让 CP-A 拉取巨大目录。 | 增加 max_entries、max_total_bytes、max_file_bytes、分页上限、超时和重定向 host 白名单，超限统一 `depth/resource_limited` 并阻断相关 caseset 路由。 | fixed | 1 |
| 13 | plugin/acc-common/fetch_source.py | 60-68 | High | Security & Risk | 计划复用 `_get()`，该函数把 token 放在 URL query；递归拉取会扩大 token 暴露到代理、访问日志和错误诊断的面积。 | 改用 GitCode 支持的认证 header；禁止跨 host 携认证重定向，并保证异常/产物永不包含认证 URL。 | fixed | 1 |
| 14 | plan.md | 99-110 | High | Dependency & Environment Safety | `master` 是可变 ref；计划未要求先解析为 commit SHA，也没规定目录列表与随后文件下载必须来自同一 commit，存在 TOCTOU。 | CP-A 先把 ref 解析为不可变 commit，所有 contents 请求 pin 同一 SHA，并记录每个 blob SHA/size/content hash。 | fixed | 1 |
| 15 | plan.md | 107-108,144-146 | High | Correctness & Reliability | 本地任务书是现有正式输入形态，但计划对其所有相对链接一律 `not_probed`，即新能力在本地任务书上静默失效。 | 在任务书父目录限定根内解析本地相对路径，拒绝软链和逃逸；若策略上不支持，必须机器 BLOCKED，而非回退生成后继续裁决。 | fixed | 1 |
| 16 | plan.md | 214-215,320-321 | High | Performance & Efficiency | 磁盘预算只算 `shape × dtype`，没有乘多输入、所有输出/golden、副本、cpp_extension snapshot、evidence、msprof 临时文件和安全余量；检查错挂载点也会假报可用。 | 对实际落盘图逐工件估算，在目标 Docker volume 上检查，加入峰值与保留空间；同时做 RAM 峰值门。最大单 case 仅输入+golden 已约 776.9 MiB。 | fixed | 1 |
| 17 | plan.md | 329-331 | High | Correctness & Reliability | “分批物化、跑完即删”没有对应工作项；当前 run_workflow、cpp_extension caseset snapshot 和验收门假定完整 caseset/文件同时存在。仅保存 SHA 后删除字节会把独立可核证据退化为生产者自报。 | 本轮要么要求足够空间，要么先单独设计事务式批处理协议：冻结全量 manifest、批次收据、原子完成标志、断点恢复、门侧重算与安全清理；未经设计不得删除。 | fixed | 1 |
| 18 | plugin/acc-common/cpp_extension_codegen.py | 71-76 | High | Correctness & Reliability | W6 加 `int64[]` dtype 是对 GaussianBlur 的 no-op：当前 spec 的 `ksize` 是 `dtype:["int64"]`、值为 list，实际数组属性由值结构/`_attr_ctype` 派生。 | 复用统一的 canonical attr ctype（如 `int_array`），不要发明与现有 spec 不兼容的 `int64[]` dtype；否则必须迁移 schema 和全部消费者。 | fixed | 1 |
| 19 | plan.md | 267-270 | High | Correctness & Reliability | 调整参数顺序不能解决 10 参 stage2。仓内已有结论 `dev-doc/oprunway-gaussianblur-support-plan.md:78`：`EXEC_NPU_CMD_EXT` 假设标准四参 stage2，GaussianBlur 是 extended 10 参。 | codegen 必须按 preflight 的 `stage2_form` 选择受验证的 extended dispatcher；标准宏仅限 standard，未知形态 fail-closed。 | fixed | 1 |
| 20 | plan.md | 267-270 | High | Maintainability | 新“显式实参表”会成为 header 之外的第二 ABI 真源，且计划只改 codegen；实际还需同步 `precision_policy.call_variants`、`gen_cases.aclnn_call`、`preflight_aclnn`、adapter、manifest、driver 和 receipt。此处是高风险 DRY 破坏。 | 建一个共享 ABI IR，由 header/preflight 产出并由所有消费者读取；禁止每层各自解释另一张参数表。 | fixed | 1 |
| 21 | plan.md | 242-254 | High | Redundant & Low-Value Work | 第三个 `change_class_no_perf_comparison` ground 在计划自己的三类性能条款中没有合法的非重复用例：无条款已有 `no_perf_requirement`，GPU 已有 `gpu_comparison`，比值条款又明确互斥。若遇到绝对时延/吞吐条款，三类分类根本没格子，反而可能误降 measure-only。 | 不新增该 ground；直接从任务书性能条款派生现有 ground。先扩全性能条款词表，未知/绝对门限 fail-closed。 | partial（throughput 终态 + GPU 比较与 ratio 重叠时的确定性分类未写明） | 1 |
| 22 | plan.md | 250-252 | High | Maintainability | “CP-B0 新产性能条款分类”没有字段名、schema、来源锚、内容摘要、validator、迁移或状态机接口；`derive_mode` 无稳定输入契约。 | 先定义版本化 contract 和 `validate_taskdoc_input` 校验，再实现纯函数派生；未知分类必须报错。 | fixed | 1 |
| 23 | plan.md | 257-258 | High | Maintainability | `change.kind` 的新词只同步模板与 prose，没有中央可执行词表；未来 `derive_mode` 若读取它，会形成三份真源。 | 把词表放入单一 Layer-0 常量/schema，模板和文档从其生成或做同步门。 | fixed | 1 |
| 24 | plan.md | 259-260 | High | Compliance & Standards | 用 change class 给 `runner_form` 默认 `cpp_extension` 违反 AGENTS.md §4/§5.1：runner form 必须由实际接口形态、header/example 和 DUT 接入方式确定，PR 改动类别不能推出执行形态。 | 由接口探测结果唯一派生；缺失或冲突就询问用户，不提供 change-class 默认值。 | fixed | 1 |
| 25 | plan.md | 282-287 | High | Correctness & Reliability | W8 要用 `cpp_extension` 跑“同一个 PR 快照”，但当前 GaussianBlur spec 明写本地 snapshot、无 PR head（`gaussian_blur.spec.json:99`）；driver 在 `cpp_extension_driver.py:119-124` 强制要求 40 位 PR head。按计划无法进入真实验收。 | 先取得规范 PR URL/head 与 vendor build receipt；否则把预期终态明确写成 provenance BLOCKED，不称 CP-A..E 真机验收完成。 | fixed | 1 |
| 26 | plan.md | 282-283,329-331 | High | Compliance & Standards | 真机 build/run 和批量删除前没有独立确认点，也没有读取 `OPRUNWAY_MACHINE_PROTECTED_ROOTS`；仅写“干净目录”不满足 AGENTS.md §5.2/§5.3。 | W8 前增加显式用户确认、机器 profile/protected roots 读取和新执行目录证明；删除另行确认并限定精确目标。 | fixed | 1 |
| 27 | plan.md | 193-196 | High | Correctness & Reliability | 对新 downgrade gap，“加入 `_FINDING_GAP_KINDS`”本身不够。`validate_acceptance_state.py:253-267` 的 collector 仍只按两个 dtype kind 分支，新 kind 不会获得 backing validation。 | 为该 kind 写独立结构校验与归并器，并测试伪造、缺字段、spec/caseset 漂移及 `passed_with_gaps` 无 backing 的拒绝路径。 | fixed | 1 |
| 28 | plan.md | 316-319 | High | Testing & Validation | 计划要把现有 `gpu_lib → tier4 blocked` 测试改成 downgrade 测试，会删除“未应用政策/无映射时 GPU 必须继续 blocked”的关键负例。 | 保留原测试，新增 `apply_device_policy` 测试；覆盖未应用、映射缺失、映射未知、授权核验失败和成功降级五类。 | fixed | 1 |
| 29 | plan.md | 240-261,301-323 | High | Testing & Validation | V0–V6 没有针对 W5 的分类器、互斥关系、绝对性能门限、缺 authorization、未知 change kind 的测试。性能裁决链新增未测分支按要求应为 High。 | 增加 perf_mode、gen_cases、perf_compare、validate_acceptance_state、run_workflow 的端到端矩阵。 | fixed | 1 |
| 30 | plan.md | 263-270,301-323 | High | Testing & Validation | 验证章节没有 W6 的数组 attr、参数重排、standard/extended stage2、错误顺序、未知 ABI 和 Median 回归测试。 | 增加 codegen 编译测试、manifest/driver 对账、extended 真机见证与 standard Median 回归；未知形态必须 fail-closed。 | fixed | 1 |
| 31 | plan.md | 94-149,313-315 | High | Testing & Validation | V2 只要求 status 覆盖，没有路径逃逸、软链、重定向、分页、资源上限、重复 case、错误 op、嵌套未知字段、NaN/Inf 阈值和 branch TOCTOU 测试。 | 补齐安全与对抗性 fixture；HTTP 行为用 mock server/录制响应，不能只用成功片段。 | fixed | 1 |
| 32 | plan.md | 150 | High | Documentation & Knowledge Transfer | 对 `test_validate_acceptance_state.py:271-294` 的说明是假的：这些测试只验证 dtype_required/dtype_deferred，不验证“任务书要求的 caseset 没跑且 gap 缺失”。该错误引用正掩盖 W2 的 fail-open。 | 删除该依据，并为 taskdoc caseset 覆盖另建真实验收门和测试。 | fixed | 1 |
| 33 | plan.md | 54,255-256 | High | Documentation & Knowledge Transfer | “仅 6 份 spec 携带 change.kind”不实。当前 `plugin/samples/specs` 有 9 份，`plugin/workflows/archive_ops` 另有 2 份，共 11 个文件；计划的逐份回归会漏过 5 个。 | 通过 `rg`/脚本动态枚举所有 spec，不写死数量，并逐文件记录行为差异。 | fixed | 1 |
| 34 | plan.md | 172-173 | High | Security & Risk | 搬运到 `<ops_root>/<op>/golden.py` 未定义已有文件冲突、原子写、备份或用户确认，会覆盖用户/既有 golden。 | 原始 payload 与生成 wrapper 使用新内容寻址路径；目标存在且摘要不同立即停止，覆盖必须单独授权。 | partial（wrapper 未明确内容寻址；写入原子性与获准覆盖后备份策略缺失） | 1 |
| 35 | plan.md | 164-173 | High | Correctness & Reliability | W2 允许每条 case 指向不同 `<file>:<func>`，W3 却只落一个 singular `golden.py`/函数名；多函数或多文件 caseset 状态未定义。 | 要么 contract 明确只支持全 caseset 单一函数并拒绝其它形态，要么实现 per-case golden dispatch 并完整绑定摘要。 | fixed | 1 |
| 36 | plan.md | 94-149 | Medium | Maintainability | 两个新脚本没有版本化 JSON schema、严格顶层键集、CLI 退出码、原子写/重跑幂等和 producer logic hash 契约。下游只能依赖散文。 | 先定义 Layer-0 schema 和内容寻址 domain，再实现 Layer-1 producer/consumer 及坏工件测试。 | partial（两个新 CLI 退出码 + taskdoc_caseset 制品 schema/原子写/logic hash 未定义） | 1 |
| 37 | plan.md | 305-309 | Medium | Testing & Validation | W1 只会列任务书实际链接的 `gaussian_blur/` 目录，不能凭该接口枚举同级全部 17 个 sibling；V0 按文字不可执行。单个留出任务书也不足以验证多 schema 泛化。 | 增加明确的 research-only parent-listing 输入和冻结清单；留出集覆盖至少本地/远端、多算子、无 caseset、非 AscendOpTest 四类。 | fixed | 1 |
| 38 | plan.md | 310-312 | Medium | Testing & Validation | 基线允许 10 个失败但未要求比对失败 nodeid；新增失败替换旧失败时仍可能保持 “1903/10” 而假绿。当前工作区还已有未提交的 `perf_mode.py` 代码变更。 | 在干净基线提交记录完整 nodeid/日志摘要；验证要求旧失败集合完全相同且新增测试全绿。 | fixed | 1 |
| 39 | plan.md | 261,280-287 | Medium | Correctness & Reliability | “gaussian_blur.spec.json 字节不变”与 W4/W8 要新增 `precision.case_source=taskdoc`、改 `runner_form=cpp_extension` 相互矛盾。 | 把字节不变限定为 W5 单项隔离测试，或改为只要求无关 spec 不变并明确 GaussianBlur 的预期 diff。 | fixed | 1 |
| 40 | plan.md | 305-307 | Medium | Compliance & Standards | 计划把网络拉取和批量 schema 探测笼统称为“零成本本地只读”；它虽不做 NPU compute，但仍有网络和落盘副作用，且没有说明产物只能进 reports。 | 明确本地只做受限只读探测、缓存落 `reports/`，并把所有生成/pytest/golden 计算留在远端容器。 | fixed | 1 |
| 41 | plan.md | 68-279 | Medium | Compliance & Standards | 这是 durable 设计和组件建设计划，却没有 AGENTS.md §5.9 要求的 canon architecture/decisions/lint grounding 步骤。 | W0 前增加 canon grounding 与冲突记录，未 settle 内容不得作为门禁依据。 | fixed | 1 |
| 42 | plan.md | 313-315 | Medium | Testing & Validation | “真实片段、不编造、覆盖全部 status”不可操作：`http_error`、歧义、资源截断、坏字段等必须通过合成/变异或 mock 才能稳定覆盖。 | 保留真实成功 fixture，同时使用最小合成 adversarial fixtures 和受控 HTTP mock。 | fixed | 1 |
| 43 | plan.md | 258 | Low | Documentation & Knowledge Transfer | `spec_schema_template.jsonc:22` 行号不准，当前 kind 词表在 `plugin/acc-common/spec_schema_template.jsonc:23`。 | 更新引用，或改用稳定章节/字段名避免行号漂移。 | fixed | 1 |

## Round 1 verify（gpt-5.6-sol / high / read-only，thread 019fd069-210b-7f43-bf8a-364c0075bccd）

38 FIXED · 5 PARTIAL · 0 NOT-FIXED · 0 REGRESSED。按 AGENTS.md §5.7「一轮即停」，未自动迭代第二轮。

### v3 新引入（不在原 43 条内）

| # | Finding | Status |
|---|---------|--------|
| NEW-1 | `golden_designation_conflict` 是 v3 新增的 fail-closed 状态，但未纳入 W-1 registry，也未定义独立 schema、归并器或状态机接口 | open |
