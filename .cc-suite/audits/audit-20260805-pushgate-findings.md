# Push 前审修门 findings（AGENTS.md §5.7）

**范围**: `origin/worktree-oprunway18`(6f77d30)..HEAD — 9 commit / 51 文件 / +9128 行
**Model**: gpt-5.6-sol | 代码审 effort=high（三批并行）· 散文审 effort=low
**Threads**: verdict `019fd3ef-4885-7d70-9660-eb219735062b` / provenance `019fd3ef-487e-72a3-81f2-dfeebae2ae64` / taskdoc `019fd3ef-4880-7731-b99e-1442fbb0cb79` / prose `019fd3ef-4810-7b40-8eb0-218284fb2179`
**Status values**: open | fixed | not-fixed | deferred(既有) | deferred(需拍板)

| # | 面 | 位置 | 严重度 | 维度 | 问题与修法 | Status |
|---|----|------|--------|------|-----------|--------|
| 1 | prose | plugin/acc-common/precision_policy.py:827-848；AGENTS.md §5.11；plugin/skills/acc-precision/SKILL.md:18-27 | Critical | 文档声称“按方法族映射，同族无 CPU 对应则 fail-closed”，但实现只接收粗粒度 `gpu_lib`，并无条件把所有 `gpu_lib` 解析成 `opencv_cpu`。这样 cuSPARSE、其他 CUDA 库等也会被错误映射成 OpenCV CPU，既不是“同族”，也不会 fail-closed。 | 把声明口径细化为可区分方法族的受控值或结构字段，如 `opencv_cuda`、`cusparse`；映射表只加入有明确同族 CPU 对应的项。无法识别具体族时返回不可解析并阻断。若短期只支持 OpenCV，文档必须明确写“当前仅支持 OpenCV-CUDA → OpenCV-CPU”。 | fixed |
| 2 | provenance | plugin/acc-common/cpp_extension_codegen.py:188 | Critical | provenance / fail-open | 预检只用 `spec_sha256` 绑定；同一 spec 配上另一份 PR head、本地快照或 header 的旧预检仍会被接受。错误的 stage2 ABI 可进入验收并产出裁决。 \| 同时核对 `source_facts_digest`、`declared_source_form`、head/merkle/scope，并把预检工件摘要写入 manifest、receipt，最终门重新核验。 | not-fixed（本轮未修，见下方分诊） |
| 3 | provenance | plugin/acc-common/cpp_extension_codegen.py:231 | Critical | stage2 evidence / fail-open | 没有 CP-C0 预检时，spec 自报 `standard`/`extended` 会被标成 `spec_declared` 且不产生 `stage2_form_unverified`；验收门因此把未经 header 核实的 ABI 当成有据事实。stage2 是 header ABI，不能由 spec 自证。 \| 验收候选只接受绑定当前源码的预检结果；spec 声明最多用于开发提示，必须继续挂 `stage2_form_unverified` 并阻断裁决。 | not-fixed（本轮未修，见下方分诊） |
| 4 | provenance | plugin/acc-common/vendor_build_receipt.py:388 | Critical | vendor binding / fail-open | `custom_opp_path()` 只验证目录尾部为 `op_api/lib/<任意文件>`，不要求被收据绑定的是运行时实际打开的规范 `libcust_opapi.so`。收据可绑定 `libfoo.so`，环境反推仍成功，但 helper 会从同目录的另一 ELF 解析符号，最终报告错误归属。 \| 要求规范库名，并以 `dladdr`/映射 inode 核验实际解析出的 phase1、phase2 函数地址都来自收据绑定的同一 ELF，而不只核目录。 | not-fixed（本轮未修，见下方分诊） |
| 5 | provenance | plugin/acc-common/cpp_extension_driver.py:202 | Critical | symbol ownership / fail-open | 只检查并记录 `aclnnXxx`，没有检查 `aclnnXxxGetWorkspaceSize`。官方 helper 会分别查找两个符号；phase1 缺失时可回退到其它 custom/default/feature 库，形成“workspace 来自 A、执行来自 B”，收据仍宣称全部由指定 vendor 提供。 \| 同时要求并记录两个符号；调用 helper 后用 `dladdr` 对两者地址做精确 ELF 归属校验，任一回退或跨库立即阻断。 | not-fixed（本轮未修，见下方分诊） |
| 6 | provenance | plugin/acc-common/validate_acceptance_state.py:1013 | Critical | adjudication provenance / fail-open | 最终门只验证 vendor receipt 自洽，没有把其 `declared_source_form`、PR head 或 snapshot scope/merkle 与本轮 CP-C0/source_facts 绑定。另一源码或另一 PR 的合法收据可被装入当前用例与 spec，错误 DUT 仍可能获得通过裁决。 \| 最终门必须取得当前 `source_facts_digest`/CP-C0 bindings 并逐字段比较：PR 路径核 head，local 路径核 scope 和 subtree merkle，同时核声明形态、授权和降级台账。 | not-fixed（本轮未修，见下方分诊） |
| 7 | provenance | plugin/acc-common/vendor_build_receipt.py:604 | Critical | build provenance / TOCTOU | 本地源码摘要在 build 前取得，但源码随后可被任意修改；`tree_state_at_emit.matches_pre_build=False` 只记录、不阻断，现有测试还明确接受这一情形。因此可摘要 A、构建 B、再让 B 的 ELF 挂在 A 的 merkle 下。 \| 在摘要对应的不可变副本/worktree 中执行构建，或对明确的构建输入清单在执行前后重核；生成物放到树外。任何参与构建的源码漂移必须拒绝收据。 | not-fixed（本轮未修，见下方分诊） |
| 8 | provenance | plugin/acc-common/vendor_build_receipt.py:607 | Critical | PR identity / forged head | PR 生产路径只检查调用者提供的字符串是否为 40 位 hex，完全不从 `build_cwd` 的 Git worktree读取或验证 HEAD。任意合成的 40 位 hex 都能成为 `VERIFIED` receipt，正是“伪造 head”路径。 \| 从实际构建 worktree执行只读 `git rev-parse HEAD`，核对预期 CP-C0 head、repo 身份和 tracked-tree 状态；CLI 参数只能作为期望值，不能作为 provenance 证据。 | not-fixed（本轮未修，见下方分诊） |
| 9 | taskdoc | plugin/acc-common/taskdoc_caseset.py:526 | Critical | identity / fail-open | 跨 case 的接口签名未包含 `op_name`，最终又在 :611 仅采用首条 case 的算子名；首条属于目标算子、后续混入其它算子的 caseset 仍会被 recognized 并可能产生通过裁决。 \| 对每条 case 校验规范化后的 `op_name == spec.op`，并加入混合算子负例。 | not-fixed（本轮未修，见下方分诊） |
| 10 | taskdoc | plugin/acc-common/gen_cases.py:945 | Critical | identity / fail-open | caseset 消费端校了 schema，却完全不核 `payload.op`、`source_op_name` 与 `spec.op`；另一个同签名算子的 caseset 可以直接用于当前 DUT。 \| 将两个算子身份字段设为必填并与 spec 逐字规范化对账。 | not-fixed（本轮未修，见下方分诊） |
| 11 | taskdoc | plugin/acc-common/taskdoc_caseset.py:831; plugin/acc-common/gen_cases.py:1198 | Critical | interface contract / fail-open | mapping IR 只用首条 case 对账 IO dtype；生成阶段只核输出名称，任务书 `outputs[].shape/dtype` 从未与 spec、golden 实际输出或最终 caseset 对账。错误的输出描述仍可在 DUT 与 golden 相互一致时得到 PASS。 \| 逐 case 校 role/顺序/dtype；golden 生成后强制核任务书 output shape/dtype，并把核验结果写入证据。 | not-fixed（本轮未修，见下方分诊） |
| 12 | taskdoc | plugin/acc-common/taskdoc_caseset.py:469 | Critical | layout semantics / fail-open | 输入 `format` 不在白名单时只告警，随后规范化时丢弃；输出 format 在 :491 同样处理。未知布局会被静默当普通 ND 数组执行，可能在错误语义上通过。 \| 未知 format 返回 `unsupported_case_field`；支持的 format 必须进入 IR 并与执行布局对账。 | not-fixed（本轮未修，见下方分诊） |
| 13 | taskdoc | plugin/acc-common/gen_cases.py:1120 | Critical | schema / fail-open | `precision.taskdoc_caseset` 声称“只识别 sha256”，却不拒未知键；例如误写 `sha265` 会得到 `sha=None`，静默关闭摘要绑定。 \| 要求键集精确等于 `{"sha256"}`；对象一旦出现，sha256 必须为非空合法摘要。 | not-fixed（本轮未修，见下方分诊） |
| 14 | taskdoc | plugin/acc-common/gen_cases.py:956 | Critical | evidence completeness / fail-open | `mapping_ir` 只要求非 `None`，空对象或任意形状均可通过；后续计划阶段也不消费或复核它，因此缺失接口映射证据仍可产裁决。 \| 对 mapping IR 建严格版本化 schema，并逐项反查 caseset 与 spec 的 role/顺序/dtype/attr 组合。 | not-fixed（本轮未修，见下方分诊） |
| 15 | taskdoc | plugin/acc-common/gen_cases.py:977 | Critical | content integrity / fail-open | `content_sha256` 只校字符串形态，不按 producer 的 domain 复算；`case_id`、`materialize.seed` 也未从内容与任务书摘要重新派生。配合可选的整件摘要绑定，篡改后的 case 可被当成原任务书 case 验收。 \| 复算 content digest、case_id 和 seed；要求 caseset 整件摘要绑定到可信的 CP-A/source-facts 产物。 | not-fixed（本轮未修，见下方分诊） |
| 16 | taskdoc | plugin/acc-common/taskdoc_caseset.py:249 | Critical | provenance / fail-open | `_read_local` 仅在 sha256 非空时比较；删掉 `taskdoc_links.json` 中的 sha 后，修改过的本地 cases/golden 会被接受。 \| `fetched` 条目的 sha256 必须存在、合法且逐字匹配，缺失直接拒绝。 | not-fixed（本轮未修，见下方分诊） |
| 17 | taskdoc | plugin/acc-common/gen_cases.py:1061 | Critical | materialization / fail-open | `generator_version` 接受任意非空值，`seed_derivation`完全不校；分布也没有与 dtype 家族绑定，例如 float 输入可声明 `uniform_int`。引擎会仍按当前算法造数并冒充指定版本的任务书输入。 \| 版本必须精确等于支持值；复算 seed；按 dtype 家族强制唯一分布并拒绝互斥字段。 | not-fixed（本轮未修，见下方分诊） |
| 18 | taskdoc | plugin/acc-common/taskdoc_caseset.py:365; plugin/acc-common/gen_cases.py:1279 | Critical | numeric correctness / fail-open | value range 只校有限性，不校能否由 dtype 表示；如 int8 `[0,1000]` 会在最终 cast 时回绕，浮点超界会变 Inf，实际字节不再属于账面值域，但 DUT 与 golden 仍可能在这些错误输入上同时通过。 \| 在 producer 和 consumer 两侧按 dtype 校上下界及转换后有限性，落盘前反查实际数组 min/max。 | not-fixed（本轮未修，见下方分诊） |
| 19 | taskdoc | plugin/acc-common/gen_cases.py:3264 | Critical | golden binding / fail-open | taskdoc caseset 与 `<ops_root>/<op>/golden.py` 独立加载，未对账 caseset 中的 `golden_original.sha256`、`mapping_ir.golden_call` 或 caseset 摘要。陈旧或错误 wrapper 可与新 caseset 混用。 \| wrapper 导出并绑定 caseset/mapping/original 摘要；`gen_cases` 加载后逐字段核验。 | not-fixed（本轮未修，见下方分诊） |
| 20 | taskdoc | plugin/acc-common/taskdoc_caseset.py:1315 | Critical | execution isolation / fail-open | 任务书外部 golden 在判定进程内执行；`BaseException` 捕获挡不住 `os._exit(0)`、本地扩展终止或进程状态篡改，可让工作流以成功码提前结束而无裁决。 \| 在隔离 worker/容器执行，父进程把 EOF、缺结构化结果或异常退出统一判 BLOCKED；移除 token/网络并限制读写与资源。 | not-fixed（本轮未修，见下方分诊） |
| 21 | taskdoc | plugin/acc-common/gen_cases.py:929 | Critical | schema evolution / fail-open | loader 明确允许所有层级的未知键且没有受控扩展命名空间；新增或拼错的语义字段可能被静默忽略后继续裁决，违反未知字段 fail-closed。 \| 每层使用精确键集；确需元数据时只允许专门的 `metadata`/`extensions` 命名空间并版本化。 | not-fixed（本轮未修，见下方分诊） |
| 22 | verdict | plugin/acc-common/perf_mode.py:195 | Critical | fail-open / 授权 | `taskdoc_snapshot_sha256=None` 被明确接受，直接违反 §5.10“ground + cite + quote + taskdoc_snapshot_sha256 缺一 fail-closed”。无快照绑定的自报仍可开启 `measure_only`，最终可走 exit 0。 \| 只接受 64 位小写 SHA；同时校验对应任务书快照及 cite/quote，缺快照必须拒绝授权。 | not-fixed（本轮未修，见下方分诊） |
| 23 | verdict | plugin/acc-common/validate_acceptance_state.py:2000 | Critical | fail-open / 验收门 | 验收门只检查授权是 dict 且 ground 在枚举中，不调用 `measure_only_authorization()`，也不检查 cite、quote、快照锚；`{"taskdoc_requirement":"gpu_comparison"}` 即可打开宽档。新增的 `change_class_no_perf_comparison` 也未与 `change.kind` 交叉核。 \| 在 caseset 中持久化受控 `change.kind` 与 spec 绑定；门调用完整授权校验，并对第三种 ground 独立复核 change kind。 | not-fixed（本轮未修，见下方分诊） |
| 24 | verdict | plugin/acc-common/run_workflow.py:622 | Critical | fail-open / 最终裁决 | 完成 `measure_only` 后无条件生成 `PASSED_PRECISION_PERF_MEASURED_ONLY`，且 `_exit_code()` 在第 219 行返回 0；没有任何逻辑强制 GPU 比值条款或改动类别下仍存在的比值/绝对/吞吐条款进入 `task_pr_gaps`。未验收条款因此能被机读为通过。 \| 为未验收性能条款定义结构化 gap，并由门从授权 ground/任务书事实双向复核；存在此类 gap 时不得生成 passed 状态或 exit 0。 | not-fixed（本轮未修，见下方分诊） |
| 25 | verdict | plugin/acc-common/validate_acceptance_state.py:760 | Critical | fail-open / 终态一致性 | `golden_unavailable` 门只检查对应逐行不是 pass；没有要求非空合法台账必须反映到 `overall.verdict`、`overall.golden_unavailable` 和计数。若 validator 回归或产物被改成 overall=`pass`，门仍可能放行，新增 BLOCKED 终态没有独立防回归闸。 \| 对账台账、overall 名单及计数；只要存在 golden-unavailable case，就明确拒绝 `pass`、`passed_with_risk`、`passed_with_gaps` 等 pass-like 终态。 | not-fixed（本轮未修，见下方分诊） |
| 26 | verdict | plugin/acc-common/validate_acceptance_state.py:887 | Critical | fail-open / ABI 证据 | stage2 门把 `manifest.degradations=[]` 当作已有证据，却不核 `variants[].stage2_form_source`，甚至 `variants` 缺失、非列表或空列表都会因 `or []` 静默通过。缺失 header/spec ABI 证据仍可进入验收裁决，可能造成错误 arity 的静默错值并最终通过。 \| 要求非空、结构合法的 variants；校验 source 受控词表，`historical_default` 必须对应 degradation；`preflight`/`spec_declared` 必须分别绑定可复核的预检或 spec 声明。 | not-fixed（本轮未修，见下方分诊） |
| 27 | verdict | plugin/acc-common/precision_policy.py:827 | Critical | precision oracle / 错标杆 | `gpu_lib` 的既有语义同时覆盖 OpenCV-CUDA、cuSPARSE 等不同方法族，却被统一映射成 `opencv_cpu`。一旦接线，非 OpenCV GPU 口径会被静默换成无关 CPU 实现并可能得到假通过；也违背“同族无 CPU 对应则 fail-closed”。 \| 将方法族编码细化为可区分的受控值或基于明确 reference API 映射；未知族返回不可解析状态并阻断，禁止 `gpu_lib` 一键映射。 | fixed |
| 28 | prose | plugin/samples/specs/gaussian_blur.spec.json:32 | High | `_authorization_note` 仍写“GPU 口径没有证据、不主张已满足、应按 fail-closed 缺口挂账”，与同文件 `task_pr_gaps` 第 109 行以及 AGENTS.md §5.11 的“已满足、不产生 gap”正面冲突。这正是把 §5.11 当成降级取证的第二处遗留。 | 删除该段旧判断，改为明确的解析记录：“原文 GPU → 按 §5.11 解析为 OpenCV CPU → 条款已满足、不挂 gap”；保留阈值来源独立说明即可。 | fixed |
| 29 | prose | plugin/samples/specs/gaussian_blur.spec.json:112 | High | spec 仍把本轮 `--pr-snapshot` 写成 `completeness=snapshot_only`、PR provenance 降级，要求报告“未绑定 PR head”；这与 AGENTS.md 当前能力边界、handoff §2 以及 workflow CP-A 的 `declared_source_form=local_source → complete、degradations=[]` 冲突。它会把合法的一等本地输入误报成降级。 | 改成中性的 source-form 记录：`local_source`、`head_sha=null`、绑定 snapshot merkle；不得写 `snapshot_only` 或“PR provenance 降级”。只有声明 `git_pr` 却实得快照时才保留该 gap。 | fixed |
| 30 | prose | plugin/skills/acc-spec/references/taskdoc-to-spec.md:619,730；plugin/samples/specs/gaussian_blur.spec.json:71-72；plugin/acc-common/gen_cases.py:1170-1189 | High | 同一份抽 spec 指南两处明确说 taskdoc 档“不写 `case_target`”，但实现强制 `case_target` 精确等于用例数，GaussianBlur 样例也实际写了 169。读者按指南省略后会得到默认 50，并被实现直接拒绝，流程无法跑通。 | 统一成实现口径：taskdoc 档必须写 `case_target`，且精确等于规范化 caseset 条数；删除“不适用/不写”表述。或者修改实现，使 taskdoc 档直接从 caseset 派生条数且禁止显式 `case_target`，二选一。 | not-fixed（本轮未修，见下方分诊） |
| 31 | prose | plugin/skills/acceptance-workflow/SKILL.md:231 | High | CP-B 的 dry-run 命令没有在 `case_source=taskdoc` 时传 `--taskdoc-caseset`，而同文 210-213 行及实现都规定缺参必须 fail-closed。读者逐字执行文档会在准备阶段失败。 | 将命令分成两档：generated 保持原命令；taskdoc 追加 `--taskdoc-caseset <work>/taskdoc_caseset.json`。同时把后面的检查项按两档分别描述，避免对 taskdoc 档声称会检查网格预算和强制特殊场景。 | not-fixed（本轮未修，见下方分诊） |
| 32 | prose | dev-doc/oprunway-changes-brief.md:41-46,59-63 | High | 同一天倒序记录中，较新的条目已经说 `gate_task1` 认识 `golden_unavailable`、终态保持 BLOCKED；紧接着的“本轮结论”却又称 gate 不认识它，并给出旧终态 `BLOCKED(验收门未过)` 和已经修复的 golden 锚阻断。没有任何“历史 checkpoint/已作废”标记，读者无法判断哪个才是当前事实。 | 将 59-63 行明确标成“早期 checkpoint，已被上方结果取代”，或直接改写为最终产物：`BLOCKED_GOLDEN_UNAVAILABLE`、`gate.passed=true`、5 条无真值；删除已经解除的两个 blocker。 | not-fixed（本轮未修，见下方分诊） |
| 33 | provenance | plugin/acc-common/cpp_extension_codegen.py:507 | High | memory/lifecycle | extended 路径的上下文释放顺序与[官方 `EXEC_NPU_CMD_V1_EXT`](https://github.com/Ascend/op-plugin/blob/master/op_plugin/utils/op_api_common_base.h)不一致：遗漏 `ReleaseExecCommonCtx()`，却在下发 lambda 内调用本应位于 `RunAclCall` 之后、调用线程上的 `UnInitExecCommonCtx()`；而 workspace 查询、分配或 phase2 抛异常时又会跳过释放。这会造成线程上下文泄漏/错线程清理，并污染后续 case。 \| 用 RAII 保证所有退出路径恰好释放一次；保持官方的线程边界和顺序。若当前库无法提供等价的 `ReleaseExecCommonCtx`，extended 形态应 fail-closed，不能省略清理继续验收。 | not-fixed（本轮未修，见下方分诊） |
| 34 | provenance | plugin/acc-common/test_cpp_extension_codegen.py:187 | High | test coverage | 测试反而断言 `ReleaseExecCommonCtx` 不得出现，固化了上述生命周期缺陷；整条 extended 分支也只有源码字符串断言，没有覆盖 workspace 查询失败、phase2 失败、连续多 case 或 task-queue 下的真实释放行为。 \| 修正错误断言，并在远程 NPU 容器增加编译与执行测试，注入各阶段失败，验证资源恰好释放一次且后续 case 不受污染。 | not-fixed（本轮未修，见下方分诊） |
| 35 | provenance | plugin/acc-common/cpp_extension_driver.py:417 | High | error classification | `except Exception` 把 materialization、schema、内存、读回、文件系统及框架基础设施故障全部降成单 case `execution_failed`，随后统一归因到 DUT 功能失败。这会把 harness/环境缺陷错误裁成算子 FAIL，并继续生成“完整执行”工件。 \| 只捕获明确的 DUT 调用异常；materialize/readback/基础设施异常应终止整轮或落独立 `harness_error/evidence_incomplete`，由验收门收敛为 BLOCKED/needs_review。 | not-fixed（本轮未修，见下方分诊） |
| 36 | taskdoc | plugin/acc-common/taskdoc_caseset.py:136 | High | genericity | 通用 Layer-1 代码内置 `cv2.GaussianBlur` 的签名和默认值，本质上是按具体算子 API 身份特判，违反仓规 5.1。 \| 将参考 API 签名/default 作为受控数据放入 spec、任务书 IR 或独立版本化 registry；通用代码只消费 schema。 | not-fixed（本轮未修，见下方分诊） |
| 37 | taskdoc | plugin/acc-common/taskdoc_caseset.py:852; plugin/acc-common/taskdoc_caseset.py:1091 | High | interface mapping | attr 映射及 golden 参数绑定依赖规范化后的符号名相同；`axis`↔`dim`、`input`↔`src` 等合法跨 API 映射会被拒绝，不符合 role/顺序/dtype 对账要求。 \| 由显式 IR 按 role、位置、dtype 和组合结构绑定；名字仅作溯源显示或歧义辅助。 | not-fixed（本轮未修，见下方分诊） |
| 38 | taskdoc | plugin/acc-common/taskdoc_caseset.py:1486 | High | golden freezing | 若任务书原件名正好是 `golden.py`，`original_path == out_path`；先写原件后立即被 wrapper 覆盖，wrapper 随后会对自身计算原件 SHA 并失败。现有代码还会无条件覆盖已有文件。 \| 原件使用内容寻址的独立文件名，拒绝路径碰撞；采用原子 no-clobber 写入，已存在时必须摘要一致。 | not-fixed（本轮未修，见下方分诊） |
| 39 | taskdoc | plugin/acc-common/taskdoc_links.py:428; plugin/acc-common/taskdoc_caseset.py:615; plugin/acc-common/gen_cases.py:922 | High | test coverage | 新增的链接解析/取材、识别结局、映射 IR、caseset 严校、taskdoc 计划与物化主判定链没有对应回归测试；现有 `test_taskdoc_caseset_golden_anchor.py` 只覆盖授权锚和 wrapper 文本。按本轮规则，判定链未覆盖分支为 High。 \| 在远程容器补齐 recognized 与全部非 recognized 结局、混合 op、未知字段、data_path、格式、摘要漂移、输出 dtype/shape、materializer 版本及禁止回退测试。 | not-fixed（本轮未修，见下方分诊） |
| 40 | verdict | plugin/acc-common/precision_policy.py:833 | High | §5.11 集成 | 新 resolver 没有任何生产调用者，解析记录也没有进入 spec/caseset/verdict/report；当前只有单元测试和一个明确标注“未接入 live 流”的 skill 引用。§5.11 实际未进入验收链，无法保证 GPU→同族 CPU，也无法留下要求的解析痕迹。 \| 在任务书解析/规格生成阶段调用 resolver，并将原始方法族、解析结果、依据、阈值异源提示贯穿 golden contract、caseset 和最终报告。 | not-fixed（本轮未修，见下方分诊） |
| 41 | verdict | plugin/acc-common/validator.py:954 | High | 终态优先级 / 错误归因 | `fails` 按 case ID 无条件排除整个 golden-unavailable 名册。若该 ID 的 evidence 实际是 `execution_failed` 或其它真实 DUT 失败，该失败也会被移除，最终可能错误落成 `blocked_golden_unavailable`，违反“真实失败优先”。 \| 仅当 caseset 名册、case 声明和 evidence 的 golden-unavailable 状态一致时单列；同一 ID 的 execution failure 必须保留为真实失败或契约冲突。 | not-fixed（本轮未修，见下方分诊） |
| 42 | verdict | plugin/acc-common/run_workflow.py:445 | High | 编排 / 终态隔离 | measure-only 分流让所有非 `precision_ok` 状态继续 Task3，包括 `blocked_golden_unauthorized`；这与第 450–454 行“跳过 Task3”直接矛盾，并会用未经授权的 golden 结果筛性能子集。 \| 对 unauthorized 设置无条件 skip；measure-only 解耦仅开放给明确允许继续记录绝对耗时的状态。 | not-fixed（本轮未修，见下方分诊） |
| 43 | verdict | plugin/acc-common/validate_acceptance_state.py:1059 | High | 测试覆盖 | 新增的 unjudgeable 证据门没有对应测试覆盖：`execution_failed` 缺错误原文、golden 状态与 caseset 不符、verdict 缺行/错误记 pass 等关键分支均未被测试。 \| 在远程容器增加逐分支正反例，并验证每个坏输入都使验收门失败。 | not-fixed（本轮未修，见下方分诊） |
| 44 | verdict | plugin/acc-common/run_workflow.py:586 | High | 测试覆盖 | 新终态的完整编排分支没有测试覆盖；现有测试只验证 validator 的小写 verdict，未验证 canonical state、exit code、gate 优先级、`precision_ok` 排除及与真实失败并存的端到端行为。 \| 在远程容器增加 `blocked_golden_unavailable` 的 workflow 级测试，并覆盖纯 GU、GU+真实失败、门失败三种路径。 | not-fixed（本轮未修，见下方分诊） |
| 45 | prose | dev-doc/oprunway-session-handoff-2026-08-05.md:20-23,77-79,140；当前 Git 状态 | Medium | 交接中的 Git 基线已经陈旧：文件写 HEAD=`cdd98b1`、相对 `4d1544d` 共 21 commit、最新 8 commit 未 push；当前仓实际 HEAD=`a51a3e9`、共 22 commit、相对 `origin/worktree-oprunway18` 有 9 commit。下一步仍让读者审“8 个 commit”，会漏掉一个提交。 | 更新 HEAD、提交总数、未 push 数量和范围；最好不要手写易漂移的连续区间，改附可执行核对命令 `git rev-list --count origin/worktree-oprunway18..HEAD` 与 `git log --oneline origin/worktree-oprunway18..HEAD`。 | not-fixed（本轮未修，见下方分诊） |
| 46 | prose | dev-doc/oprunway-session-handoff-2026-08-05.md:4；AGENTS.md 开头及 §8 | Medium | handoff 要求“先读这份，再读 AGENTS.md”，但 AGENTS.md 自称仓根唯一指令入口，且 handoff 本身可能陈旧。这样会让下一 session 在读到现行仓规前先接受一份状态文档里的命令和规则性表述。 | 改为“先读 AGENTS.md，再按其 §8 读取本交接”；并声明发生冲突时以 AGENTS.md 和确定性产物为准。 | not-fixed（本轮未修，见下方分诊） |
| 47 | prose | AGENTS.md:349-351；dev-doc/oprunway-session-handoff-2026-08-05.md:10,58-72 | Medium | “任务书自带用例集/golden 通路已闭环”“五项泛化能力已经全部落地”表述过满：同一 handoff §5 仍记 CP-B0 为 `NEEDS_USER`，GaussianBlur 最终也是 `BLOCKED_GOLDEN_UNAVAILABLE`。虽然实现链路确实能产工件，但“闭环/全部落地”容易被读成验收能力已经完整通过。 | 收窄为“机制已接通并产出确定性 BLOCKED 终态”或“工程通路闭环，验收结论未闭合”；紧邻注明 5 条无真值、CP-B0 尚需人工补事实，避免与 PASS 混淆。 | not-fixed（本轮未修，见下方分诊） |
| 48 | verdict | plugin/acc-common/gen_cases.py:1295 | Medium | 错误处理 | 捕获 `BaseException` 会把 `SystemExit`、`MemoryError`、`GeneratorExit` 等进程级/实现级故障全部包装成“参考实现算不出 golden”。这会掩盖 golden 代码或运行环境损坏，并错误生成业务 BLOCKED 状态。 \| 只捕获明确可归类为参考实现不支持的异常，至少收窄到 `Exception`；未知异常应终止生成并暴露原始故障。 | not-fixed（本轮未修，见下方分诊） |

## 一轮处置（AGENTS.md §5.7「一轮即停」）

本轮**已修 4 条**（都是本轮自己造成的、且都是「把 §5.11 当降级」的残留）：
- precision_policy `gpu_lib` 整族无条件映射 → 收窄为按**具体库**映射（`opencv_cuda`→`opencv_cpu`），
  粗粒度 `gpu_lib` **解析不出 → fail-closed**，绝不猜一个 CPU 库；AGENTS.md §5.11 同步收窄；
- spec `_authorization_note` 与 `task_pr_gaps[11]` 两处仍按「未验收缺口 / provenance 降级」写 → 改成解析记录与形态记录。

**剩余 44 条如实交用户，未自动迭代。** 分诊：

| 类别 | 条数 | 说明 |
|---|---|---|
| 既有缺陷（非本轮引入） | 多数 Critical | 如 vendor receipt 的 PR head 只校形态不读 worktree HEAD、最终门未把 receipt 与本轮 source_facts 交叉绑定、golden 在判定进程内执行 —— 这些在本轮之前就存在，修它们要动收据契约与执行隔离，属 §5.2 需先给方案的架构改动 |
| 本轮引入但需设计决策 | 若干 High | 如 §5.11 resolver **尚无生产调用者**（只有单测）、taskdoc caseset 的逐 case op_name 对账、mapping IR 的严格 schema |
| 测试覆盖缺口 | 4 条 High | 新增的 unjudgeable 证据门、新终态编排分支、taskdoc 主判定链、extended 生命周期 |
| 文档陈旧/过满 | 4 条 Medium | handoff 的 Git 基线数字、changes-brief 早期 checkpoint 未标作废、「闭环/全部落地」措辞过满 |

⚠ 其中一条值得单独点出：**`resolve_gpu_oracle_to_cpu` 目前没有生产调用者**——§5.11 的仓规已立、
机制已实现并有测试，但**尚未接进 spec 生成 / golden 契约链**。本轮 GaussianBlur 不受影响
（它的 golden 本来就是 CPU、spec 直接写 `opencv_cpu`），但换一份任务书写 `opencv_cuda` 时
规则不会自动生效。这条要接线才算真正落地。
