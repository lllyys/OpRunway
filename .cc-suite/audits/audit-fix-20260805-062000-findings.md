# Audit Findings

**Run**: audit-fix 20260805-062000 | **Scope**: `dev-doc/oprunway-taskdoc-caseset-absorption-plan.md`（v3）
**Audit type**: full，**目标导向重审**（用户需求 13：按「尽快走通」这个最终目的审，确保计划为目标服务）
**Model**: gpt-5.6-sol | **Effort**: high | **Sandbox**: read-only | **Fan-out**: 3 路并行镜头（需求 10）
**Threads**: goal `019fd071-e64d-7d33-8d5e-688761fec66f` / blocker `019fd071-e653-7f72-982d-a34b63f36cec` / coverage `019fd071-e795-7401-af81-e284abc74ac9`
**严重度口径**：Critical=照此执行走不通或严重跑偏；High=显著拖慢或返工；Medium=次要拖累；Low=措辞/引用
**Status values**: open | fixed | not-fixed | partial | regressed | skipped

| # | Lens | Plan line | Severity | Dimension | Finding | Suggested fix | Status | Round |
|---|------|-----------|----------|-----------|---------|---------------|--------|-------|
| 1 | blocker | 173 | Critical | W2／需求4违背 | “不认仍走 generated”直接违反用户需求4，也与 plan.md:135-137 的 fail-closed 规则冲突。任务书明确提供了 caseset 时，识别器失败后回退自生成，会再次得到上一轮那种“流程看似通过、实际绕过任务书用例”的错误产物。 | 只有完整扫描证明任务书没有提供 case 时才能走 `generated`；存在明确 caseset 候选但无法识别、字段不支持、身份冲突或资源受限时必须 BLOCKED，并保留候选摘要和原因。 | fixed | 1 |
| 2 | blocker | 209 | Critical | W4／Task1 数据入口缺失 | 新增 `precision.case_source=taskdoc` 仍没有说明 taskdoc caseset 究竟存在哪里、如何传入生成器。`run_workflow.py:335-337` 只把 `spec` 和工作目录传给 `gen_cases`；`gen_cases.py:2735-2788` 也只从 spec 和 `_plan()` 取数据。W2甚至没有命名其规范化输出制品。照计划实现到这里会立即返工接口。 | W2必须产出版本化、内容寻址的 `taskdoc_caseset.json`；spec只引用其摘要/逻辑身份。为 `run_workflow`、CLI、dry-run ledger 和 `gen_cases` 增加显式输入，并绑定 `taskdoc_links.json`、prototype、原 golden 和映射 IR 的摘要。 | fixed | 1 |
| 3 | blocker | 211 | Critical | W4／任务书 case 无法物化 | 169条 case 只有 shape、dtype、`value_range` 等描述，没有输入数据字节。计划说在 `_plan()` 前分叉，却没有定义如何把它们确定性生成成输入；现有 `gen_cases.py:1376` 起的 `_build_inputs` 使用 workflow 自己的 regime，不能自动等价消费任务书的 `value_range`。 | 定义 taskdoc 专用确定性 materializer：明确 `float→float32`、区间端点语义、分布、逐 case 内容摘要派生 seed、shape/rank 校验，并把 seed与策略写入 caseset。否则不能声称“使用了任务书 case”。 | fixed | 1 |
| 4 | blocker | 216 | Critical | W4／golden_unavailable 无法穿过验收门 | “保留 case 并 BLOCKED”目前没有对应的数据模型。`gen_cases.py:2807-2811` 的任一 golden 异常会直接中断全量生成；`validate_acceptance_state.py:687-700` 要求每个 case 有 golden，`:719-726`、`:864-891` 要求证据一一对应，`:1283-1311` 又会实际读取 golden/output。已知超512通道 case 一旦触发 OpenCV 限制，计划无法继续产出 acceptance。 | 增加一等状态 `golden_unavailable`：case身份仍写入完整 manifest，但允许无 golden 文件；执行证据写 `not_run/golden_unavailable`；Task1/Task2门明确将其判为 BLOCKED而非结构损坏。其余可执行 case继续跑，最终仍生成可核验 acceptance 产物。 | fixed | 1 |
| 5 | blocker | 219 | Critical | W4／性能仍会零数据 | 计划没有规定 taskdoc 精度 case 如何成为性能候选。现有 `gen_cases.py:337-470` 只选择带“性能”维度或匹配 include tag 的 case；如果169条规范化 case仅标精度，就会得到零性能 case，再次复现上一轮性能挂起，无法满足需求2的 msprof 绝对耗时输出。 | 当 `perf.case_source=precision_cases` 时，将所有成功物化且满足硬件/资源门的 taskdoc 精度 case放入候选池，再应用现有 `max_cases`、大小 shape 分档和采样规则；性能 case ID必须引用原精度 case ID。 | fixed | 1 |
| 6 | blocker | 248 | Critical | W5／用户需求2被改写 | 删除“四类场景使用 `cpp_extension`”与用户逐字需求2及 plan.md:38 已确认口径矛盾。`aclnn_py` 是 Python ctypes 直调 ACLNN，不是 torch 封装；虽然它是现成最快通路，但不能被包装成已经满足需求2。 | 分离“要求的接入表面”和“ABI能力探测”：四类场景要求 torch wrapper；ABI探测只决定通用 `cpp_extension` adapter能否实现。支持时将 spec明确设为 `runner_form=cpp_extension`，不支持则报真实 blocker。若为了尽快走通临时使用 `aclnn_py`，产物必须明确标注需求2尚未满足，并取得用户对该临时降级的同意。 | fixed | 1 |
| 7 | blocker | 272 | Critical | W7／CP-A..E 状态机漏改 | W7只写了 CP-A、CP-B、CP-B0、CP-E，漏掉真正会卡住的 CP-C/CP-D。`plugin/skills/acceptance-workflow/SKILL.md:160` 要求 CP-C 生成完整 caseset和信任收据；`:169-177` 的 CP-D仍要求精确40位 head。新 taskdoc case身份、`golden_unavailable`、local_snapshot和新 performance selection都无法通过这些旧门。 | 同步修改状态制品表与恢复校验；CP-B dry-run绑定规范化 caseset；CP-C验证完整 taskdoc manifest、映射和 unavailable 状态；CP-D按最终执行路径验证 local_snapshot或40位head；CP-E消费对应门结果。还要更新 `verify_aclnn_harness.py:278-350` 的 caseset绑定逻辑。 | fixed | 1 |
| 8 | blocker | 282 | Critical | W8／真机前置无可行解 | 计划自己确认 `cpp_extension_driver.py:119-124` 强制40位 `pr_head_sha`，`validate_acceptance_state.py:824-848` 还会再次拒绝；本地无 `.git` 的输入只能由 `fetch_source.py:576-637` 产生 `head_sha=null`。但W8只给“取得不存在于当前输入的PR head”或“提前BLOCKED”两条路，两条都不能完成用户要求的端到端验收。 | 最短严格解是让 cpp_extension receipt与门支持通用 `local_snapshot` provenance：绑定整树/目标子树 Merkle、snapshot scope、构建命令、vendor ELF摘要和工作目录，复用 `source_provenance.py:230-301` 的核验模式。最快临时解是用已跑通的 `aclnn_py` local_snapshot通路完成一次E2E并如实登记“非torch封装”，随后再补 cpp_extension；两者不能混称。 | fixed | 1 |
| 9 | coverage | 240 | Critical | 需求2·性能口径 | v3 明确否决按四类场景派生 `measure_only`，改成仅按任务书性能条款分类；因此 `new_op`、扩 dtype/shape/rank 一旦任务书写绝对门限、吞吐或非 GPU 比值，就不会按用户要求统一“只报 msprof 绝对耗时”，甚至会 fail-closed。 | 增加用户已授权的 `change_class_no_perf_comparison` ground；从经过任务书/PR 事实核验的 `change.kind` 派生，并使它对四类场景优先落 `measure_only`。仍要求真实 msprof 数据，但禁止 baseline、ratio 和达标宣称。 | fixed（round1 verify 后追补） | 1 |
| 10 | coverage | 248 | Critical | 需求2·torch 封装 | line 38 已确认“torch 封装 = `cpp_extension`”，这里却删除四类场景的 `cpp_extension` 规则，降成“逐份探测、冲突再问”。这使需求 2 不再成立，并允许 GaussianBlur 再次走回 `aclnn_py`。 | 四类场景的 spec 生成必须明确写入 `runner_form: cpp_extension`；接口预检只决定“支持或 BLOCKED”，不得静默改选 `aclnn_py`。`run_workflow` 仍可继续只从 `spec.runner_form` 派生 mode，不违反单一真源。 | fixed（round1 verify 后追补） | 1 |
| 11 | coverage | 173 | Critical | 需求4·任务书 case | 对已经明确提供 caseset 但“不认”的情况仍回退 `generated`，正面违反“任务书提供 case 就用它，不再自行生成”。层层对账一旦遇到未知字段、attr 漂移或缺默认值，最终很可能验的是自生成用例。 | `generated` 只允许在完整链接探测确认“任务书未提供 caseset”时使用。已提供但暂不能规范化时必须保留原 case 身份并 BLOCKED，修正 spec/通用适配器或询问用户；不得换成生成用例继续验收。 | fixed | 1 |
| 12 | coverage | 280 | Critical | 需求7/9·provenance | W8 只允许“取得不存在于输入中的 PR head”或“在 provenance 阶段 BLOCKED”。第二条不会完成 cpp_extension build、精度和 msprof，不能算流程走通。源码确实在 [cpp_extension_driver.py](/Users/ll/Desktop/workspace-ascend/OpRunway/.claude/worktrees/oprunway18/plugin/acc-common/cpp_extension_driver.py:119) 强制 head，但 [source_provenance.py](/Users/ll/Desktop/workspace-ascend/OpRunway/.claude/worktrees/oprunway18/plugin/acc-common/source_provenance.py:12) 已有通用 `local_snapshot + merkle + head=null` 契约，因此不是只能二选一。 | 把 vendor build receipt 升级为按 `source.kind` 分流的契约：`git_pr` 绑定 40 位 head；`local_snapshot` 绑定 snapshot scope、完整 manifest/Merkle、构建 argv、ELF hash，并显式记 `pr_head_unbound`。复用现有 `source_provenance.bind()`，不得伪造 commit。 | fixed | 1 |
| 13 | goal | 95 | Critical | W-1 / 兼容性 | “未知 kind → BLOCKED”没有迁移现有自由文本 gap；GaussianBlur spec 现有 10 条 `task_pr_gaps` 全是字符串，无 `kind`，方向反转后会在新功能执行前直接阻断 | SIMPLIFY：不要先建全局 registry；只为“任务书明确给了 caseset”增加一个专用、摘要绑定的硬路由状态，保留现有 legacy gap 行为。砍掉完整 registry 不影响走通，风险仅是旧 gap 仍非机器强校 | fixed | 1 |
| 14 | goal | 157 | Critical | W2 / 实际输入不兼容 | 计划要求 IO 名称/dtype 与 spec 一致，但真实 prototype 是 `self/out/float`，spec 是 `src/dst/float32`，169 条会全部判 identity mismatch | SIMPLIFY：按 role、顺序、数量、canonical dtype 和 shape 约束对账，显式产映射 `self→src`、`out→dst`、`float→float32`；不能要求跨 API 层名字逐字相同 | fixed | 1 |
| 15 | goal | 164 | Critical | W2 / 无谓人工阻塞 | 把缺失 `border_type` 列为必须询问用户，但任务书指定 golden 已通过省略参数选择 OpenCV 默认边界语义；等待用户会无谓卡住重跑 | SIMPLIFY：将“golden 调用采用的库默认值”落成有来源的 spec 常量绑定；本例等价于 OpenCV `BORDER_DEFAULT`。若未来无法确定库默认值再询问 | fixed | 1 |
| 16 | goal | 173 | Critical | W2 / 违反需求 4 | 任务书明确给了 caseset 后，识别失败仍回退 `generated`，即使记 gap 也仍然违反“有任务书 case 就用任务书 case” | KEEP 硬失败但删 registry 前置：只有“任务书没有提供 case”才允许 generated；明确提供但解析失败时直接停止并报告解析错误 | fixed | 1 |
| 17 | goal | 207 | Critical | W4 / 缺失可执行转换 | W2/W4只描述识别、校验和分叉，没有定义如何把 `shape/data_type/value_range/attr_desc/output_desc` 变成确定性的输入字节、golden 调用和 caseset；原 JSON 没有 `data_path`，缺此步骤无法运行 | KEEP 并补最短转换：以 taskdoc 摘要+case 内容摘要派生 seed，按 `value_range` 生成输入，映射 attrs/outputs，调用指定 `expect_func`，记录生成器版本与摘要 | fixed | 1 |
| 18 | goal | 216 | Critical | W4 / 流程提前终止 | 任一 `golden_unavailable` 就让 Task1/Task2 BLOCKED；已知至少 5 条超 512 通道 case 很可能触发，流程会在性能采集前停下 | SIMPLIFY：保留全部 169 个身份与分母，把不可生成 golden 的条目标为 per-case ERROR/UNVERIFIED，继续执行其余可运行 case；最终结论可 BLOCKED/FAIL，但流水线必须完成 | fixed | 1 |
| 19 | goal | 248 | Critical | W5 / 用户需求冲突 | 计划删除“四类场景→cpp_extension”规则，与已确认口径和需求 2 直接相反；同时当前 spec 仍是 `aclnn_py`，计划没有任何工作项把目标 spec 改为 cpp_extension | SIMPLIFY：四类场景按用户规则派生 `measure_only + torch封装`；GaussianBlur 明确更新为 `runner_form=cpp_extension`。接口不支持时报告 unsupported，不得静默退回 aclnn_py | fixed（round1 verify 后追补） | 1 |
| 20 | goal | 254 | Critical | W5 / 重复拍板 | 用户已经要求四类场景统一只测绝对耗时，计划却再次等待是否增加授权 ground，执行会在开工前停住 | CUT 该拍板点：直接实现用户给定规则；绝对时延/吞吐等未来分类不在本轮扩展，另行 DEFER | fixed | 1 |
| 21 | goal | 284 | Critical | W8 / 必然无法达到目标 | 两个方案只有“找不存在/未知的 PR head”或“provenance BLOCKED”；用户明确输入是无 `.git` 的本地目录，这两条都不能完成 cpp_extension 真机流程 | KEEP W8 但改方案：扩展 vendor receipt 支持 `local_snapshot`，绑定 `head=null + snapshot scope + subtree merkle + ELF digest + build argv`；同时修改 driver 和 adapter 两处校验，不能只改 `cpp_extension_driver.py` | fixed | 1 |
| 22 | goal | 291 | Critical | W8 / 无法产出性能数据 | 计划没有处理 cpp_extension 当前“所有精度 case 全过才采性能”的硬门；只要一个 taskdoc golden 不可运行或 DUT 精度失败，需求 2 的 msprof 绝对耗时就完全没有 | 增加本轮必需改动：`measure_only` 下从已成功执行且精度可判的 case 中选性能子集继续采集，完整保留失败/未验证分母；性能报告不得据此宣称精度或任务书整体通过 | fixed（round1 verify 后追补） | 1 |
| 23 | blocker | 95 | High | W-1／前置工作 | “未知 kind 一律 BLOCKED”会先破坏现有 spec，而不是帮助 GaussianBlur 跑通。`validator.py:613-618` 与 `validate_acceptance_state.py:253-267` 当前允许自由文本及未注册 gap；`plugin/samples/specs/median.spec.json:109-115` 仍有无 `kind` 的结构化条目。计划既未迁移旧条目，也未定义 legacy 兼容边界，却把 registry 放在全部工作的最前面。 | 不要让完整中央 registry 阻塞首轮垂直切片；先只注册本轮真正进入裁决的少量 kind。若坚持立即封闭词表，必须先枚举并迁移全部现存字符串、无 kind 条目和既有 dtype/API kind，再启用 unknown→BLOCKED。 | fixed | 1 |
| 24 | blocker | 140 | High | W1／产物接缝 | `taskdoc_links.json` 记录 blob hash 和 producer hash，但没有明确绑定产生它的任务书快照摘要及 `source_facts` 摘要。现有 `fetch_source.py:576-637` 会为本地任务书/快照生成独立 provenance；缺少该绑定时，任务书变更后 CP 热恢复可能复用旧链接结果。 | schema 中强制加入 `taskdoc_snapshot_sha256`、`source_facts_sha256`、resolved commit SHA，并在 CP-A 恢复时重新计算校验；不能只校验脚本逻辑 hash。 | fixed | 1 |
| 25 | blocker | 157 | High | W2／第一个确定性拒绝点 | 计划要求 IO 名称与 spec 一致，却只设计了 attr 映射。任务书 prototype 使用 `self/out`，现 spec 使用 `src/dst`，所以169条 case 会在进入 golden 前全部成为 `identity_mismatch`。 | 建一个统一、摘要绑定的接口映射 IR，同时描述 input、output、attr：至少包含 `self→src`、`out→dst`、位置、数量、dtype 和 shape 对账。不能只为 attr 做映射，也不能简单要求不同来源的符号名逐字相等。 | fixed | 1 |
| 26 | blocker | 164 | High | W2/W3／不必要的人为阻断 | `border_type` 不需要停下询问用户。冻结的任务书 golden 调用 `cv2.GaussianBlur` 时未传 `borderType`，其可执行语义就是 OpenCV 默认 `BORDER_DEFAULT`；这比猜测更直接。继续把它列为待拍板项会无谓卡住开工。 | 规范化时派生并记录 `border_type=BORDER_DEFAULT`，绑定原 golden 摘要和 OpenCV API 语义；wrapper 同时拒绝非默认值。GaussianBlur 专属适配逻辑本身只需约20–30行：取 `inputs[0]`、拆 `ksize`、重命名 sigma、校验 border、调用冻结函数并从其单元素列表返回值中取数组；泛化部分放在映射 IR，不写算子分支。 | fixed | 1 |
| 27 | blocker | 190 | High | W3／golden 执行接缝 | 计划只指出 `check_golden.py:15-35` 同进程执行的问题，却没有覆盖真正生成用例的路径。`gen_cases.py:502-587` 在主进程导入 golden，`gen_cases.py:2807-2842` 又在主进程逐 case 调用；只改 `check_golden` 不会形成沙箱。若逐 case 启进程，面对最大约777 MiB 输入又会严重拖慢甚至 OOM。 | 首轮采用单个长驻受限 worker/container，通过文件或 mmap 传递大数组；`check_golden` 和 `gen_cases` 共用同一执行服务。若无法立即完成这项重构，应把完整沙箱移出首轮关键路径并明确风险，不能实现一个只保护检查脚本的伪沙箱。 | partial（check_golden 与 gen_cases 共用执行服务的协议） | 1 |
| 28 | blocker | 254 | High | W5／不必要的决策等待 | 当前 GaussianBlur 已有 `measure_only` + `gpu_comparison` authorization，`run_workflow.py:84-109` 会校验它，`aclnn_adapter.py:1302-1435` 已支持只采 custom msprof。先扩建完整性能条款分类 contract并再次要求用户拍板，不是本次跑通的必要条件。 | 本轮沿用现有合法 measure-only 路径，把绝对门限、吞吐和新 ground 分类器移到实测后的改进阶段；不要用待拍板事项阻塞已经明确的用户需求。 | fixed | 1 |
| 29 | blocker | 298 | High | W9／磁盘假两难 | “要么给足空间，要么先设计事务协议”与已记录环境事实不符。`dev-doc/oprunway-real-machine-environment.md:76-85` 已记录 Docker data volume约1.3 TB可用、`/tmp`约376 GB；6.76 GiB及其若干副本无需新协议。把W9当待决项会把最快合法路径变成设计项目。 | 在未受保护的 Docker volume中新建干净执行目录，让 reports、caseset、输出和 msprof临时文件都落该挂载点；运行前用真实物化图做空间/RAM门即可。本轮不做删除式批处理，也无需向用户索要额外空间。 | fixed | 1 |
| 30 | blocker | 307 | High | 总顺序／目标偏离 | 当前顺序要求先完成中央 registry、全链接系统、沙箱、完整性能分类和共享 ABI IR，最后才验证唯一关键问题：能否以合法 provenance跑一次真机。这不是“尽快走通”的顺序，任何一个大改都可能在没有实测反馈前返工。 | 改为垂直切片：先确定 `cpp_extension local_snapshot` 或获准的 `aclnn_py` 临时路径；随后只接GaussianBlur实际需要的GitCode目录、单函数golden、映射IR、169条规范化与性能子集；立即真机跑；再根据阻断日志扩展 registry、沙箱、多golden及完整性能分类。 | fixed | 1 |
| 31 | coverage | 105 | High | 需求3·golden 接口 | “无论任务书要求什么，一律 CPU”及 GPU→CPU 映射，把“使用任务书明确指明的接口”改成了“使用平台统一替代接口”。强制记录 GPU gap 只能诚实承认没有按原接口验，不能补救需求 3 未被执行。 | 通用规则应是优先执行任务书/caseset 明确指定的 `expect_func`。仅本次 GaussianBlur 因用户明确“不考虑 GPU”且自带 caseset 指向 CPU golden，选择该 CPU 接口；把正文 CPU/GPU 冲突写进报告即可，不建立全局 CPU 覆盖规则。 | fixed | 1 |
| 32 | coverage | 40 | High | 需求7·精度口径 | v3 规定任务书 case 阈值优先，但需求 7 对本次 GaussianBlur 明确要求“精度按 workflow 默认”。计划没有说明这是一项后续用户覆盖，因而当前文字直接改变了本轮精度判据。 | 为 W8 写明运行级优先级：复用任务书的 169 条输入/属性组合，但本次裁决使用 workflow 默认精度 policy；若确有后续用户指令要采用 `err_threshold`，必须记录其覆盖了需求 7，而不是同时保留两种说法。 | fixed | 1 |
| 33 | coverage | 289 | High | 需求7·隔离执行 | 计划只指出“干净工作目录”不足，却没有安排用户明确要求的 subagent、干净 session、干净本地/远端执行目录，也没有防止复用上一轮 24-case 产物的具体门。 | W8 应明确：在实现检查点 commit 后启动新 session；通过 acceptance workflow dispatch subagent；本地使用新的报告根，远端使用位于 Docker volume、且不在 protected roots 下的新目录；禁止携带旧 caseset/golden/receipt，仅复用只读源码快照。 | fixed | 1 |
| 34 | coverage | 307 | High | 需求8·实测反馈 | 主序列在 W8 结束；后面只有预先设计好的验证，没有“读取本次真实验收失败 → 区分 workflow 缺陷与 DUT 失败 → 泛化修复 → 重跑”的工作项。需求 8 完全缺失。 | 在 W8 后增加 W10：分析实际产物和首失败点，仅修 workflow/harness 类问题；用同一冻结输入重跑。设置一轮有界优化即可，DUT 真实失败不强修。 | fixed | 1 |
| 35 | coverage | 307 | High | 需求10·fan-out | 顺序图基本为单链，计划全文没有 ultracode fan-out 的任务拆分、汇合点或并行安全约束。 | 把互不写同一文件的 W1 链接取材、W2 fixture/规范化、W6 ABI/codegen 分给独立 fan-out 单元；在 W7 前统一汇合和集成。真机连接、共享报告目录和最终裁决仍串行，避免并行写冲突。 | partial（lane 文件所有权重叠（A/C 共改 gen_cases.py；B/C 共改 cpp_extension_adapter.py）） | 1 |
| 36 | coverage | 320 | High | 需求13·尽快走通 | 首次真机前要求枚举 17 个 sibling、四类留出集、九种 gap 每种四类拒绝测试、全性能分类状态机、11 份 spec 回归和多套端到端矩阵，范围远超“先让新 workflow 跑通 GaussianBlur”。这会显著延迟唯一能提供实测反馈的 W8。 | 首轮只保留阻断本次链路的最小能力和针对性回归：GitCode 相对目录、169-case 规范化、任务书 golden wrapper、cpp_extension extended ABI、local_snapshot receipt、measure-only。留出集、17 sibling 研究、完整性能分类和全仓回归移到首次完整跑通之后。 | fixed | 1 |
| 37 | coverage | 344 | High | 需求13·决策阻塞 | 五个“动手前待拍板”把已经由需求或已知事实确定的事项重新交回用户：W5 应服从四类场景口径；W8 输入就是 local snapshot；磁盘已知应使用 Docker volume；脏改动应先隔离核查。计划因此在实施前自我阻塞。 | 删除已可确定的拍板项。仅在三源仍无法确定 `border_type` 默认值时询问；磁盘先对 Docker volume 做真实 preflight，不足时再请求扩容或启用 W9。 | fixed | 1 |
| 38 | goal | 94 | High | W-1 / 范围膨胀 | 先建中央 registry、9 个 kind、校验器、归并器、终态影响，再给每 kind 写四类拒绝路径，是独立架构项目，不是走通 GaussianBlur 的必要前置 | SIMPLIFY 为一个 `taskdoc_caseset_status` 硬门；全局 gap 体系 DEFER。风险是其它 gap 暂时仍靠现有机制，但不影响本轮目标 | fixed | 1 |
| 39 | goal | 105 | High | W0 / 正确性加固挤占主线 | 新增全局 §5.11、机器强制 GPU 条款 gap、阈值同源证明都不影响 CPU golden 实际执行，却被排在 W1 前 | DEFER 全局政策改造；本轮只在报告记录“按用户口径使用任务书 CPU golden，未验 GPU 条款”。风险仅是报告约束尚未全机器化 | fixed | 1 |
| 40 | goal | 127 | High | W1 / 取材过度设计 | commit pin、逐 blob 元数据、重定向认证策略、完整资源限制、严格 schema/producer hash 一次全做，会显著拖慢唯一需要的公开 tree/blob/relative 路径 | SIMPLIFY：复用 `fetch_source._get/_repo_file`，先支持远端 taskdoc 的 blob、tree、relative，设简单文件数/单文件/总字节/超时上限并记录内容 hash；认证迁移和通用重定向策略 DEFER | fixed | 1 |
| 41 | goal | 135 | High | W1 / 非目标输入扩张 | 本地任务书的相对链接、软链和根逃逸是另一种输入形态，本轮目标是远端 GitCode 任务书 | DEFER 本地任务书相对资源；保留现有本地 taskdoc 读取，不把它放在 GaussianBlur 前置链。风险是本地 taskdoc 自带相对 caseset 暂时不能自动吸收 | fixed | 1 |
| 42 | goal | 154 | High | W2 / 过严 schema 门 | 所有嵌套键必须被受控消费表完全覆盖会让无关元数据也阻断执行，实际 case 含 `case_path/golden_path/value_range` 等字段 | SIMPLIFY：只对会改变执行语义的字段严校；未知元数据保留并告警。非空 `data_path`、未知 dtype/shape/attr 等才阻断 | fixed | 1 |
| 43 | goal | 190 | High | W3 / 沙箱工程过大 | 无网络、只读输入、限额输出、容器级隔离是安全加固工程，不是适配这份公开任务书 golden 的必要条件 | DEFER 完整沙箱；本轮至少保留独立 worker 进程、超时、退出码和输出大小限制。风险是隔离强度较低，但不会阻碍目标流程 | fixed | 1 |
| 44 | goal | 195 | High | W3 / 无关分支 | 两档 golden 取信、正文与 caseset 冲突状态机不影响本例：169 条全部通过 `expect_func` 指向同一 CPU golden | SIMPLIFY：本轮只实现 caseset `expect_func` 强路径；正文弱路径及冲突仲裁 DEFER | fixed | 1 |
| 45 | goal | 200 | High | W3 / 违反范围削减 | GPU→CPU 降级层及五类测试与“不考虑任何 GPU 和 ATK”无关 | CUT W3(d) 和 V3；本例直接消费任务书自带 CPU golden。风险为未来 GPU-only 任务书暂未支持，与本轮无关 | fixed | 1 |
| 46 | goal | 219 | High | W4 / 过早建模 | 为磁盘门精确建模所有副本、evidence、msprof 临时文件和 RAM 峰值，成本高且仍可能估错真实 Docker volume | SIMPLIFY：开工最早阶段只读探测目标挂载点 `df` 和内存；足够则全量物化，只有不足时才进入 W9。风险是预估不精细，但真实空间检查更直接 | fixed | 1 |
| 47 | goal | 243 | High | W5 / 不必要新契约 | 为性能条款新建版本化 contract、迁移和状态机接口，远超需求；现有 `perf.mode=measure_only` 已经落地且 GaussianBlur 已显式使用 | SIMPLIFY：在现有 perf_mode 前增加最小派生函数，按受控 `change.kind` 四类及 GPU 条款落 measure_only；其余性能条款保持现行为或报 unsupported | partial（最小派生函数的受控值口径） | 1 |
| 48 | goal | 252 | High | W5 / 回归范围失控 | 在重跑前枚举全部 11 份历史 spec 做行为差异回归，与 GaussianBlur 走通无直接依赖 | DEFER 全量枚举；只验证 GaussianBlur 和一个既有非 measure-only 见证，防止最明显回归 | fixed | 1 |
| 49 | goal | 266 | High | W6 / 架构迁移过大 | 为一个已被 `preflight_aclnn`/`aclnn_runtime` 正确解析的 extended ABI，再把所有消费者迁移到新共享 ABI IR，显著增加改动面和返工概率 | SIMPLIFY：让 cpp_extension codegen 直接消费现有 preflight 签名记录/解析器，只补 `int_array` 和 extended dispatcher；不要同步重写 gen_cases、adapter、manifest 等已工作的调用槽契约 | fixed | 1 |
| 50 | goal | 275 | High | W7 / 文档挤占关键路径 | taskdoc-to-spec 同步和过期行号修正不影响运行 | 只 KEEP 仓规强制的 changes-brief；其余文档与行号清理 DEFER 到真机流程成功后 | fixed（round1 verify 后追补） | 1 |
| 51 | goal | 307 | High | 并行度 / 需求 10 | 顺序图几乎全串行，且没有 ultracode fan-out、subagent 或干净 worktree 安排 | 改成三路并行：A=GitCode+caseset+golden；B=cpp_extension extended ABI+local_snapshot receipt；C=最小 perf 派生。A 先用已跑通的 aclnn_py 通路做早期 caseset 冒烟；汇合后做 W7 和最终 cpp_extension 重跑 | partial（并行安全拆分） | 1 |
| 52 | goal | 311 | High | 关键路径 | 把 W-1 设为所有工作的前置没有必要，且让最重的正确性工程阻塞链接读取、codegen 和磁盘探测 | 删除该依赖；只让 taskdoc caseset 的硬路由依赖自身状态工件，W1/W6/空间探测可立即并行 | fixed | 1 |
| 53 | goal | 323 | High | V0' / 停止 TDD | 五类留出输入与多 schema 泛化属于加固测试集，不是本轮实际目标 | DEFER；本轮只保留真实 GaussianBlur happy path 和一个“任务书无 caseset→generated”小 fixture。风险是多算子/异构 schema 尚未证明，但不影响目标 | fixed | 1 |
| 54 | goal | 325 | High | V1 / 停止 TDD | 重建干净基线提交、冻结 10 个失败 nodeid、再跑 1900+ 全套测试会显著推迟真机反馈 | SIMPLIFY：实现后跑相关模块测试和语法/import smoke；全套 pytest 放到流程走通之后或准备 push 时。停止 TDD 不等于不测试，而是删除测试先行和全量回归前置门 | fixed | 1 |
| 55 | goal | 330 | High | V2 / 停止 TDD | HTTP、分页、重定向、截断、软链、TOCTOU 等完整对抗矩阵是安全加固，不应挡住目标任务 | SIMPLIFY：保留真实成功 fixture、HTTP 失败一个负例、显式 caseset 解析失败不得 fallback 一个负例；其余 DEFER | fixed | 1 |
| 56 | goal | 334 | High | V4 / 停止 TDD | 每个新 gap kind 四类拒绝测试随 W-1 一起扩大成数十条测试 | CUT 当前 V4；专用 taskdoc 路由只需“显式提供但不可解析→停止”及摘要漂移两个测试 | fixed | 1 |
| 57 | goal | 335 | High | V5 / 停止 TDD | 五模块端到端性能分类矩阵不是目标通路必需，且计划还要先造全新条款 contract | DEFER；本轮只测四类派生的代表项和 GaussianBlur `measure_only` 实际产出 | fixed | 1 |
| 58 | goal | 337 | High | V6 / 停止 TDD | ABI 全矩阵、错误顺序、未知 ABI、Median 全回归会把 codegen 修复拖成完整平台验证 | SIMPLIFY：保留 GaussianBlur codegen 编译、一次 extended 真调用，以及一个 standard 小见证；其它组合 DEFER | fixed | 1 |
| 59 | goal | 344 | High | 决策阻塞 | 五个“动手前拍板”中 W5 已由用户决定，border 可由 golden 语义确定，磁盘可先只读探测；把三者都再问一次会浪费一轮 | 只保留真机 build/run 的仓规确认点；provenance 直接按本地快照实现，磁盘先探测，`perf_mode.py` 现有用户改动保持不动并隔离处理 | fixed | 1 |
| 60 | blocker | 212 | Medium | W4／阈值契约 | 计划要求拒绝“未知顺序”，但没有定义 AscendOpTest `err_threshold` 两个值的权威顺序。GaussianBlur 两值均为 `1e-3`，本次碰巧看不出交换错误，换成不等值后会阻断或错误裁决。 | 在 taskdoc schema adapter 中明确并验证来源格式究竟是 `[rtol, atol]` 还是其他顺序，将来源 schema/version 写入规范化制品；validator只从该受绑定来源重新派生。 | fixed | 1 |
| 61 | coverage | 41 | Medium | 需求1·GitCode API | 计划把 `gitcode_discussion` 记为 `unsupported_api` 且不阻断，line 44 又裁掉 design template/tasklist；这只能算支持部分 blob/tree/relative 链接，不能满足“读取任务书涉及的 GitCode 链接内容”的字面要求。遗漏内容还可能影响任务书解释。 | 所有抽取到的 GitCode 链接必须产生 `fetched / explicitly_excluded / blocking_unsupported` 三态；没有用户明确排除时，无法读取的链接应阻断语义完成。补 discussion 可用读取接口，或要求用户明确授权把该链接排除，不能默认忽略。 | fixed（round1 verify 后追补） | 1 |
| 62 | goal | 320 | Medium | V0 / 无关研究 | 枚举 17 个 sibling 并做覆盖表是研究任务，对目标任务没有运行依赖 | CUT；风险只是暂时不能声称同级 17 个算子都兼容 | fixed | 1 |
| 63 | goal | 328 | Medium | 工作区管理 | 为三处无关 `perf_mode.py` 未提交修改先决定保留/回滚，会把别人的现有改动卷入主线 | 保留且不覆盖；从明确基线建立干净实现 worktree，只移植本任务需要的补丁。不要为取测试基线回滚用户改动 | fixed | 1 |
| 64 | goal | 340 | Medium | V8 / 发布范围 | 用户只说可以 commit，没有要求 push；push 前审修门不应进入当前关键路径 | DEFER 到用户明确要求 push；本轮可在聚焦验证后 commit，不能因此调用额外审计循环 | fixed | 1 |
| 65 | coverage | 328 | Low | 需求11·commit | 计划知道工作区已有未提交改动，却没有任何 checkpoint commit 落点；这也妨碍 W8 创建真正干净的新 session/worktree。 | 在 W4/W7 集成通过后、W8 前安排一个仅包含本任务改动的本地 checkpoint commit；先隔离现有 `perf_mode.py` 用户改动，不 push。 | fixed | 1 |

## Round 1 verify（thread 019fd07f-7f41-7763-beb2-756b59f6ddce）

55 FIXED · 10 PARTIAL · 0 NOT-FIXED · 0 REGRESSED。

verify 后按用户 2026-08-05 两项决定（真机 build 已授权 / 只能 cpp_extension 不许降级）与 4 条新漏项追补，
其中 #9 #10 #19 #22 #50 #61 已在 v4 中补齐（新增 C0 节、C3 双层门、A1 三态更正、§8 changes-brief）。
按 AGENTS.md §5.7「一轮即停」，未再自动迭代。

### v4 新引入 / 仍残留

| # | 事项 | Status |
|---|------|--------|
| NEW-1 | `run_workflow.py:416` 第二层性能总门 | 已补进 C3 |
| NEW-2 | `provenance_kind`/`source_mode`/`source.kind` 三套命名未统一 | 已补进 B1 |
| NEW-3 | `[H,W,1]` squeeze 修复的归属（任务书 golden 已自处理，wrapper 不得重复） | 已补进 A3 |
| NEW-4 | 漏排 changes-brief 更新（§5.6） | 已补进 §8 |
| R-1 | lane 文件所有权重叠，三路并行不安全 | **仍残留**：A/C 共改 `gen_cases.py`，B/C 共改 `cpp_extension_adapter.py` |
| R-2 | golden 执行服务协议（check_golden 与 gen_cases 共用长驻 worker） | **仍残留** |
