# Audit Findings

**Run**: audit-fix（push 前审修门，AGENTS.md 5.7）| **Scope**: `4d1544d..HEAD` 的 9 个高风险文件
**Model**: gpt-5.6-sol | **Effort**: high | **Sandbox**: read-only | 每文件独立会话
**Status values**: open | fixed | not-fixed | partial | regressed | skipped

| # | File | Line | Severity | Dimension | Finding | Suggested fix | Status | Round |
|---|------|------|----------|-----------|---------|---------------|--------|-------|
| 1 | plugin/acc-common/aclnn_adapter.py | 801 | Critical | fail-open | 快照摘要跳过特定目录和全部符号链接，但 `cp -a` 又把这些未摘要对象完整复制进实际构建树。 | 按生成 merkle 的同一文件清单复制普通文件并拒绝符号链接，或复制后对 `$CKO` 做覆盖实际构建输入的完整摘要并与期望值比较，不一致立即退出。 | fixed | 1 |
| 8 | plugin/acc-common/aclnn_runtime/aclnn_runner.py | 1569 | Critical | fail-open | `stage2_form` 缺失、解析不到或为空字符串时均被 `or STAGE2_STANDARD` 静默降级为四参调用。 | 删除 `or STAGE2_STANDARD`；让 `AclnnSignature.stage2_form` 必须显式为 `STAGE2_STANDARD` 或 `STAGE2_EXTENDED`，header 未找到 stage2 声明时直接报错，手工构造者也必须显式声明。 | fixed | 1 |
| 10 | plugin/acc-common/aclnn_runtime/aclnn_runner.py | 296 | Critical | fail-open | `_classify_param` 仅用子串判断 `aclScalar`/`aclTensor`，会把裸值、多级指针等未支持 ABI 误判为普通指针参数。 | 为 tensor 和 scalar 分别增加完整锚定正则，只接受明确支持的 `(const) aclTensor *name` 与 `(const) aclScalar *name`；其余形态全部报错。 | fixed | 1 |
| 36 | plugin/acc-common/source_provenance.py | 177 | Critical | fail-open | `git_fetch` 构建身份校验只比较三个 SHA 是否相等，三方均缺字段时 `None == None` 会直接返回成功。 | 在第 177 行比较前，要求三个字段都显式存在、类型为 `str` 且匹配 `_HEX40`，然后再逐字比较。 | fixed | 1 |
| 37 | plugin/acc-common/source_provenance.py | 184 | Critical | fail-open | `local_snapshot` 分支把字段缺失等同于显式 `null`，并允许整仓与子树摘要通过 `None == None`。 | 要求两侧 `head_sha` 键显式存在且值严格为 `None`；第 186、197 行比较前要求所有摘要键显式存在、为合法 64 位小写十六进制字符串。 | fixed | 1 |
| 52 | plugin/acc-common/validate_taskdoc_input.py | 468 | Critical | fail-open | 清单不完整只在 owner 原始状态为 `satisfied` 时阻断，`missing/ambiguous` 的 owner 经 `supplied` 决策后可让 `complete=false` 的收据最终变成 `PASSED`。 | 在第 706 行收集决策后，禁止 owner 已被决定但 `deliverable_inventory.complete` 仍为 false；`supplied` 必须携带结构化 deliverables 并重新执行 `_check_deliverables`，最终 `PASSED*` 应无条件要求 `complete=true`。 | fixed | 1 |
| 53 | plugin/acc-common/validate_taskdoc_input.py | 433 | Critical | fail-open | `deliverable_scan_exemptions` 只要求任意非空 rationale，未绑定用户决策便会把对应必选标记当作已覆盖。 | 必选标记不得由自由文本直接豁免；将每个豁免路由为绑定 source digest、精确标记位置和 `source=user` 的待确认决策，并在确认前保持 `NEEDS_USER`。 | fixed | 1 |
| 2 | plugin/acc-common/aclnn_adapter.py | 788 | High | fail-open | `local_snapshot` 在摘要 `$SNAP` 后才复制到 `$CKO`，却未复核真正参与构建的 `$CKO` 字节。 | `cp -a` 完成后立即用同一算法摘要 `$CKO` 的整树和算子子树，分别与先前值比较；任一不等即 `SNAPSHOT_COPY_MISMATCH` 并退出。 | fixed | 1 |
| 3 | plugin/acc-common/aclnn_adapter.py | 688 | High | fail-open | 附加构建参数只按完整字符串去重，允许重新声明并覆盖派生出的 `--soc`、`--ops`、`--vendor_name` 和 `--experimental`。 | 解析每个附加参数的 option key，明确拒绝 `pkg`、`experimental`、`soc`、`ops`、`vendor_name` 等受保护键，并拒绝同一 key 重复出现。 | fixed | 1 |
| 9 | plugin/acc-common/aclnn_runtime/aclnn_runner.py | 348 | High | fail-open | stage2 解析对原始文本执行无边界的首次 `re.search`，不区分真实声明、注释、禁用预处理分支、包装器或多份冲突声明。 | 用词法感知的声明解析；至少先移除注释、要求完整函数声明边界、收集全部候选，并在候选不唯一或结构冲突时 fail-closed。 | fixed | 1 |
| 11 | plugin/acc-common/aclnn_runtime/aclnn_runner.py | 1524 | High | fail-open | `int_array` 元素通过 `int(v)` 宽松转换，浮点数、字符串和布尔值会被静默接受，超出 int64 的值还会在 ctypes 编组时回绕。 | 对每个元素使用严格整数协议校验，显式拒绝 `bool`、字符串和浮点数，并在创建 ctypes 数组前检查 `-(2**63) <= v < 2**63`。 | fixed | 1 |
| 13 | plugin/acc-common/fetch_source.py | 207 | High | fail-open | example 与 `_def.cpp`/`op_host` 候选从整个 PR 选取，未限制在 `target_dir` 下。 | 先按 `target_dir.rstrip("/") + "/"` 过滤全部 `paths`，三档候选只从过滤结果生成；目标目录无改动文件时设置机读 `blocked`。 | fixed | 1 |
| 16 | plugin/acc-common/fetch_source.py | 553 | High | fail-open | 未指定 `--target-dir` 时，整仓快照中的多个候选算子会被 `_guess_op` 静默选中排序后的第一个。 | 收集全部不同的候选目标目录；数量不等于一时 fail-loud 并要求显式 `--target-dir`，确定目标后再按该目录过滤关键文件。 | fixed | 1 |
| 17 | plugin/acc-common/fetch_source.py | 661 | High | fail-open | 调用方仅凭 `provenance_kind="local_snapshot"` 就能选择原因折叠分支，构造侧未验证该档位的结构契约。 | 折叠前严格校验受控词表；`local_snapshot` 必须满足 `head_sha is None`、合法 64 位 Merkle、字符串 scope、所有 key ref 均为 `local_snapshot`，否则落 `blocked`。 | fixed | 1 |
| 18 | plugin/acc-common/fetch_source.py | 647 | High | fail-open | completeness 校验只检查多个字段是否为字符串以及 `changed_files` 是否真值，不拒绝空字符串或错误容器类型。 | 在 647–660 行改为严格契约校验：重新解析非空 PR URL，要求非空仓与状态字段、`changed_files` 为非空字符串列表、关键文件路径为安全非空相对路径。 | fixed | 1 |
| 25 | plugin/acc-common/perf_mode.py | 77 | High | fail-open | `measure_only` 宽档完全由待验 spec 的 `perf.mode` 自报即可启用，没有核对任务书是否属于“无性能要求”或“GPU 对比”。 | 将 `resolve_spec_mode` 改为同时消费经任务书校验门绑定的性能要求事实；只有该事实明确为“无性能要求”或“GPU 对比”时才允许返回 `measure_only`，否则抛错，并把授权事实摘要及任务书快照哈希写入 Task1 收据供门复核。 | partial | 1 |
| 26 | plugin/acc-common/perf_mode.py | 74 | High | fail-open | 显式存在但类型错误的 `spec.perf` 会被当成未声明并降级为默认档。 | 用哨兵区分字段缺失与显式坏值：仅 `"perf" not in spec` 时返回默认档；字段存在但不是 dict 时抛 `PerfModeError`。同理，第 94–96 行应让 `policy_mode` 拒绝非 dict，由调用方在字段真正缺失时显式选择 legacy 默认。 | fixed | 1 |
| 28 | plugin/acc-common/perf_mode.py | 79 | High | fail-open | `measure_only` 仅用五项 denylist 排除比值配置，未知字段或拼错的判据字段会被静默接受。 | 用统一的 `perf` schema/允许字段集合校验完整对象；`measure_only` 下只接受明确列出的测量与 case-policy 字段，任何未知字段均 fail-closed，再检查比较字段必须缺席。 | fixed | 1 |
| 31 | plugin/acc-common/reconcile_deliverables.py | 286 | High | fail-open | 任务书门只拒绝 `BLOCKED`，会把 `NEEDS_USER` 及未知状态继续处理并可能返回 `RECONCILED`。 | 将 286 行改为状态白名单，仅接受 `{"PASSED", "PASSED_WITH_PENDING"}`；`NEEDS_USER` 和未知值均抛 `ReconcileError`，并携带 `blocking_items`。 | fixed | 1 |
| 32 | plugin/acc-common/reconcile_deliverables.py | 93 | High | fail-open | 直接信任裸 `pr_facts.json`，未校验其与内容寻址的 `source_facts`、provenance 档位、PR head 和关键文件 ref 的一致性。 | 在 93 行前读取并验证 `source_facts`，复用 `source_provenance.bind()`；逐项核对 `changed_files`、关键文件摘要/ref，并使用其返回的 provenance bindings，拒绝未授权 snapshot、blocked completeness 及不合法 SHA。 | fixed | 1 |
| 33 | plugin/acc-common/reconcile_deliverables.py | 202 | High | fail-open | 任意目录只要包含一个改动文件就被认作完整交付件归宿。 | 删除 201–205 行的目录前缀放行，只接受逐字命中的具体文件；若必须支持目录交付件，应要求映射提供经规范化的完整文件清单并逐项验证，禁止仓根及宽泛祖先目录。 | fixed | 1 |
| 34 | plugin/acc-common/reconcile_deliverables.py | 211 | High | fail-open | “符号”仅按长度至少 3 的原始子串匹配，普通词、注释或字符串文本都能成为通过证据。 | 禁止 symbol-only 覆盖；要求符号绑定具体文件、符合完整标识符语法，并在去除注释及字符串后按 token/声明边界匹配。 | fixed | 1 |
| 38 | plugin/acc-common/source_provenance.py | 82 | High | fail-open | `complete` 档通过 `str(source_head)` 校验 SHA，使 40 位十进制整数等非字符串值也能被当作合法 commit SHA。 | 先要求 `isinstance(source_head, str)` 和 `isinstance(facts_head, str)`，再直接用 `_HEX40.fullmatch(source_head)` 校验，禁止字符串化输入。 | fixed | 1 |
| 39 | plugin/acc-common/source_provenance.py | 75 | High | fail-open | `provenance_kind` 缺失或为空时被默认成 `gitcode_pr`，且 `complete` 档不拒绝与档位矛盾的任意 kind。 | 删除 `or PROVENANCE_GIT_PR`；要求该键显式存在且为字符串，并强制 `complete == gitcode_pr`、`snapshot_only == local_snapshot`。 | fixed | 1 |
| 40 | plugin/acc-common/source_provenance.py | 75 | High | correctness | `bind()` 从未核对 `pr_facts.provenance_kind` 与 `source_facts.pr.provenance_kind`。 | 读取并严格校验 `pr_facts["provenance_kind"]`，要求其显式存在、类型合法且与 `kind` 逐字相等。 | fixed | 1 |
| 41 | plugin/acc-common/source_provenance.py | 102 | High | fail-open | `snapshot_only` 通过 `.get()` 判断两侧 `head_sha`，导致字段完全缺失与显式 JSON `null` 无法区分。 | 在值检查前要求 `"head_sha" in source_pr` 且 `"head_sha" in pr_facts`，随后再要求两值严格为 `None`。 | fixed | 1 |
| 42 | plugin/acc-common/source_provenance.py | 112 | High | correctness | 快照绑定只读取 `source_facts` 的 `snapshot_scope`，没有与 `pr_facts.snapshot_scope` 对账。 | 要求两份事实包都显式包含字符串 `snapshot_scope`，并在返回 bindings 前逐字比较；空字符串若代表仓根应保留为合法显式值。 | fixed | 1 |
| 43 | plugin/acc-common/source_provenance.py | 147 | High | fail-open | 起跑前配置门用 `.get()` 校验 head，畸形 bindings 缺失 `pr_head_sha` 时可被当作合法 `None`，git 路径还可由两个缺失值相等而通过。 | 分支前严格校验 `cfg` 和 bindings 的必需键；git 路径要求双方 SHA 为合法 `_HEX40` 字符串，snapshot 路径要求 `pr_head_sha` 键显式存在且为 `None`。 | fixed | 1 |
| 45 | plugin/acc-common/validate_acceptance_state.py | 1359 | High | correctness | raw 输出按逻辑 `compare_dtype` 解码，但 aclnn runtime 会把 bfloat16 输出展宽为 float32 后落盘。 | raw 分支使用经过结构契约核验的物理输出 dtype；至少将 raw bfloat16 按稳定存储契约映射为 float32，并校验该映射、shape 和字节数，不能直接使用逻辑 `compare_dtype`。 | fixed | 1 |
| 46 | plugin/acc-common/validate_acceptance_state.py | 1711 | High | fail-open | 是否进入 `measure_only` 宽档完全由待验目录中的 caseset 自报 mode 决定，未与原始 spec 绑定。 | 从经摘要绑定的原始 spec 派生 mode，并要求 caseset mode 与其逐字一致；在建立 spec↔caseset digest 绑定前，不得仅凭 caseset 开启 `measure_only`。 | partial | 1 |
| 47 | plugin/acc-common/validate_acceptance_state.py | 503 | High | fail-open | `source="spec_supplied"` 被当作充分授权，未知甚至缺失 hardware 时任意正整数 `small_max_bytes` 都可通过。 | 立即补上非空字符串 hardware 校验，并将 hardware/source/metric/limit 与经摘要绑定的 spec 原值交叉核验；若无法取得权威 spec，则删除 `spec_supplied` 放行分支并 fail-closed。 | partial | 1 |
| 54 | plugin/acc-common/validate_taskdoc_input.py | 400 | High | fail-open | requirement 一致性只在引文包含恰好一种 modality 时检查，包含必选和可选标记的引文可将必选交付件声明为 `optional`。 | 引文含多种 modality 时 fail-closed，要求拆成单一语义引文或显式逐 site 映射；至少在 `required` 出现而条目不是 `required` 时直接阻断。 | fixed | 1 |
| 55 | plugin/acc-common/validate_taskdoc_input.py | 396 | High | fail-open | 一个引文的全部文本区间都被视作覆盖，并通过 `_occurrences` 自动覆盖所有相同文本位置，形成不受限制的多对多覆盖。 | 每条引文必须显式绑定一个确定的标记 occurrence/site id，并限制一个 site 的归属关系；第 396、440 行不得把 `_occurrences` 返回的所有区间直接并入覆盖集。 | fixed | 1 |
| 56 | plugin/acc-common/validate_taskdoc_input.py | 478 | High | fail-open | `complete` 被定义为“受控词表未发现未覆盖项”，词表外的交付义务会在零扫描结果时被认定完整。 | 将零命中或词表外抽取结果路由为待确认；只有受控结构化任务书或绑定用户确认的穷尽性声明才能把 `complete` 置为 true。 | fixed | 1 |
| 4 | plugin/acc-common/aclnn_adapter.py | 856 | Medium | correctness | `local_snapshot` 的构建复用成功分支不输出后续核验强制要求的整树和子树摘要哨兵。 | snapshot 模式下复用前重新核验实际快照，并输出 `OPRUNWAY_ACLNN_SNAPSHOT_SHA256` 与 `OPRUNWAY_ACLNN_SNAPSHOT_SUBTREE_SHA256`；否则直接禁止 snapshot 模式复用。 | skipped (severity filter) | 1 |
| 5 | plugin/acc-common/aclnn_adapter.py | 1612 | Medium | fail-open | `measure_only` 无论采集失败或零条计时结果都无条件写入 `collected=True`。 | 让 `collect_perf` 返回结构化成功状态及预期/实得 case 集；只有全部目标 case 得到合法实测值时写 `collected=True`，否则写 `False`、失败原因和缺失 case。 | skipped (severity filter) | 1 |
| 6 | plugin/acc-common/aclnn_adapter.py | 74 | Medium | rule-5.1 | 通用 adapter 在 vendor 后缀缺失时静默默认领域专名 `nn`。 | 删除 `_DEFAULT_VENDOR_SUFFIX`，在 `_aclnn_cfg` 第 405 行用 `_req` 强制显式提供后缀，并让 `_vendor_suffix` 对缺失或空值直接报错。 | skipped (severity filter) | 1 |
| 7 | plugin/acc-common/aclnn_adapter.py | 758 | Medium | duplication | 快照 walk 与 merkle 算法以内联 Python 字符串重新实现，仅与 intake 侧共享跳过目录集合。 | 抽出单一可执行的快照身份模块，由 `fetch_source` 和远端构建共同调用；删除第 758–780 行的重复实现。 | skipped (severity filter) | 1 |
| 12 | plugin/acc-common/aclnn_runtime/aclnn_runner.py | 1338 | Medium | correctness | `aclDestroyIntArray` 的返回码被直接丢弃，`try/except` 只能捕获 Python 异常，捕获不到 ACL 的非零失败状态。 | 通过统一清理状态检查调用 `aclDestroyIntArray`；若主流程成功则在清理后报告非零状态，若已有原始异常则仅记录而不覆盖原异常。 | skipped (severity filter) | 1 |
| 14 | plugin/acc-common/fetch_source.py | 484 | Medium | fail-open | `os.walk` 未设置 `onerror`，目录扫描失败会被静默忽略。 | 给 `os.walk` 传入直接抛出异常的 `onerror`；任何扫描错误都应中止取材或使事实包明确 `blocked`。 | skipped (severity filter) | 1 |
| 15 | plugin/acc-common/fetch_source.py | 517 | Medium | fail-open | 关键文件读取或 UTF-8 解析失败被转换为 `None`，调用处随后无记录地跳过该文件。 | 不要吞掉这些异常；或者返回结构化错误，并将每个失败路径写入机读 blocked reasons，使状态强制为 `blocked`。 | skipped (severity filter) | 1 |
| 19 | plugin/acc-common/fetch_source.py | 798 | Medium | correctness | `main` 在落盘前只验证快照根和 `target_dir` 语法，目标子目录是否存在直到写完任务书工件后才检查。 | 在 `os.makedirs(a.out)` 前解析快照根与目标目录，并执行与 `scan_pr_snapshot` 相同的存在性及安全路径检查。 | skipped (severity filter) | 1 |
| 20 | plugin/acc-common/fetch_source.py | 546 | Medium | correctness | 目标子目录检查使用会跟随符号链接的 `os.path.isdir`，而后续 Merkle 读取也未经过安全路径约束。 | 使用 `content_address.safe_path` 校验目标目录的每个路径段并拒绝 symlink；`_snapshot_merkle` 打开每个文件时也应使用相同安全路径函数。 | skipped (severity filter) | 1 |
| 21 | plugin/acc-common/perf_compare.py | 672 | Medium | fail-open | `measure_only` 接受全部 `_VALID_SCOPES`，会把 device/host E2E 计时标记为 `measured`，而契约只允许 `kernel_only`。 | 将第 672 行改为 `_finite_pos(us) and scope == "kernel_only"`，并同步把第 678 行错误提示改为明确要求 `kernel_only`。 | skipped (severity filter) | 1 |
| 22 | plugin/acc-common/perf_compare.py | 654 | Medium | correctness | `measure_only` 零性能用例出口复用 `_no_perf_cases`，导致 summary 含禁止出现的 `达标` 字段且缺少 `measured`。 | 在第 653–656 行直接构造 measure-only 专用的 `no_perf_cases` 报告，summary 至少包含 `perf_cases=0`、`measured=0`、`blocked=0`、`status="no_perf_cases"`，不得包含 `达标`。 | skipped (severity filter) | 1 |
| 27 | plugin/acc-common/perf_mode.py | 61 | Medium | fail-open | `normalize` 把显式 JSON `null` 与字段缺失混为一谈。 | 让 `normalize(None)` 抛错；在 `resolve_spec_mode` 和 `policy_mode` 中用私有哨兵检测键是否缺失，只有缺键时才返回 `DEFAULT_MODE`。 | skipped (severity filter) | 1 |
| 29 | plugin/acc-common/perf_mode.py | 33 | Medium | duplication | `MODES` 并非实际唯一词表，采集端仍分别硬编码 `"ratio_gated"` 与 `"measure_only"`。 | 导出并复用一个严格的 plan-mode 解析函数；若远端运行时必须独立部署，则从本模块生成共享契约数据，不再手写第二份字符串集合。 | skipped (severity filter) | 1 |
| 35 | plugin/acc-common/reconcile_deliverables.py | 108 | Medium | fail-open | `key_files` 中非字符串正文被静默过滤，而不是作为契约类型错误 BLOCKED。 | 将 108–109 行改为显式遍历；任何非字符串路径或正文立即抛 `ReconcileError`，不得过滤后继续。 | skipped (severity filter) | 1 |
| 48 | plugin/acc-common/validate_acceptance_state.py | 1733 | Medium | fail-open | 禁止字段用 `pr.get(key) is not None` 检查，使 `target_ratio: null` 和 `baseline_source: null` 被当成字段不存在。 | 改为 `if key in pr:`，同时让报告生产端彻底省略这两个键。 | skipped (severity filter) | 1 |
| 49 | plugin/acc-common/validate_acceptance_state.py | 1807 | Medium | correctness | `measured_by_shape_class` 被直接压成字典，重复 class 和未知 class 行会被覆盖或忽略。 | 建字典前要求列表恰含一条 `small` 和一条 `large`，拒绝非对象、重复 class、未知 class 及额外行。 | skipped (severity filter) | 1 |
| 50 | plugin/acc-common/validate_acceptance_state.py | 2191 | Medium | correctness | measure_only 分支从未复核新增的 `measured_by_dtype` 汇总。 | 从 caseset 的输入 dtype 与已绑定的 per_case 实测值独立重算完整 dtype 汇总，要求 `measured_by_dtype` 集合、计数、中位数和 comparison 字段精确一致。 | skipped (severity filter) | 1 |
| 57 | plugin/acc-common/validate_taskdoc_input.py | 190 | Medium | fail-open | 契约仅验证 `owner_item` 存在，没有验证其必为恒适用且所有未满足状态均走阻断路由。 | 第 179 行传入完整 item 映射，在 `_check_inventory_spec` 中强制 owner 的 `requirement=="must"` 且 `missing/ambiguous` 均为 `stop`，或让清单不完整独立于 owner 路由无条件阻断。 | skipped (severity filter) | 1 |
| 58 | plugin/acc-common/validate_taskdoc_input.py | 375 | Medium | correctness | `_DELIVERABLE_ID_RE.match` 配合 `$` 会接受带末尾换行的 ID。 | 改用 `_DELIVERABLE_ID_RE.fullmatch(entry_id)`，或把正则尾锚改为 `\Z`。 | skipped (severity filter) | 1 |
| 23 | plugin/acc-common/perf_compare.py | 925 | Low | correctness | CLI 在调用受保护的 `perf_compare()` 前再次解析 `perf.mode`，且没有捕获 `ValueError`。 | 删除第 925–927 行的提前解析；无 baseline 时统一传入 `None`，待 `perf_compare()` 返回后再根据报告字段打印 measure-only 提示。 | skipped (severity filter) | 1 |
| 24 | plugin/acc-common/perf_compare.py | 609 | Low | duplication | `_measured_shape_aggregate` 重复实现了 `_shape_class_aggregate` 的 case 映射、分类校验、账本计数核对和中位数汇总骨架。 | 删除第 609–646 行的重复分类骨架，从第 338 行现有聚合器抽出共享的分类与计数函数，两种模式仅分别组装 ratio 字段或 measured-only 字段。 | skipped (severity filter) | 1 |
| 30 | plugin/acc-common/perf_mode.py | 55 | Low | over-design | `PerfModeError` 与一行的 `is_measure_only` 都没有提供独立行为，只增加了 API 层级。 | 删除第 55–56 行和第 99–100 行，直接抛 `ValueError`，调用处使用 `mode == MODE_MEASURE_ONLY`；保留常量与严格解析函数作为真正的共享契约。 | skipped (severity filter) | 1 |
| 44 | plugin/acc-common/source_provenance.py | 164 | Low | duplication | 第 164–174 行与第 138–146 行重复实现 bindings 归一化、source mode 映射及来源通路一致性判定。 | 抽取一个严格校验 `cfg`、`preflight_bindings` 并返回 `(adapter_kind, bindings)` 的私有 helper，让两个入口共同调用。 | skipped (severity filter) | 1 |
| 51 | plugin/acc-common/validate_acceptance_state.py | 1775 | Low | duplication | `_gate_measured_shape_report` 复制了 `_gate_shape_report` 的 caseset 加载、分桶、计数、中位数和数值比较逻辑。 | 抽取共享的 case→shape-class 分桶、严格 class 行校验、median 和数值比较 helper；两个调用处只保留各自字段映射。 | skipped (severity filter) | 1 |
