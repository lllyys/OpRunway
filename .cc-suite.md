# CC-Suite Configuration

Project-specific settings for cc-suite commands.
Generated by `/init`. Edit freely — all fields are optional.

## Project

- **Stack**: Python 3 (standard library first)
- **Test command**: `cd plugin && PYTHONPATH=../repos/repo-task-atk-test/third_party/ATK python3 -m pytest -q`
  （`plugin/pytest.ini` 的 `testpaths` 是 `skill .claude/hooks`，隔离用的根 conftest 在
  `plugin/conftest.py`，两者都要求在 `plugin/` 下起跑；ATK 只有 `repo-task-atk-accept`
  侧的测试需要，它在 ignored 的参考 clone 里）
- **Source directories**: plugin/skill/

## Defaults

These override the built-in defaults for all commands in this project.
Remove a line to fall back to the built-in default.
`latest` is a policy, not a model name: Codex-delegating commands resolve it to
the newest general-purpose Codex model via preflight. Replace it with a concrete
slug (any entry from `/cc-suite:codex-preflight`'s model list) only to pin —
pins stop tracking the catalog.

- **Default model**: gpt-6-astra
- **Default effort**: xhigh
- **Default audit type**: mini
- **Default sandbox**: workspace-write

## Audit Focus

balanced

Additional instructions appended to every audit's developer-instructions:

```text
Give equal attention to all audit dimensions.
```

## Skip Patterns

Files and directories to always skip during audits (glob patterns):

```text
node_modules/
dist/
build/
coverage/
*.min.js
*.bundle.js
*.lock
vendor/
.git/
```

## Project-Specific Instructions

Custom instructions appended to Codex's developer-instructions for every command.
Use this to tell Codex about your project's conventions, architecture, or constraints.

```text
本仓是上游社区算子验收 skill 的开发工作区（上游 gitcode.com/Justbin/repo-task-atk-test）。

- `plugin/` 是上游根的逐字镜像，基线记在 `plugin/.claude-plugin/upstream.json`。
  `plugin/skill/` 下是**一组平级 skill，不是单个**：验收侧有 `repo-task-blas-accept`
  （ops-blas / ops-sparse，CSV 驱动的 GTest harness + msprof kernel 耗时）与
  `repo-task-atk-accept`（ATK 节点驱动，含不依赖 ATK 的独立 C++ 执行器 run_cxx.py）；
  生成侧有 `repo-task-blas-case-gen`（六件任务包）与 `repo-task-case-gen`（ATK 用例包）；
  另有 `repo-task-doc-write`（写任务书）和一组 `cann-*` 工具 skill。
  `repo-task-blas-harness-gen` 在未合入的 `feature/harness-gen` 分支上。
- 判据的确定性由各 skill 自带的 `scripts/`（机械门）与 `tests/` 承担；本仓不在 `plugin/`
  之外另建一套生成或裁决实现。
- 发布切片只有 `plugin/.claude-plugin/` 与 `plugin/skill/`。`plugin/CLAUDE.md` 与
  `plugin/docs/` 是开发件，不进任何测试部署——验收零上下文，一次验收的完整契约就是
  `SKILL.md` 加 `references/` 加 `scripts/`。
- `dev-doc/` 是本仓开发记录，`repos/` 是只读参考 clone，`reports/` 与 `.oprunway/` 不入仓。
- 评审按 `.claude/rules/codex-review.md` 的八维进行（复杂度优先级最高）；自写 markdown 的
  行文规则见 `.claude/rules/prose-style.md`。
```

## Enabled Tools

Which coding agents cc-suite bridges in this project. Tick a tool to enable it,
then run `/cc-suite:bridge-tools`. claude, codex, and antigravity use their own
bridges (ticked by default). grok, opencode, qwen, and kimi read AGENTS.md and
shared skills natively — only their MCP config is mirrored, each to its own
format.

China note: opencode, qwen, and kimi work natively in mainland China;
antigravity and grok are effectively VPN-only there.

- [x] claude
- [x] codex
- [ ] antigravity
- [x] grok
- [ ] opencode
- [ ] qwen
- [ ] kimi
