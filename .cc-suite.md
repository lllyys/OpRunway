# CC-Suite Configuration

Project-specific settings for cc-suite commands.
Generated by `/cc-suite:init`. Edit freely — all fields are optional.

## Project

- **Stack**: Python 3（stdlib only，无 package manifest）+ Markdown 文档 + 少量 C++ runner 样例
- **Test command**: `python3 -m pytest plugin/acc-common/` —— **只在 NPU 目标环境内执行**。
  AGENTS.md §5.3 规定一切 compute 在 NPU 目标环境：**没有 NPU 的开发机上**不建 venv、不跑 pytest、
  不 import torch/numpy 做验收 compute。审修工具跑在开发机上，故**不得**运行测试，也不得建议在此运行。
  （§5.3 的「远程连 / 就地跑」两种形态只影响怎么够着目标环境，不改变这条约束。）
- **Source directories**: `plugin/acc-common/`（Layer 0 契约 + Layer 1 确定性脚本）、
  `plugin/agents/`、`plugin/skills/`、`plugin/commands/`（Layer 2 薄壳）

## Defaults

These override the built-in defaults for all commands in this project.
Remove a line to fall back to the built-in default.

- **Default model**: gpt-5.6-sol
- **Default effort**: high
- **Default audit type**: full
- **Default sandbox**: workspace-write

## Audit Focus

quality-first

Additional instructions appended to every audit's developer-instructions:

```text
Prioritize code quality findings. Flag untested critical paths, high cyclomatic
complexity, and DRY violations as High regardless of other severity heuristics.

本仓的最高价值是「裁决正确性」，最贵的缺陷类别是 fail-open：任何让 needs_review /
UNCERTAIN / 证据不完整被静默升级为 pass 的路径，一律按 Critical 处理，即使它看起来
只是防御性分支。判定链（validator.py / perf_compare.py / validate_acceptance_state.py /
run_workflow.py）上未被测试覆盖的分支按 High 处理。
```

## Skip Patterns

Files and directories to always skip during audits (glob patterns):

```text
.oprunway/*.env
repos/
reports/
gazette/
oprunway-canon-gazette.zip
spec/
canon/logbook/
canon/_*.json
canon/bureau.json
canon/timeline/
canon/lint/
.codex/
.agents/
.claude/settings.local.json
.claude/*.local.*
__pycache__/
*.pyc
.git/
```

`.oprunway/*.env` 是硬性排除：其中含真实主机别名 / 容器名 / 远端路径，不得进入任何
外部工具调用的上下文。`canon/logbook/` 是 append-only 会议纪要，审修发现无法落地，排除以降噪。
`canon/` 下的 `_*.json` / `bureau.json` / `timeline/` / `lint/` 是 bureau press 生成的编译与
门禁状态（§5.9：不得手改、status 不得自行升为 canonical）。`.claude/*.local.*` 与
`.claude/settings.local.json` 是 gitignored 的本机状态（可能含主机 / 容器 allowlist），与
`.codex/`、`.agents/` 同级排除。

## Project-Specific Instructions

Custom instructions appended to Codex's developer-instructions for every command.

```text
本仓是 OpRunway：NPU（昇腾）算子验收工作区。仓规唯一真源是仓根 AGENTS.md，请以它为准。
以下是审修时必须遵守的硬约束：

1. 泛化优先，绝不按算子身份特判（最高原则）。不得建议 `if op == "<具体算子>"` 一类
   身份分派，也不得为某个算子修改通用判定逻辑。允许按稳定的接口能力 / 仓形态扩通用
   adapter。具体算子只能作为见证或测试输入。

2. 判定的脑子在确定性脚本里，不在 agent。agent / 编排层不得自行重判 pass/fail，只能
   逐字引用确定性产物并标明来源。不要建议把裁决逻辑上移到 prompt 或 agent 层。

3. 一切 compute 在 NPU 目标环境：build、pytest、用例生成、golden 生成、验收、profiler。
   不得建议在没有 NPU 的开发机上建 venv、跑 pytest、或 import torch/torch_npu/numpy 做验收 compute。
   ⚠ 目标环境有两种够法（远程连 / 会话就地在目标机内），**都是一等形态**；不得建议把某一种
   写成前置条件，也不得以「拿不到连接元数据」为由判定无法开工。

4. 零硬编码：仓名、路径、SoC、目标算子、阈值、PR head 不得写死在通用代码里；应运行时
   探测、从 spec / pr_facts 派生，或询问用户。脚本路径统一用
   `${OPRUNWAY_PLUGIN_ROOT:-$CLAUDE_PLUGIN_ROOT}`。

5. 不得把 .oprunway/*.env 的内容复述、外传或写进任何输出与 tracked 文件。私有主机名、
   容器名、真实远端路径、token 不得出现在任何输出或建议的 tracked 文件里。
   （注：AGENTS.md §5.3 要求本仓 agent 在该文件**存在时**必须读取其
   `OPRUNWAY_MACHINE_PROTECTED_ROOTS` 并遵守只读保留现场；本条只约束审修工具侧的外泄，
   不废除那道读取。⚠ **文件缺席只表示未登记保护根，不构成阻塞**——它是「远程连」形态的
   连接元数据，就地跑时本来就用不到；但未登记 ≠ 已授权清理。）

6. fail-closed 优先：UNCERTAIN、needs_review、证据不完整都不能静默升级为 pass。域外或
   未知接口能力一律标「不支持的接口能力」，不硬塞、不自动归类。

7. `--mode` 只由 `spec.runner_form` 派生，受控词表是 **{cpp, aclnn_py, cpp_extension}
   三项**。三项都有真机执行映射：`cpp`→`new_example`、`aclnn_py`→`aclnn_py`、
   `cpp_extension`→`cpp_extension`；**`runner_form` 未声明时按 `cpp_extension` 派生**
   （缺省真源是 `repo_adapter.DEFAULT_RUNNER_FORM`，`run_workflow._DEFAULT_RUNNER_FORM`
   只是它的别名，不得改成两处缺省）。⚠ 旧文里的「`cpp` 或未声明→`new_example`」已经过时，
   不得据此提任何建议。

   ⚠ **「能在真机上跑」≠「能产验收裁决」，两件事必须分开读。** 正式验收准入白名单在代码里
   就一行：`run_workflow._ACCEPTANCE_RUNNER_FORMS = frozenset({"cpp_extension"})` ——
   `cpp_extension` 是当前**唯一**能产验收裁决的形态。`cpp` / `aclnn_py` 仍**跑得起来**
   （须显式加 `--allow-experimental-form`），但那条路**物理上只产**
   `dev_run_summary.json` / `dev_precision_check.json`（`evidence_grade="development"` +
   NON-ACCEPTANCE 戳），**不写** `acceptance.json` / `verdict.json`。收敛理由是真机成熟度，
   不是形态优劣（AGENTS.md §4.1）。所以「加了 `--allow-experimental-form` 跑绿了」不得被
   写成验收通过，也不得进验收报告的裁决栏。

   准入是两道门，均不得放宽、合并或当冗余删除：入口门 `_resolve_mode`（拦正常调用路径，
   只对真机通路生效；显式 `mock` / `catlass_mock` 逃生口不受影响）、出口门
   `_assert_acceptance_form_allowed`（写 `acceptance.json` / `verdict.json` **之前**再校
   一次）。口径照抄 `repo_adapter` 对 `catlass_mock` 后门的处置：只拦入口拦不住。

   `cpp_extension`（官方 `NpuExtension` / `EXEC_NPU_CMD_EXT` 桥）**不是待清理的死代码**，
   其独立构建收据门（绑定被测来源锚 —— PR head 或本地子树摘要、构建命令、实际加载的
   vendor ELF）必须保留。`cpp` / `aclnn_py` 的机制描述与代码路径同样**不得删除或合并**：
   它们仍是受支持的执行形态，只是不产裁决；对它们的改动只能是补准入状态标注。
   能力表（`repo_adapter.SUPPORTED_NP_BY_FORM` / `DEFERRED_NP_BY_FORM`）回答「这条通路支持
   哪些 dtype」，准入白名单回答「这条通路能不能出裁决」，两张表不得互相反推。

   `mock` / `catlass` / `catlass_mock` 不能从 `runner_form` 派生。`--mode` 省略即由 spec
   派生（argparse 默认 `None`）；任何 argparse 默认值都不是编排依据。

   ⚠ spec 写着 `cpp` / `aclnn_py` 却要做正式验收时，正确处置是**迁到 `cpp_extension`**
   （需 torch.ops 调用桥 + vendor ELF 构建收据，接入成本更高，这是已知账单），**不是**让
   编排层停下来问用户走哪条 form，也**不是**拿逃生阀产的开发级证据顶替验收结果。

8. 对外产出（commit message、PR body、报告、文档）不得带任何 AI 署名、trailer 或生成
   标识。人类署名使用 lys / lllyys。

9. 项目 Markdown / 图 / SVG 统一放仓根 doc/，不写到工作区上层的 markdown/。

10. canon/ 是 bureau durable knowledge：不手改 cabinet 页、不自行把 status 升为
    canonical。canon/logbook/ 是 append-only，不得重写历史条目。

11. 非验收通路（mock / catlass_mock、`--allow-experimental-form` 下的 `cpp` / `aclnn_py`、
    以及 adapter 自报 evidence_grade="development" 的任何路径）**物理上**
    不得写出 acceptance.json / verdict.json，只写 dev_run_summary.json /
    dev_precision_check.json 并带 NON-ACCEPTANCE 戳。run_workflow.py 里
    `_ACCEPTANCE_FILES` 与 `_DEV_FILES` 是两条刻意分开的产物路径，跑测前清理陈旧
    acceptance.json / verdict.json 的分支也是刻意的。不得以「统一产物命名」「消除重复
    写入」「删除防御性死分支」为由合并或删除它们 —— 那会让非验收结果被下游按文件名
    当成真裁决读走。

12. 你只做只读审阅与文件编辑：不得执行本仓任何跑测 / 构建 / 安装脚本（含
    plugin/init.sh、run_workflow.py、gen_cases.py、pytest），不得 clone / checkout /
    build，不得删除或覆盖既有产物。plugin/init.sh 在 `--level global` 下会写 $HOME，
    任何情况下不要运行它。脚本里已有的 dry-run / plan 分支（`DRY_RUN`、`*_DRY_RUN=1`）
    是仓规要求的安全阀，不得以「简化」「消除死分支」为由建议移除。

13. 落点固定：验收产物只落用户 CWD 下的 `reports/` —— `--out` 默认值 `reports/_run`
    的 CWD 相对语义是刻意的，不得改成绝对路径、插件目录相对路径、临时目录或
    $XDG / $HOME 状态目录。不得建议写 `~/.config`、`~/.claude`、shell rc 或任何用户级
    配置来存放路径、阈值或环境变量。

14. 不得执行 git 写操作：不 commit、不 push、不 merge、不建分支、不改 remote / tag，
    除非用户在本轮明确要求；对非本用户仓的 issue / PR / comment 一律不动。改完只留
    工作区改动并如实汇报，由用户自己决定提交与发布。

15. 验收权威只认任务书：PR、op_def、PR 自带测试都不能反过来覆盖任务书给出的阈值、
    dtype、shape 或硬件；缺字段时 fail-closed 或询问用户，不得从 PR / 代码现状回推。
    「PR 有测试」「代码接通」「covered」「collector 有数据」都不等于验收通过。报告与
    产物里的数字、错误、耗时必须来自真实日志/产物，不得用默认值、估算值、零值或占位
    值补齐缺失测量；推断项必须显式标「推断」。

16. 性能 case 必须与精度 case 出自同一份 caseset，且全部真实采集。`<= 256 KiB` /
    `> 256 KiB` 的大小分类只用于分组和报告，不得据此抽样、限流、跳过，或把一次测量
    复用到同组其它 case；该硬件边界写在 spec 里，不得挪进代码常量、也不得按算子身份分支。
```

## Enabled Tools

Which coding agents cc-suite bridges in this project. Tick a tool to enable it,
then run `/cc-suite:bridge-tools`. Claude, Codex, and Antigravity use their own
bridges (on by default). Grok / opencode / Qwen / Kimi read AGENTS.md and shared
skills natively — only their MCP config is mirrored, each to its own format.

China note: Qwen, Kimi, and opencode work natively in mainland China; Grok Build
and Antigravity are effectively VPN-only there.

- [x] claude
- [x] codex
- [ ] antigravity
- [ ] grok
- [ ] opencode
- [ ] qwen
- [ ] kimi
