# 未开放阶段脚本（暂存）

kernel UT / pytest / msprof 三个阶段当前**未开放**：各仓 build.sh 的能力还没对齐，
跑起来结果不可信。脚本放在这里而不是删掉，是为了等能力对齐后能直接恢复。

**不要调用本目录下的任何脚本**，也不要把它们当成 cann-ops-run 的可用能力。
用户提出这三类跑测请求时，如实告知「暂不受理」。

恢复时：移回 `scripts/`，并在 SKILL.md「并发拓扑与入口命令」里补回入口。
