**任务书填写整体要求**：
1. 所有链接需要显式贴出来，不能使用隐藏跳转链接
   - 正确示例：请参考链接：https:xxx
   - 错误示例：请参考[链接](https:xxx)
2. 严格遵循模板的章节内容要求进行填写

# xxx 任务书
填写格式：xxx 任务书。“xxx”为实际的开发任务，例如 abs算子开发、matmul接口开发，大小写需要和算子/接口的正式命名保持一致。

## 1. 任务概述
填写要求：基于 xxx 背景，使用 xxx 语言/模板库开发 xxx 算子/接口

## 2. 核心开发要求

### 2.1 功能实现要求
填写要求：
1. 详细描述需要实现的算子功能、数学公式、算法说明；
2. 明确是否有确定性计算要求；
3. 明确对标的基线接口，如torch.xxx

### 2.2 算子工程模式
填写要求：要明确算子的开发工程模式，kernel直调/aclnn算子工程化开发/torch接口

### 2.3 接口定义
填写要求：要明确算子的接口定义，包括算子名、参数序列。如 add(Tensor a, Tensor b, Tensor output)。

### 2.4 参数说明
填写要求：参考昇腾官网Ascend C API的呈现格式，以表格形式填写算子原型定义，包括如下字段：
| 参数名 | 输入／输出/属性 | 描述 | 数据类型 | dtype类型 | 数据排布格式 | 维度(shape) | 值域范围 | 异常行为 |
|---|---|----|------------|----------|----------|-----------|-----|-----|
| 参数名是什么？ | 该参数是输入还是输出还是属性？；如果是输出，需要明确是独立输出、元组输出还是原地输出 | 该参数的含义是什么？ | Tensor/scalar/attr/list？ | 精度类型是什么？和其他参数的精度类型是否需要一致？ | 排布格式 | 参数的shape是多少？和其他参数的shape是否有对齐约束？dynamic shape要求是什么？广播机制是什么？ | 元素的取值范围是多少？ | 非法 类型/shape/dtype/排布格式/值域 会触发什么错误？ |

## 3. 验收标准

### 3.1 软硬件环境要求
填写要求：描述测试软硬件环境，包括硬件型号（A2/A3/950）、CANN版本以及其他依赖的三方软件版本；

### 3.2 精度要求
填写要求：
1. 明确对标的算子/接口；
2. 给出在不同数据类型输入下的误差要求，建议参考生态算子开源精度标准（https://gitcode.com/cann/opbase/blob/master/docs/zh/ops_precision_standard/experimental_standard.md ）

### 3.3 性能要求
填写要求：
1. 给出所有输入范围内的性能要求、对标算子/接口，如果没有现成的对标接口，使用小算子拼接的性能采集策略要明确出来；
2. 给出性能要求测试的case和标杆性能，也可以在测试用例文件中提供；
3. 需要明确各个具体型号的性能要求，例如A2类型要明确910B3、910B4的要求

### 3.4 内存要求
填写要求：描述算子调用的内存占用要求，例如对标torch.xxx接口，内存占用比值不能超过xxx;若不涉及请填“不涉及”

### 3.5 自验要求
填写要求：
1. 描述测试工具、开发者自验策略
2. 如果参数序列和标杆接口的参数序列不一致，需要说明它们之间的对应关系；
3. 测试用例入参生成规则:基于[2.4章节](#24-参数说明)的参数说明，给出测试用例入参的生成规则，包括：
	- Tensor值域分布（"Tensor 生成规则"）：均匀分布/正态分布 μ/σ，各占比是多少 
	- Attr 覆盖规则（"Attr 生成规则"）：标量/布尔/枚举参数的覆盖要求

## 4. 验收交付件
填写要求：以表格形式需要列出开发者在开发交付过程中需要提交的内容

## 5. PR 申请合入
填写要求：需要清晰指出算子开发完成后的提交代码仓和目录

## 6. 参考资料
填写要求：给出开发者在开发过程中需要用到的参考资料

## 7. 特别注意事项
填写要求：填写开发者需要重点注意的事项

## 8. 环境获取（无需修改，使用模板原始内容）

 1. 使用 hidevlab webIDE 算力：https://hidevlab.huawei.com/online-develop-intro?from=hiascend 。
 - **【补充说明】填写示例：本人gitcode账号是 yolo，现在参与社区任务"7月社区任务-aclnnRoll算子开发"，需要申请A2/A3算力进行任务开发。**
 	 
 	![环境截图](./pics/zaixiankaifa1.png)  
 	![环境截图](./pics/apply.png)  
 	 
2. 开源仓提供100小时免费时长，请不使用时及时关闭，用时耗尽前请务必保存相关资料，建议及时提交备份。
 	 
 	![环境截图](./pics/yunkaifa.png)
 	 
3. 如需额外环境资源，请联系昇腾CANN小助手。

