# solver skill 补充信息归档

本文件归档 原负责人 对 solver 两个 skill（`repo-task-solver-case-gen`、`repo-task-solver-accept`）
的补充信息。地位：**用户裁定级输入**，与任务书同为设计依据；与既有档案冲突时以本文件为准，
并回写订正到对应文档。

关联档案：裁决与两 skill 分工见 `solver-0923-context.md` 第 8′ 节；实施分期与十二条硬边界见
`solver-plan-v3.md`；精度标准侧参考件见 `repos/solver_tasks-main/交接文档.md`。

## 条目

### 2026-09-24i · potrf 残差基维持 A64

- 原话或要点：HT-15 二选一裁「2」——potrf 的 DPOT01 残差基维持 A64（构造侧），
  不随 potrs 的「实际输入升精」条款类推；待任务侧对任务书 potrf 节补字后复核。
- 影响到哪些既有结论 / 文档：DPOT01 通路零改动；任务书反馈清单增一条
  「potrf 节补明残差 A 取数口径」。
- 落地动作：无代码动作；反馈清单随 ε 修订等一并路由。

### 2026-09-24h · 主判比对基准用任务书口径：golden64

- 原话或要点：「用任务书的」——第一层主判（生态标准逐元素）比对基准按任务书条 1
  的 FLOAT64 golden，即 potrf/potrs/potri 三算子主判统一从 golden32 换 golden64，
  批量卡同步。判定结果不受影响（基准差 ~2⁻²⁴ ≪ 容差 2⁻¹³），属契约字面对齐。
- 影响到哪些既有结论 / 文档：_tri_pair 与 potrs/potri 主判目标的 golden32 取数改
  golden64；HT-1 动态锚点门内的 g_low 收窄语义不变（标准本就要求按输出 dtype 收窄，
  与比对基准无关）。
- 落地动作：并入 HT-7（A5）施工一次做掉。

### 2026-09-24g · max_abs 门裁定：采 opbase 最新标准动态锚点

- 原话或要点：「HT-1 max_abs 硬上限口径，这个用新的生态标准 opbase 最新标准，这个决定了」
  ——即 a5e8e71 定义：max_abs_error_limit = max(兜底值, 32·ULP(g_low))，FP32 兜底 1e-2，
  or=取宽，配 ±inf 收窄比对规则。issue C2 的「删 or 定 1e-2」方案不采。
- 影响到哪些既有结论 / 文档：HT-1 转可开工；MAX_ABS_ULP32 固定常数（32×EPS32，参照待裁）
  退役，改按 g_low 值域动态计算（ULP 取间距语义，与残差 ε=2⁻²⁴ 分属两个常数）。
- 落地动作：随 criteria 改造轮落地；包内 verify 口径随 v3 重渲。

### 2026-09-24f · 判定次序裁定：先生态精度标准，不过再走 LAPACK

- 原话或要点：「我们的设计有问题，得先走生态精度标准，不通过的话再走 lapack」——
  即采任务书 3.2.1→3.2.2 两步结构：第一步逐元素混合容差对 golden（生态标准口径），
  不过才进第二步 LAPACK 残差复核。A5 就此裁决（选项①按任务书），potri 的同型
  层次问题一并适用。
- 影响到哪些既有结论 / 文档：spec §2.3′「potrf 还原主判、potri 双目标」作废——
  potrf 主判改 F vs golden（原诊断项转正，recon 撤出第一步、其数学形式留在
  DPOT01 兜底层）；potri 第一步收成 C vs golden 单目标，A·A⁻¹ vs I 只在 DPOT03；
  potrs 不变。病态 case 顾虑由两步结构自身消解（直审挂→残差层兜底复核）。
- 落地动作：随 criteria 改造轮落地（cards 主判/诊断对调、批量卡同步、CRITERIA_VER
  递增）；施工待向任务侧发出令。

### 2026-09-24e · A0 批量抽样不落 npz：现场构造、现场使用

- 原话或要点：「不采用 npz 了吧，就用我们之前的方案，现场构造，现场使用」——A0 抽样
  通路沿用流式方案（stream_check 型：生成→喂被测→判定→即弃），不再经 npz 落盘中转。
- 影响到哪些既有结论 / 文档：A0 设计中「npz 物化完整 batch 张量」一步取消；batched
  包 README 的「第 0 步造数落盘」接入方式随 v3 改为 DUT 挂钩式（sim_dut/stream_check
  同型接口）；sample_map 仍随包固化不变。
- 落地动作：随 A0 施工线落地（施工现暂停，待问题捋清）。

### 2026-09-24d · 混合容差层用 opbase 最新版标准

- 原话或要点：「用新版」——针对 C1（任务书 rtol=2⁻¹⁰/atol=2⁻¹⁶ vs 标准 2⁻¹³）的裁定。
  背景实证：任务书数值逐字来自 opbase 旧版 experimental_standard.md；opbase 于
  2026-09-17 提交 233ee24 改阈值并改名 mixed_tolerance_standard.md（FP32 rtol=atol=2⁻¹³），
  又于 2026-09-24 提交 a5e8e71 定义 max_abs_error_limit = max(兜底值, 32·ULP(g_low))
  动态锚点与 ±inf 收窄比对规则。
- 影响到哪些既有结论 / 文档：T3 张力收束（标准套 2⁻¹³ 升主判，任务书套退役待任务侧修表）；
  C2 两方案同日晚些时候裁定采标准动态锚点（24g，HT-1 转可开工）；INF/NAN 期望类获得判定依据；
  repos/opbase-precision-standard 快照（09-23）过时，以 repos/opbase clone 为准。
- 落地动作：
  - [ ] criteria 混合容差层换 2⁻¹³ 双参 + 动态锚点 max(1e-2, 32·ULP(g_low)) + ±inf 收窄
  - [ ] 与 A1/A2/A3/B 档合并为一轮 criteria 改造（A5 裁定后一并动 cards）
  - [ ] 提醒任务侧修任务书表与失效的标准 URL

### 2026-09-24c · 执行免仪式：流程跟建线/跑线走

原话要点：「写 skill 的时候需要这个流程，用 skill 的时候不需要。」「直接执行就好了。」

落定：**跑线零仪式**——新任务书/同族复跑/扩量/复测一律照 SKILL 直接执行
（freeze → gen → 装包 → 演练 → 交付），判断只保留「对账异常即停」；一键 driver
为其标准形态。**建线才有流程**——仅当任务书要求现有机器没有的机制（新算子族、
新判定语义、契约变更）才走 spec/评审，且只审增量。与仓规评审触发条件一致
（核心判定逻辑与对外契约改动才触发）。

### 2026-09-24b · 六包补发纯脚本 v2，v1 废止

原话要点：「补发 v2 的六包，老的不要了」。

落定（随后订正）：原负责人 澄清 v1 六包尚未到开发者手里——**直接撤下替换，无需废止
通知**；v1 归档为内部证据。交付形态统一为**十包全纯脚本一次交齐**（六算子 v2 +
batched 四包），开发者首见即统一形态。改动面：render/sim/build 的非批量
materialize 通路 + 纯脚本 README 模板泛化 + 六包 v2 装配与冒烟 + 十包统一交付说明。

### 2026-09-24a · 数据策收束：新包一律纯脚本，全都现场造

原话要点：「全都现场造，无论大小」（背景：batched 包是否保留小 case 冻结 npz）。

落定：batched 四包与此后一切新包不携带数据数组（无 npz/golden/ratio 数组），
index 全条目 materialize:"gen"；开发者先运行 gen_data 造数再测，verify 判定时
自行现场重生成（同环境逐位）。23i 的流式裁定由「验收运行形态」升格为「包交付
形态」的统一数据策。已交付六包维持原形。S3 spec 升 v2.1 同步。

### 2026-09-23i · 数据形态裁定：不落盘，流式测试

原话要点：「不能落盘，落盘对硬盘空间的影响太大了」「应该生成以后直接拿来测试就行」
（背景：全量 584 条按冻结 schema 估算 132.34 GiB，n>4096 的 142 条占 104 GiB）。

落定与影响：

- **包内不装大数据**：包 = gen_data.py（显式 seed）+ canonical 清单 + 小表
  （perf_baseline、ratio_cpu 参考，KB 级）+ verify/sim + README + manifest。
- **流式测试**：逐 case 现场生成 → 喂被测 → 判定 → 丢弃，只留判定记录；
  调试可选单 case --dump，非默认。
- **确定性机制替换**：字节冻结 → 显式 seed + manifest 记环境版本 + 生成与判定
  同进程同环境闭环（输入不跨环境旅行，末位差无从发生）；跨环境复现差异按环境
  记录追溯。S1 spec §2.2 冻结 schema 与「F 复用冻结产物」条款对大数据段作废，
  对小表与脚本仍有效。
- S1 子集包（百 MB 级）保留为已验收存档；全量落盘生成取消，替代工作项 =
  流式驱动（gen 逻辑模块化，accept/verify 增流式模式）。
- 交接工程「冻结三档」纪律的适用范围收窄为：小体量中间量与抽验样本。

### 2026-09-23h · 标准指涉用最新版

原话要点：solver_tasks-main 里提到的开源生态标准，用最新的标准。

落定：其文中「开源生态标准/现行标准/原标准」（原引 `experimental_standard.md`，已 404）
一律代换为最新 `mixed_tolerance_standard.md`——三层判定第一层的阈值以最新标准表为准。
连锁：T3 默认升级为「按最新标准表（FP32 rtol=atol=2⁻¹³）计算，任务书数值并列展示、
疑为旧版残留，正式结论留任务方确认」。设计 v2 第 3.3 节与 T3 行已同步。

### 2026-09-23g · solver_tasks-main 就是 solver 的精度标准

原话要点：「不，solver_tasks-main 就是 solver 的标准。原精度标准并不完全适用 solver。」

订正与落定：

- 此前把七点第 2 解读为「采混合容差标准、不采三层制」是误读。正确关系是嵌套：
  **三层判定是 solver 标准本体**，最新混合容差标准充当第一层的逐元素判——这正是
  solver_tasks-main 自身的结构（还原后「按原标准验证」）。
- 兜底默认启用；23f 条目「不默认启用」的表述作废。
- **ratio_cpu 基线进包契约**（兜底阈值参数，CPU 同精度链路逐 case 实测，case-gen 产）。
- solver_tasks-main 的留档空档升级为我们标准的空档，记 T8（Cholesky batched 空章、
  gels 未做、getrs 大 n 与 geev R2 留档待决）。
- 设计 v2 第 0/2/3.3/4 节已同步。

### 2026-09-23f · verify_accuracy 参考 solver_tasks-main

原话要点：精度的 verify 可能要参考 `repos/solver_tasks-main`。

落定：判定标准仍是混合容差（09-23 七点第 2 的裁定不变），但精度实现机制从
solver_tasks-main 取定稿工程件——还原目标构造、ipiv 正序回放、复数 golden z 前缀、
半三角范数口径、冻结三档。LAPACK 残差兜底不默认启用；仅当首包实测出现合法实现被
大面积误报（其已论证的 n·κ·ε 击穿）时，作为向任务方提标准诉求的备选弹药。
设计 v2 第 3.3 节已同步。

### 2026-09-23e · verify_perf 判据数据源

原话要点：性能的 verify 要从 bench_result 中取对应数据作判据。

落定：`verify_perf` 的判据数据逐 case 从 bench_result 取对应条目（按 n/batch/lda/uplo
等参数精确匹配）；case-gen 生成 `perf_baseline` 时完成匹配与去重（cases.json 实测有
重复行），无对应条目的 case 标记性能证据不足。**T1 争议面因此收窄**：数据源已定，
未定的只剩达标公式（比值系数）、统计口径（竞品 avg vs 任务书中位数）与硬边界 10 的
改写方式。设计 v2 第 2 节与 T1 行已同步。

### 2026-09-23d · 双 verify

原话要点：应该有两个 verify，一个验精度，一个验性能。

落定：包内自测辅助件拆为 `verify_accuracy.py`（混合容差双门 + 还原目标）与
`verify_perf.py`（读采样结果对 `perf_baseline` 出逐 P 项比值与参考性提示，受 T1
运行语义约束）；两者都是 accept `criteria/` 的渲染副本，输出均不构成验收证据；
criteria 内核相应扩为「精度判据 + 性能采样协议与基线比较规则」。设计 v2 第 2、3.3
节已同步。与发布方 0928 批次 `verify_accuracy.py` 的命名惯例顺向兼容。

### 2026-09-23c · 裁决主权与自测件关系

原话要点：任务包给开发者辅助自测；仓里的自测代码可以是另外的；考虑是否把开发者
自测代码和 case-gen 产物做统一；accept 必须把判定标准牢牢握在手里——随后收紧为
**判定标准、执行、裁决都必须在 skill 侧**。

影响与落定：

- **裁决主权入设计第 3 节**：判据内核住 accept 的 `criteria/`；验收只用自带实现
  计算裁决，不执行包内或开发者环境脚本出结论；包内 `verify.py` 降格为 case-gen
  渲染的自测辅助件，输出不构成验收证据；重渲染逐字节核对拦无意漂移（警告级）。
- 设计 v1 第 6 节定位 1 原写「权威在任务包内」，据此订正。
- **T6 已确认（原负责人，2026-09-23「T6 按你的建议」）：v1 不统一**，复议条件见设计文档
  第 4 节。（S0 评审曾指出本条目与设计状态冲突——滞后的是本条目，据此订正。）

### 2026-09-23b · 环节目标与两处落定

原话要点：目标是打通 case-gen 实现用例+测试脚本生成，发给开发者开发自测，开发者返回
代码，accept 验收；整个环节先设计好、再打通。

影响与落定：

- **调用点归属定了**：测试脚本由 case-gen 生成随包，调用点归 case-gen。
- **交付形态修正**：乙裁决的「自产·现场交付（档③）」改为「自产·随包交付（档②）」，
  因开发者自测需要同一套测试件；乙内核（自产 + 新起两 skill）不变。
- 全环节设计落 `solver-pipeline-design.md` v1（E1–E4 环节、任务包八件契约、
  两 skill 契约、五处张力 T1–T5、S0–S4 打通计划）。

### 2026-09-23 · 任务包模式、精度标准与数据构造规则（七点）

原话要点（编号沿用 原负责人 原文）：

1. solver 的任务包（case 与脚本）由我们提供：先写 case-gen，产物先给开发者自测；
   验收 = accept skill + case-gen 产物 + 开发者算子。
2. 精度验收用最新标准：
   `https://gitcode.com/cann/opbase/blob/master/docs/zh/ops_precision_standard/mixed_tolerance_standard.md`。
3. 各算子的输入有合法性要求，只有满足要求的才是合法输入；0923 目录的 cu 脚本记录了
   GPU 侧如何构造合法输入，skill 要把这套规则与方式转换过来。
4. `cases.json` 是测试用例描述文件，用来构建精度与性能测试用例。
5. case-gen 功能之一：读 cases.json 与 cu 文件，参考 cu 的数据构造逻辑生成一个
   python 脚本；任务发布者把脚本给开发者用以构造实际数据，同时也用这个脚本生成
   一份测试用例数据给到开发者。
6. 测试用例数据两用：性能测试与精度测试，两者输入值相同。
7. 性能测试从 bench_result.json 读对应时长、与实测比较；精度测试与 numpy 或 scipy
   的 CPU 计算结果比对。

影响到的既有结论：

- **精度标准张力落定一半**：不采 solver_tasks-main 的三层制（那是建议稿），采 opbase
  最新混合容差标准；solver_tasks-main 降级为输入构造与踩坑参考。标准原文待拉取，
  阈值与「解不唯一算子比什么」以原文为准。
- **任务包升级为对外交付物**：消费者从 accept 一家变成开发者自测 + 任务发布者 +
  验收者三方，case-gen 产物成熟度门槛高于乙估算时的内部件口径——成本复核点。
- **角色模型补充**：case-gen 的直接用户是任务发布者（发脚本与数据给开发者），
  不是验收者的内部工序。
- **bench.cu 身份升级**：从「参考耗时产地 + 结构反面教材」升为「合法输入构造规则的
  权威来源」；case-gen 要做的是把 cu 构造逻辑转写成带显式 seed、可落盘的 python
  生成器（cu 自身 rand() 无播种、序列依赖清单顺序，不能照搬只能转写）。
- **case-gen 是「生成器的生成器」**：产物含一个 python 数据构造脚本 + 一份数据集，
  与 BLAS 线 harness-gen 的形态同构。

待 已裁定或向任务方澄清的张力：

- 第 7 点「从 bench_result.json 读时长比较」与硬边界 10「仅有 min/max/avg 只能停在
  摸底态」及任务书「T_A100 开发者实测、预热≥10/采样30/中位数」相撞——bench_result
  是竞品 avg 口径。按第 7 点执行则硬边界 10 需改写，待明示。
- 第 6 点「性能精度同输入」：任务书 P-xx 的性能规格（大 batch）与精度覆盖（九类场景）
  的用例集合不天然重合，「同输入」是按交集执行、还是性能 case 全部附带精度校验，待确认。

标准原文解读（2026-09-23 拉取，存 `repos/opbase-precision-standard/`；
旧 `experimental_standard.md` 已 404，本标准是其后继）：

- **FP32 阈值：rtol = atol = 2⁻¹³，matched_ratio 0.99，max_abs 1e-2 or 32·ULP**。
  与任务书的 rtol 2⁻¹⁰ / atol 2⁻¹⁶ 不一致——第三处待澄清张力：任务书容差与最新
  标准表谁生效。「or」的取向标准原文同样未定义。
- **单标杆比对**：与更高精度实现（CPU 或昇腾小算子拼接）直接比——与第 7 点的
  numpy/scipy golden 吻合。
- **标准只定「怎么比」（逐元素混合容差），不处理解不唯一**；比什么（还原产物、
  指定三角、解向量）仍由任务书定。solver_tasks-main 对旧标准的批判（解不唯一时
  逐元素对比在契约层不成立）对新标准同样成立，所以「先还原、再按标准比」的组合
  仍是必需，不因换标准而失效。
- **§1 用例生成规则是 case-gen 覆盖设计的直接依据**：50/50 均匀/正态与
  solver_tasks-main 的造数规约同源；维度取 2 的幂与 2 的幂减一（n=31 的来历）；
  特殊场景表（空 tensor、标量、边界、INF/NAN）与「全组合覆盖」口径。

落地动作：

- [x] 拉取 mixed_tolerance_standard.md 原文并归档解读（见上）
- [ ] case-gen 契约按「读 cases.json + cu → 产 python 生成器 + 数据集」重写
- [ ] 硬边界 10 的改写或保留，提交裁定
- [x] 调用点归属：2026-09-23b 落定归 case-gen（测试脚本随包）

### 格式模板

- **日期 · 主题**
- 原话或要点：
- 影响到哪些既有结论 / 文档：
- 落地动作：

## 已消化回写的条目

（条目内容回写进正式档案后移到这里，注明回写位置。）
