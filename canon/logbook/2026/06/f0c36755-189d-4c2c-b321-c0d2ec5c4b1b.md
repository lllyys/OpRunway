---
title: session f0c36755-189d-4c2c-b321-c0d2ec5c4b1b · 2026-06-29
updated: 2026-07-02
status: logbook
session: f0c36755-189d-4c2c-b321-c0d2ec5c4b1b
transcript: ""
---

## [2026-06-29T19:41:51+0800] checkpoint f0c36755 — OpRunway init + 设计调研收口

**Intent.** Init OpRunway 工作区,用来构建一系列 workflow/agents/skills 做 NPU 算子验收（三段式：用例生成 ST → NPU 跑测 → NPU↔GPU 性能对比报告）。先以 catlass 打底，后续泛化。用户要求：先 init + 把设计讨论清楚，组件暂不实现。

**Decisions.**
- 定位 OpRunway = NPU 算子「验收」工作区，确立三段式流水线，Task 1 的测试用例集是连接 Task 2/3 的共享脊柱 — implies dossier Pipeline design / OpRunway overview。
- 精度/性能验收以 catlass 自身机制 + 算子任务书目标为准，**不基于姊妹项目 cann-ops-test 的 ops-test「跑没跑崩」4 层判定**（用户明确定调）— implies dossier Acceptance methodology。
- 发布形态：自维护一个 OpRunway 插件仓（skills+agents+workflows），把 skills 部分 external-sync 进 awesome-ascend-skills（cannbot 同款模式）— implies dossier Publish form。
- 编排倾向照 cannbot 的 ops-registry-invoke 模式（workflow 型 SKILL 剧本 + 角色子 agent + JSON 机读证据 + Python 状态校验器），而非 Claude Code 的 Workflow 工具（后者不便打包发布）；Workflow 工具留作我侧批量跑测/调研的加速器 — implies dossier Orchestration design。
- 组件拆分（草案）：1 个 workflow（op-acceptance）+ 3 个 skill（acc-casegen / acc-npu-run / acc-perf-compare）+ 子 agent（任务书解析 / 跑测 / 独立评测）。
- 采用 bureau（canon 工作区）管理决策与 canon。

**Reversals / 更正.**
- 早前说「awesome-ascend-skills 只收 skills，所以 agents/workflows 没法发布」不准确：经 claude-code-guide 核实，plugin 原生可打包 skills+agents，官方/社区 marketplace 两者都收；「只收 skills」是 awesome-ascend-skills 自身策略，非平台限制。结论收敛为：自维护仓可带 agents，只有 external-sync 进 awesome 那条通道只抽 skills。

**Changes.**
- doc/oprunway-design.md (updated → v2：catlass §4、精度/性能验收方法论 §5、组件 §6、发布 §7、开放问题 §10)
- CLAUDE.md (updated：参考项目与复用边界、发布形态、目录结构、bureau import 块)
- doc/oprunway-changes-brief.md (updated：倒序简表 + 待决)
- repos/ (new：clone 12 个仓 ~604M — catlass + cannbot-skills + 10 个算子仓 asc-devkit/ops-sparse/ops-blas/ops-cv/catccos/shmem/oam-tools/amct/hixl/cann-recipes-infer，均 --depth 1)
- canon/ + BUREAU.md + 根 .gitignore + gazette/ (new：bureau:init default 模式 + inspect 渲染，health 全绿)
- 项目 memory：project-oprunway / sibling-cann-ops-test / catlass-task2-mechanics / cannbot-acceptance-methodology

**Open threads.**
- Q1 算子任务书真实格式（已知 md，待样例）→ 定 Task 1 字段映射。
- Q3 精度口径之争：catlass 内置阈值（fp16≈2^-8）vs cannbot 分类（fp16 MERE 2^-10）vs 任务书，以谁为准、如何分层（倾向：任务书 > 统一标准 > catlass 内置作冒烟）。
- Q4 性能口径：catlass 默认 kernel-only（不含 H2D/D2H），必须与外部 GPU 标杆口径一致，等 GPU schema（Q5）。
- 下一步讨论：逐文件把 cannbot ops-registry-invoke 工作流映射到 OpRunway 三段式（阶段/CP/角色/状态）。
- catlass 接法待澄清：被验收算子是 PR 新增的 example，还是需写调用壳的 kernel/模板。
- story 抽屉（characters/timeline/canon）是否清掉待定（本仓是工程项目，仅 software 抽屉相关）。
- 以上决策尚未 file 成 canonical —— 等讨论收敛 + bureau:review 人工提升。

**Source.** transcript （本次会话，未落盘）

## [2026-06-30T10:29:49+0800] checkpoint f0c36755 — Codex 评审收敛，设计升 v3

**Intent.** 让 Codex 对 OpRunway 设计做独立评审，并把采纳点落进设计文档与 canon。

**Decisions.**
- 让 Codex(gpt-5.5) 只读评审设计，存全文 `doc/oprunway-codex-review.md`；核心结论「验收口径未契约化 → 自动化外壳」被采纳为第一原则（契约先行）。
- **精度验收三层口径**（任务书 > 平台统一标准 > catlass 内置仅 smoke），报告出 catlass_compare_pass/standard_profile_pass/acceptance_precision_pass，放行只看 acceptance；任务书宽于平台底线 → PASSED_WITH_RISK，人工 CP 决 — implies dossier ADR 0005 Precision acceptance（解决 Q3）。
- **性能跨平台对比必须同 timing_scope**（必填枚举 kernel_only/device_e2e/host_e2e），默认 kernel-only、取 median/p50；缺/不一致 → 不出结论只出「不可比」；GPU 标杆最小字段契约先定 — implies dossier ADR 0006 timing scope（解决 Q4，框定 Q5）。
- **判定归确定性 validator**：PASS/FAIL 由校验器按 schema + 日志 hash 算，agent 只解释不宣告通过 — implies dossier ADR 0007 deterministic validator（强化 ADR 0004）。
- **共享数据契约/证据链**：一条 case_id 串 任务书→PR→NPU→GPU→判定；补 case_origin/spec_clause_ref/pr_change_ref/oracle_source/tolerance_policy_id/timing_policy_id；oracle 分层枚举 — implies dossier Acceptance contract and evidence chain。
- **仓适配器**：三模式 existing_example/new_example/generated_harness + 7 方法接口（discover/build/materialize_case/run_correctness/run_perf/parse_results/collect_artifacts），解决 catlass 接法歧义 Q2 — implies dossier Repo adapter interface and modes。
- **Task3/整体状态机**：PASSED/PASSED_WITH_RISK/FAILED_PRECISION/FAILED_PERFORMANCE/BLOCKED_WAIT_GPU_BENCHMARK/BLOCKED_INCOMPARABLE_TIMING_SCOPE — implies dossier Task 3 acceptance state machine。
- **组件**：新增共享 acc-common（统一 schema+validator+数据模型），acc-npu-run 内拆「仓适配器/验收判定器」，子 agent 保持三角色不过拆 — 更新 OpRunway component breakdown。
- ADR 0002 重构：catlass 机制是「首仓执行后端」非总规范，正式验收以任务书+统一策略为上层；ops-test 保留为 smoke gate（smoke_only）。ADR 0003 补「接口稳定前不 external-sync」。

**Changes.**
- doc/oprunway-design.md (updated → v3) · doc/oprunway-codex-review.md (new) · doc/oprunway-changes-brief.md (updated)

**Open threads.**
- 优先级 Q3>Q4>Q5>Q6>Q1>Q2>Q8>Q9>Q7；M1 = 定契约 + 用真实任务书/PR 打穿。
- §3 契约字段、§6/§7 阈值仍待首份真实任务书校准（勿空想镀金）。
- 这批 canon 仍是 proposed，待 bureau:review 人工提升。

**Source.** transcript （本次会话 2026-06-30 段，未落盘）

## [2026-06-30T15:02:00+0800] checkpoint f0c36755 — 编排选型核实，ADR 0004 修订

**Intent.** 核实「Workflow 是否可分发 / 能否直接采用 Claude Workflow 工具」，据此修订 ADR 0004。

**Decisions.**
- 经 claude-code-guide 查官方文档核实：① plugin 不能打包 workflow（plugin 组件只有 skills/agents/hooks/MCP/LSP/monitors，plugin.json 无 workflows 字段）；② Workflow 工具要 opt-in（v2.1.154+，Pro 默认关，可被 disableWorkflows 禁）；③ Workflow「No mid-run user input」不支持中途人工 CP；④ skill 不能直接调 saved workflow。
- 结论不翻、更稳：成品编排照 cannbot **混合架构**——skill/command 入口 + 角色子 agent fan-out + AskUserQuestion 卡人工 CP + 确定性 validator 判定；**Workflow 工具不当骨架，只作「可用则用、不可用降级为子 agent」的内部并行加速器** — 更新 ADR 0004（理由换成官方实锤）与 design §9。
- 更正：上一段我曾怀疑「不便打包发布」理由不够准——官方实锤证明它正确，已更正。

**Changes.**
- canon/decisions/0004（更新，仍 proposed）· doc/oprunway-design.md §9（更新）· doc/oprunway-changes-brief.md（更新）

**Open threads.**
- ADR 0004 仍 proposed，待 review；CP 具体卡几道、谁来卡，待后续讨论。

**Source.** transcript （本次会话 2026-06-30，未落盘）

## [2026-07-01T15:42:33+0800] checkpoint f0c36755 — 深挖 catlass-op-generator / ops-direct-invoke，折进设计

**Intent.** 深挖 cannbot 的 catlass-op-generator + ops-direct-invoke，找 catlass 验收可复用资产，并把结论折进 canon/design。

**Decisions.**
- cannbot 覆盖：只有 catlass（catlass-op-generator）、tilelang（tilelang-op-orchestrator）有专属编排，asc-devkit 是底层方法；其余 8 个仓（sparse/blas/cv/catccos/shmem/oam-tools/amct/hixl）无覆盖——泛化时它们是 greenfield，强化 Q9 仓适配器抽象。
- catlass-op-generator = ops-direct-invoke 的 catlass 特化，二者都是「开发/生成」导向、非验收导向，复用要剥掉「选型+设计串讲+100 分评分」创作层。
- **它本质是个 generated_harness 生成器**，给了现成配方：调用壳拆分（example main()→host ACL+`<<<>>>` + op_kernel `Kernel{}(params)`、去 DeviceGemm）+ CMake 注入（`-I catlass/include` + `-DCATLASS_ARCH`、禁用 catlass 自家 CMake 函数）+ 确定性脚本 `verify_cmake_config.py` + `run.sh` 流水 —— 折进 [[Repo adapter interface and modes]]。
- **验收性能默认工具改为 `msprof op`（不是 msTuner）**：msprof op = profile 交付的 kernel（验收），msTuner = 搜最优 tiling（调优）；两者同为 kernel-only Task Duration，timing_scope 不变 —— 更新 [[catlass acceptance mechanics]] 与 [[ADR 0006 — Compare performance at a matched timing scope]]。
- 吸收：ops-direct-invoke 的「代码 bug vs 精度问题」诊断分类法，编进 validator 的不达标归因。
- 反向验证：cannbot 用「文本 summary + LLM 主观判定、无人工 CP」，我们已定的「JSON 证据 + 确定性 validator + AskUserQuestion 人工 CP」（[[ADR 0007 — Verdicts come from a deterministic validator]]）明显更强，坚持不退回。

**Changes.**
- doc/oprunway-cannbot-catlass-reuse.md（new，M1 实现参照）· doc/oprunway-design.md §4/§5/§7（更新）· canon: catlass-acceptance-mechanics（canonical，内容精化）、ADR 0006、Repo adapter（更新）· 简表。

**Open threads.**
- generated_harness 的「包住 PR 现成 kernel」比 cannbot 的「从 DESIGN 生成新 kernel」少一层，具体套壳待首个真实 PR 验证。
- 验收性能工具 msprof op 已定；其余 8 仓适配器仍 greenfield。

**Source.** transcript （本次会话 2026-07-01，未落盘）

## [2026-07-01T16:56:52+0800] checkpoint f0c36755 — 深挖 AscendOpTest（M1 精度/性能实体）

**Intent.** 任务书精度=AscendOpTest 默认阈值、且它「性能也能用」；用 workflow 深挖它，校准 Task 2。结论经对抗验证。

**Decisions.**
- **复用判定 = hybrid**（对抗验证通过）：精度高度复用、性能部分复用、catlass 接入必须补桥 — implies dossier ADR 0008 Reuse AscendOpTest for Task 2 (hybrid)。
- **精度阈值落地**：FP16 默认 = tolerance 1e-3 + error_rate 1e-3（允许 0.1% 坏点）；判据 `|golden|≥1` 相对 / `<1` 绝对（共用阈值）+ 坏点占比；只出 pass/fail 无数值指标；复用 `compare.py`+`accuracy_config.py`，我们供 `expect_func`（numpy 融合 golden）。这是精度三层「平台层」的实体（[[ADR 0005 — Precision acceptance is a three-layer policy]]）— implies dossier AscendOpTest precision thresholds（verified）。
- **性能**：AscendOpTest 唯一可消费口径 = kernel-only（msprof op → Task Duration），确认 [[ADR 0006 — Compare performance at a matched timing scope]] 不推翻；但它 `get_prof.py` 不算 speedup、无 torch 通路 → **1.2× 基线（torch 未融合链）由我们 generated_harness 自测、比值由 validator 算**；caveat：1.2× 必须同 timing_scope 对同 timing_scope（torch 链是 host/e2e，别拿它 e2e 比融合 kernel-only）— 更新 ADR 0006。
- **catlass 接入**：AscendOpTest 硬绑 aclnn 两段式 API，catlass 裸 kernel 不能直测。两条桥（都落 generated_harness）：路线 A 封成 aclnn 自定义算子（终交付）、路线 B 自造 exe 遵守框架协议（快验证）— implies dossier catlass to aclnn bridge。
- Task 2 简化：acc-npu-run = 包一层 AscendOpTest 做精度 + 自建 perf-ratio validator + generated_harness 产出 aclnn 桥。

**Open threads.**
- OQ1：FP16 融合经 Matmul 累加能否稳过 1e-3/0.1% 坏点？可能要 fp32 中间 golden 或放宽——M3 实测。
- OQ2：1.2× 分母口径（kernel-only 之和 vs device-e2e）用户正在确认。
- OQ3：路线 A 封 aclnn 时 group(G)+可变 M 的动态 shape / group_list 原型对齐。
- 本批新页仍 proposed/verified，待 bureau:review；catlass-acceptance-mechanics 上轮被手改+自盖 reviewed，需补进 review 队列。

**Source.** transcript （本次会话 2026-07-01 AscendOpTest 段，未落盘）

## [2026-07-02T09:58:57+0800] checkpoint f0c36755 — 转向通用工作流：规则库 + harness 4 职责 + e2e 更正

**Intent.** 用户明确「要做通用工作流、不只这一个算子」。手写用例集 → 对抗评审 → 提炼通用规则，正式化进 canon。

**Decisions.**
- **定位纠正**：手写 cases.yaml 是**夹具/规则发现器**，不是产品；产品 = acc-casegen（跨算子生成器）+ repo-adapter/harness（跨仓/跨框架跑起来）。评审证明 golden 数学对、覆盖合格，但用例「一条跑不起来」——卡点全通用。
- **acc-casegen 核心 = 「原语→case 规则库」**：算子=原语组合，每类原语（归约/matmul-分组/有界激活/elementwise）挂 case 模式 + 展开逻辑 + 元规则（golden fp32→降 dtype、动态语义先从 host 锁、每 case 标 compare 分支/数据来源/条款/PR）— implies dossier Primitive-to-case rule library。
- **repo-adapter 核心 = generated_harness 4 职责**：① bin-IO shim ② layout 字节契约(logical vs bin) ③ 数据注入(预置 bin 同源) ④ 性能测量栈 — implies dossier generated_harness responsibilities。catlass-to-aclnn-bridge 是「catlass×AscendOpTest」的具体实例，4 职责是通用抽象。
- **AscendOpTest e2e 更正**：不是不能测 e2e——采集层内建 `msprof --application`(run_test.py:341，含 H2D/D2H)，只是自带 `get_prof.py` 只解析 kernel-only 的 OpBasicInfo.csv(:60/:65)；端到端由我们驱动 --application + 自解析 op_summary + 裁到算子窗口（避开 init/文件IO）。→ device_e2e 可行，融合类走 device_e2e 更站得住。更新 ADR 0006 + probe doc。

**Changes.**
- canon: Primitive-to-case rule library（new,proposed）、generated_harness responsibilities（new,proposed）、component-breakdown/repo-adapter/ADR 0006（更新,proposed）
- doc: oprunway-task1-cases-critique.md（评审提炼，已建）、design §5/§7/§9、ascendoptest-probe（e2e 更正）
- fixture: reports/.../cases.yaml（草稿，待作 acc-casegen 金标准输出，含待修项）

**Open threads.**
- OQ2 1.2× 分母口径用户确认中；device_e2e 现已确认可行。
- 下一步：据规则库 + 4 职责写 acc-casegen 与 repo-adapter/harness 两组件规格。
- 本批新页 proposed，待 bureau:review。

**Source.** transcript （本次会话 2026-07-02，未落盘）

## [2026-07-02T·acc-casegen] checkpoint f0c36755 — 起草 rule-catalog（acc-casegen 首个组件产物）

**Intent.** 把 canon 的「Primitive-to-case rule library」从设计填成能用的 `rule-catalog.md`（acc-casegen 核心 IP）。

**Decisions.**
- 建 `plugin/skills/acc-casegen/references/rule-catalog.md`（acc-casegen skill 骨架起步）。v1 手写 → 对抗评审（4 lens，对照 catlass 74 example，40 findings）→ **v2**。
- 评审揭示 v1 三大病并已在 v2 修：① 覆盖漏洞（~40% example 家族无骨架/被 matmul 静默吞并→补 attention/conv/swiglu/gather-scatter/sparse/strided + `UNCOVERED_PRIMITIVE` 硬 guard + identify_by 排他）；② Ascend 硬件契约缺失（NZ/zN/nZ 分形、splitK 原子加归约、tiling/BlockDim、workspace、dtype 相关对齐）；③ 数值机理错误（large_K/large_reduce 的 why、bf16 归因、golden 一路 fp32、rsqrt 近似、MX/round-mode）。
- 结构修：命名 base+扰动（可复现）、三轴叠加策略（防爆炸）、data_rule 阈值绑 dtype（可实例化）、composition 冲突消解、exec_unit/arch 标签。

**Changes.**
- plugin/skills/acc-casegen/references/rule-catalog.md（v2，new）· doc/oprunway-rule-catalog-critique.md（new）· 简表。

**Open threads.**
- rule-catalog 是 [[Primitive-to-case rule library]] 的实现（canon 页设计不变，实现落 plugin/）。
- prec 类阈值 + tiling 具体值待 M3 真机校准。
- 下一步：acc-casegen 的 SKILL.md（展开逻辑）+ 用 v2 重生成夹具 cases.yaml 验证；或先啃 harness。

**Source.** transcript （本次会话 2026-07-02 acc-casegen 段，未落盘）

## [2026-07-02T11:27:37+0800] checkpoint f0c36755 — 泛化架构定调：一个 workflow + 每仓薄适配器

**Intent.** 定「每仓一个工作流 vs 一个泛化工作流共享」。

**Decisions.**
- **一个泛化 op-acceptance workflow + 共享 acc-casegen 规则库 + 共享验收 policy + acc-common schema；每仓(×每框架)只写一个薄 repo-adapter/generated_harness；不为任何仓 fork 工作流** — implies dossier ADR 0009 One generalized workflow with per-repo adapters。
- 理由：结构全仓一致（避免冗余/漂移）；验收语义必须一致（否则「通过」各仓含义不同）；演进改一次全生效；变化沿「仓轴（算子怎么 build）+ 框架轴（AscendOpTest vs 其它）」两轴，都被 adapter/harness 吸收，workflow 坐其上共享；catlass = 第一个 adapter 而非「catlass 工作流」；cannbot 同款（ops-direct-invoke 底座 + 薄特化）。

**Open threads.**
- ADR 0009 proposed，待 review。
- 下一步仍是：用 v2 规则库重生成夹具验证 / 写 acc-casegen SKILL.md / 啃 harness 三选一。

**Source.** transcript （本次会话 2026-07-02 架构段，未落盘）

## [2026-07-02T15:05:28+0800] checkpoint f0c36755 — 真机 de-risk 通过（ascend-a3）+ torch 参考拆解洞察

**Intent.** 上真机验证整条机器链路能不能编能不能跑 catlass。

**Decisions / 进展.**
- **torch 参考拆解洞察（待折入）**：把融合算子拆成标准 torch 小算子拼接 = **一份可移植的参考实现**——torch_npu 跑=NPU 未融合基线(1.2× 分母)、torch.cuda 跑=GPU 标杆(Task3)、fp32/CPU 跑=golden；一份拆解三处用，且来自原语拆解。推论：GPU 标杆不必外部黑盒给，其定义=约定的 torch 拆解跑 GPU，可复现。坑：语义等价/签名对、device 算子映射两套、同 timing_scope、golden(fp32) vs baseline(device dtype)。**尚未折进 canon（Acceptance contract + rule-catalog 原语→torch 映射），待用户点头。**
- timing_scope 概念澄清：kernel_only/device_e2e/host_e2e；融合算子倾向 device_e2e；OQ2「1.2× 分母」本质就是选 scope。
- **真机 de-risk 通过（2026-07-02，ascend-a3=910/A3/arch2201/CANN9.0.0-beta.1）**：catlass tar 传上 `/home/lys/catlass` → `build.sh 00_basic_matmul` 编成 → 跑 `256 512 1024 0` → **Compare success.**。证明：机器能编能跑 catlass、CPU golden 路径 work。
- **远程联网隧道**：autossh `-R <隧道端口>:localhost:<代理端口>`（→本机 <代理端口> 代理），远程经 `http_proxy=127.0.0.1:<隧道端口>` 实测 gitcode 200。
- 关键约束记牢：本机是 **910/A3(2201) 不是 950(3510)**——只 de-risk / 跑 A2/A3 example，真 950 算子需 950 机器；无 CANN build 镜像→host 编译(不动系统)；torch_npu 未装(perf 基线要用时装)；AscendOpTest install 会写共享 opp/vendors→用 ASCEND_CUSTOM_OPP_PATH。

**Changes.**
- CLAUDE.md「远程 NPU 环境」节（待补→已填 ascend-a3 全部事实）· memory: remote-npu-ascend-a3（new）+ MEMORY.md · 远程 `/home/lys/catlass`（编译产物）· autossh 隧道（本机后台）

**Open threads.**
- torch 参考拆解待折进 canon（Acceptance contract + rule-catalog）。
- 下一刀候选：AscendOpTest 桥真机验证(bin-IO shim) / 多编贴近算子的 example / 本地做 acc-common schema+torch golden builder / bureau:review 清一批。
- OQ2 1.2× 分母口径用户确认中；rule-catalog v2 的 prec 阈值 + tiling 具体值待真机校准（M3）。

**Source.** transcript （本次会话 2026-07-02 真机段，未落盘）

## [2026-07-02T15:12:12+0800] session filed f0c36755 — 全会话汇总（init → 设计收口 → 组件起步 → 真机 de-risk）

**Intent.** Init OpRunway（NPU 算子验收工作区），把「任务书+PR → 用例(ST) → NPU 跑测 → NPU↔GPU 对比」做成一套**通用** workflow/agents/skills，先以 catlass 打底。本会话跨 init→设计收口→组件起步→真机 de-risk。

**Decisions（全会话，均已成 canon dossier 或待折）.**
- 定位/流水线：三段式 + Task1 用例集为脊柱（[[OpRunway acceptance pipeline]]）；契约先行、一条 case_id 串到底（[[Acceptance contract and evidence chain]]）。
- 验收依据（ADR）：不基于 ops-test（[[ADR 0002 — Acceptance grounded in catlass and the spec]]）；发布自维护插件仓+skills sync（[[ADR 0003 — Publish as self-maintained plugin and sync skills to awesome-ascend-skills]]）；cannbot 式编排、非 Workflow 工具（[[ADR 0004 — Orchestrate like cannbot ops-registry-invoke]]）；精度三层（[[ADR 0005 — Precision acceptance is a three-layer policy]]）；性能 timing_scope（[[ADR 0006 — Compare performance at a matched timing scope]]）；判定归 validator（[[ADR 0007 — Verdicts come from a deterministic validator]]）；hybrid 复用 AscendOpTest（[[ADR 0008 — Reuse AscendOpTest for Task 2]]）；一个泛化 workflow+每仓薄适配器（[[ADR 0009 — One generalized workflow with per-repo adapters]]）。
- 机制：仓适配器 3 模式（[[Repo adapter interface and modes]]）+ catlass 桥（[[catlass to aclnn bridge for AscendOpTest]]）+ generated_harness 4 职责（[[generated_harness responsibilities]]）；acc-casegen 原语→case 规则库（[[Primitive-to-case rule library]]，已填 v2 实现 rule-catalog.md）；AscendOpTest 精度实体（[[AscendOpTest precision thresholds]]）；Task3 状态机（[[Task 3 acceptance state machine]]）。
- 待折入：torch 参考拆解 = golden/NPU 基线/GPU 标杆单一来源。
- 真机：ascend-a5（真 950/3510）主力 + ascend-a3（910/2201）备用，catlass 两台均 Compare success。

**Changes（全会话）.**
- `CLAUDE.md`（多次：复用边界/发布/目录/远程环境两台机器）
- `doc/`：oprunway-design.md（→v3）、changes-brief、codex-review、ascendoptest-probe、task1-cases-critique、rule-catalog-critique、cannbot-catlass-reuse
- `canon/`：14 个 dossier（decisions 0001–0009 + 8 architecture，共 7 canonical / 12 proposed / 1 verified）+ 本 minute
- `plugin/skills/acc-casegen/references/rule-catalog.md`（v2）
- `reports/catlass/LayerNormGroupedMatmulBiasSilu/_nopr/cases/cases.yaml`（首个夹具草稿）
- `repos/`：clone 12 仓（catlass/cannbot-skills/AscendOpTest/+9）
- memory：project-oprunway / catlass-task2-mechanics / cannbot-acceptance-methodology / remote-npu-ascend-a3 / sibling-cann-ops-test

**Open threads.**
- 12 个 proposed dossier 待 `bureau:review` 提升。
- torch 参考拆解待折进 Acceptance contract + rule-catalog。
- 下一刀：AscendOpTest 桥真机验证（a5，路线 B）/ acc-common schema+torch golden builder（本地）/ 多编 950 example。
- 卡外部：真 950 算子 PR（未开发）、OQ2 1.2× 分母口径（用户确认中）；prec 阈值+tiling 待 M3 校准。

**Source.** transcript （本次会话，2026-06-29 ~ 07-02，未落盘）
