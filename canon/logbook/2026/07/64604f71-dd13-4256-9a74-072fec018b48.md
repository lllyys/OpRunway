---
title: session 64604f71-dd13-4256-9a74-072fec018b48 · 2026-07-09
updated: 2026-07-09
status: logbook
session: 64604f71-dd13-4256-9a74-072fec018b48
---

## [2026-07-09T09:26:59.887Z] context checkpoint (compaction)

_Context was compacted here. Full transcript on disk. Run `bureau:note` to capture live minutes._

## [2026-07-09T13:03:14Z] 收口②：散文全仓同步 + provenance 门 + T4 接线调查（手工 capture，低权威）

> 手工追加的 minute（bureau:note 命令在本会话不可调）。低权威 capture 层，待 compile→review。

**已落地入库（本地 commit，未 push；author=lys）**：
- `306d975` provenance 门对抗攻击 4 条：`precision_policy.compute_metrics` 补 out 侧 dtype 支持校验 + out/golden
  dtype 严等（拒 complex-out 数值、拒 uint8/bool 跨型逐位比假通过）；产物定位 base 走 realpath 且拒 `..`；
  numpy/precision_policy import 失败统一判 FAILED 不 crash；sha 校验与 `np.load` 合成 `_load_verified`
  堵 TOCTOU。判定逻辑不越 ADR 0007（重算属证据可信、pass/fail 仍 validator 裁）。
- `8d3d515` 散文 5 缺陷经 codex 散文门 3 轮扩为全仓同步 9 文件：(a) `acceptance.json.overall` 门失败真实值
  `"BLOCKED(验收门未过)"(exit 1)` 非裸 BLOCKED；(b) 「均 tbe」加 catlass matmul(baseline=None) 后成假 →
  限定 aclnn 重写类 + 标 catlass 例外；(c) exception→PASSED_WITH_RISK 补 gate_task3 门前置；
  (d) baseline.json 产物枚举标「有基线时」（仅 baseline is not None 落盘）；(e) ADR 0006(proposed)/0007(canonical)
  tier 分清。**门价值实证**：只改命中点=没扫净，门逐轮抓出散落多处。
- `7a25f0f` T4-④ scope 常量漂移守卫（`perf_compare._VALID_SCOPES`＝`gpu_baseline._SCOPES` 一致性测试）
  + 纠 ① 误导注释。

**T4「补 4 处接线」调查结论（并行 4 agent + 亲手复现）——原 TODO 措辞把它们框错了**：
- ①（gen_cases 注册 catlass golden）**drop**：GOLDEN 是 elementwise 引擎、与 matmul (m,n,k) plan 结构不兼容，
  塞进去无法工作；catlass golden 由 catlass_adapter 独产、零重复、无漂移。
- ②（run_workflow --baseline）**drop**：catlass 无 builtin-TBE 分母（走 gpu_external），aclnn TBE 基线带内产
  `_real_baseline.json`——两条真实注入路径已覆盖，第三条外部非 GPU 基线不在设计内。
- ③（门 NON-ACCEPTANCE 标）**证伪**：agent 断言「synthetic catlass_mock → PASS acceptance.json」经实跑
  **REFUTED**——`run_workflow.py:76` 无条件先走 gen_cases、catlass op 直接 ValueError 崩；`catlass_adapter.main`
  只产 development-grade evidence（显式带 acceptance_note=NON-ACCEPTANCE）、**不产 verdict/acceptance.json、不跑门**。
  **无可达路径让 synthetic catlass demo 伪造 PASS**——现设计在 catlass 线的防假验收是 sound 的。
  （可选纵深防御：门读 evidence_grade、拒 development-grade→干净 PASS，guard 未来若真把 catlass 接进门；
  属设计取舍、抛用户，非当前洞。）
- ④（perf_compare↔gpu_baseline 去重）**drop**：无匹配逻辑可抽（perf_compare 根本没用 fingerprint）；真实但
  低概率的是 scope 常量漂移——已用一致性测试兜住（`7a25f0f`），非重构。

**canon 候选（待 compile→review）**：路线 C（repo-native harness）对 `catlass-to-aclnn-bridge`【canonical】的更正
（用户已拍板接受路线 C）此前草拟、codex 门待重跑；本轮未 compile。ADR 0007 门职责边界（重算属证据可信、非重判）
可固化。这些走 bureau tool 车道（compile 产 proposed），promote 为 canonical 是人门 `bureau:review`。
