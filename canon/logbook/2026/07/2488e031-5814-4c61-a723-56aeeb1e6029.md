---
title: session 2488e031-5814-4c61-a723-56aeeb1e6029 · 2026-07-13
updated: 2026-07-15
status: logbook
session: 2488e031-5814-4c61-a723-56aeeb1e6029
transcript: "~/.claude/projects/-Users-ll-Desktop-workspace-ascend-OpRunway/2488e031-5814-4c61-a723-56aeeb1e6029.jsonl"
---

## [2026-07-15] session 2488e031 — usertest 定稿落地：V1/Q1/Q9/Q7 + golden 最高律令

**Intent.** 承接 session 0513d745 的干净用户测 11 问定稿（`doc/oprunway-usertest-findings-design.md`），把 P0–P2 改法逐条落地：V1 dtype 来源红线、Q1 spec 样例隔离、Q9 golden 接线、Q7 dtype fail-closed 门。方法：全程 ultracode fan-out（调查 → 对抗核验 → 落地 → 再核验）+ codex 门 + 真机（a3）验证。

**Decisions（逐条附 dossier 归属）.**

- **最高律令 · golden/精度标准只来自任务书指定的测试方法**（用户 2026-07-13/14 多次明示）：golden 的**值**源于任务书指定的测试方式/方法测出来的值，**不是**照理解自撰 numpy（除非任务书要求）；精度标准（阈值/oracle/口径）只来自任务书；任务书要求的标准/方法**不在支持范围 → fail-closed 抛用户**、绝不静默降级。是 [[Task spec is authoritative over PR]]（绝不信 PR）在精度维的延伸，强化 [[Verification code provenance for runner and golden]]。→ 新 dossier「Golden and precision standard come only from the task-doc-specified method」。

- **AscendOpTest 自己没有 golden 源**（真代码坐实，两轮 fan-out）：它只有 `expect_func`（开发者写的参考函数）与 `golden_path`（预置 bin）两个槽位——`compare.py` 只读文件+阈值、`compute.py` 跑用户函数、`gen_case.py` 把 `expect_func` 留空。故任务书写「满足 AscendOpTest 默认阈值」只指定了**比对方法+阈值表**、**不指定 golden 值源**（golden 源在任务书别处：任务概述/参考实现字段）。印证 [[AscendOpTest precision thresholds]]（canonical：golden 由 expect_func 提供）与 [[ADR 0008 — Reuse AscendOpTest for Task 2]]。→ 新 dossier「AscendOpTest provides no golden source only a comparison harness」。

- **golden = CPU 标杆、固定用 torch(CPU) 单后端**（用户 2026-07-14 拍板）：torch 与 numpy 在边界不一致（`torch.sign(NaN)=0` vs `np.sign(NaN)=NaN`）→「谁装了用谁」会产随环境而变的**非确定** golden；故 golden **恒走 torch**、torch 缺失 → fail-closed 报错要求安装（**不回退 numpy**）。精度验证一般在装了 torch 的 NPU 机器上做。四算子 reference 已核任务书原文：IsClose/Equal=语义改造「二进制→对齐 CPU 逻辑比较」、Sign/Neg=纯重写、Neg uint8 任务书点名 `torch.neg` 回绕(256-x)。→ 新 dossier「Golden is fixed to torch on CPU for determinism」。

- **V1 · dtype 全集来源绝不信 PR**（落地 `45084c0`）：acc-spec 三入口（`SKILL.md` / `references/taskdoc-to-spec.md` / `agents/acc-spec-extractor.md`）dtype 全集来源改「任务书显式表 > 原 TBE 算子信息库（独立源，非 PR）> 问用户」；PR 的 `op_def` 降为**仅对照记 gap**；独立源未接通/新算子 → 问用户、绝不回退读 PR。落实 [[Task spec is authoritative over PR]] 的「标准来源路由」节。⚠ TBE 信息库独立源当前**未接通**（`fetch_source` 不取；读法随运行环境本机/ssh/ssh+docker，接时探测、不写死 ssh）= TODO(a)。

- **Q1 · spec 样例隔离**（落地 `45084c0`，即缺陷修复）：真样例迁 `samples/specs/`、建零真值空模板 `spec_schema_template.jsonc`、acc-spec 三入口写死「产 spec 禁读任何 `.spec.json`」、测试重定、archive_ops 内联副本、新增 `test_spec_isolation.py` 守门。**此即 [[Spec examples pollute acc-spec derivation]] 所述缺陷的修复**——该页 verified 认定（基于修复前 taskdoc:5 指向 + specs/ 含真答案）已过时，本会话已手工降 proposed + 撤 `_verify.json` 指纹使 commit 诚实，**待本次 compile 正式刷新为「已修复」**。

- **Q9 · golden 接线**（落地 `0894a2a`）：gen_cases golden 改 torch-required（`_require_torch`；四内置 elementwise 的 `golden_source` 恒以 torch 开头、下游映射的 `oracle_source` 恒 `torch_ref`；gen_cases 只出 `golden_source`、不直出 `oracle_source`；catlass 路径另为 numpy→`analytical_ref`、非全局 torch_ref）；`precision_policy.select_standard` 白名单 fail-closed（未知 oracle raise、堵 class C「与 python 一致」静默降级）；`oracle_source` 止血（删 `repo_adapter`/`catlass_adapter` 两处写死 `cpu_ref`、据 caseset `golden_source` 严格首 token 据实映射，缺失 fail-closed）；catlass spec 补 `precision.standard`。治了 [[oracle_source is a hardcoded constant not a recorded fact]] 的假常量。顺带修传输 GNU-tar 可移植性（`_deploy.tgz` 写打包目录外，否则 Linux/server 报 file-changed exit 1）。

- **Q7 · dtype fail-closed + 两门校**（落地 `7a96581`）：spec 加 `dtype_required`(任务书权威全集)/`dtype_tested`；`validate_acceptance_state` 加两门——dtype 覆盖门（required 未被**真实用例** dtype 覆盖且无 `dtype_deferred` 挂账 → BLOCKED）+ oracle_source 一致门（`evidence.oracle_source == 映射(golden_source)`）。**改判据为真实生效对象、不再信自报的 `dtype_tested` 与 evidence 的 oracle_source**（防「跑子集报全」的 dtype 粒度 + 防伪造 evidence 篡改 oracle_source）。⚠ 半闭合：`dtype_required`/`dtype_deferred` 仍取自 caseset 自报结构、其任务书权威来源（原 TBE 信息库）未接通，故 dtype 门「任务书要求」侧尚未真正锚到任务书。IsClose 权威全集={float32,float16,bfloat16,int32}+deferred gap；Sign/Equal/Neg=`needs_user`（全集待信息库/用户、门不硬 BLOCK）。强化 [[A gate must validate the object that actually takes effect]] 与 [[Gate checks evidence integrity not verdict]]。

**Changes（3 commits，均在 `fix/q2q3-transport`、未 push）.**
- `45084c0` V1 dtype 来源红线（acc-spec 三入口散文）+ Q1 样例隔离 + compile（0513d745 minute → 4 canon 页）。
- `0894a2a` Q9 golden torch-required（`gen_cases`/`precision_policy`/`repo_adapter`/`catlass_adapter`/门/specs/测试）+ 传输 tar 可移植性修。
- `7a96581` Q7 dtype 覆盖门 + Q9 oracle_source 门校（`validate_acceptance_state`/`gen_cases`/specs/测试）。
- 均过 codex 门（散文 `codex exec` / 代码 9 维）；golden+门相关在 **a3 真 torch（py3.13 / torch 2.13.0+cpu venv）跑 `acc-common` 全量 14 测全绿**（本机 py3.14 无 torch wheel，e2e 转 a3）。

**Open threads.**
- PR #6（marketplace+Q2/Q3）待用户 merge；3 个本地 commit push 待用户定。
- bureau：本 minute 待 compile→review；`spec-examples-pollute` 页正式刷新为「已修复」。
- 小尾巴：Q9 golden NaN/±0/Inf 边界覆盖（codex #6）；run_workflow 级「Q7/Q9 门失败→BLOCKED」端到端断言（codex #3，a3 e2e 已跑真实流）；legacy 无 `dtype_required` 的宽容可用 schema version 收紧。
- (a) TBE 信息库接通（dtype 独立源，环境探测读法）。
- 外部阻塞：真 GPU 基线数据、其余 11 仓 adapter、catlass 真机验收。

**Lessons（方法论）.**
- **torch-required → 测试上 server**：golden 恒 torch，本机 py3.14 装不了 torch（无 wheel）；测试挪 a3（py3.13 + CPU torch venv）。用户定调「不依赖 mac、工作流可在服务器起、验收必在 NPU」。
- **门信自报字段 = 跑子集报全**：Q7 dtype 门初版读 caseset 自报 `dtype_tested` → 手搓可绕；改用真实 cases 对账（[[A gate must validate the object that actually takes effect]] 的又一实例）。
- **codex 门抓真 bug**：Q9 抓出 torch/numpy `sign(NaN)` 非确定性（推翻「torch==numpy 逐值同」前提）；Q7 抓出抗坏输入 `dtype=dict` TypeError 崩 + 删 `dtype_required` 绕过对账。fan-out 对抗核验 + codex 门互补、各抓不同层。
- **gitignore ≠ 文件不在**：52 份社区任务书在 `repos/cann-ops-competitions`（gitignore 但本机可读）——核四算子任务书 reference 靠它。
- **判绿用进程退出码、不 grep "OK"**（ANSI 色码骗人，反复栽）；**工具调用前不写任何前导字**。

**Transcript.** `~/.claude/projects/-Users-ll-Desktop-workspace-ascend-OpRunway/2488e031-5814-4c61-a723-56aeeb1e6029.jsonl`

## [2026-07-15T02:42:39.930Z] context checkpoint (compaction)

_Context was compacted here. Full transcript on disk. Run `bureau:note` to capture live minutes._

## [2026-07-15] session 2488e031（续）— 精度用例按 opbase §1 生成 + fail-fast + 性能同输入 + bf16

**Intent.** 用户新需求：精度用例生成 50 条、全过才跑性能、一条挂即整体不过并提前结束。随后指向 `cann/opbase` `docs/zh/ops_precision_standard/experimental_standard.md`（pin `f69d4e4e3f2626ddd37855a8d05063a1764ac4c9`）：**§1 用例生成规则采纳为权威、§2 误差指标不用（阈值走 ascendoptest）**；性能与精度**同一套输入**；数量**以用户为准**默认 50；bf16 本轮扩 runner。方法：ultracode 蓝图评审（先审地基）→ fork 分层落 → a3 真 torch 验 → codex 门。

**Decisions（附 dossier 归属）.**
- **精度用例生成规则源自 opbase §1**（浮点计算类；整型/搬运类另定）：dtype×格式×维度×attr 正交覆盖 + §1.4 特殊场景（空/标量/边界/inf·nan）+ 值域 50%均匀+50%正态 + 维度 2ᵏ/2ᵏ−1。§2 误差表**不用**，阈值继续 ascendoptest（现有 `precision_policy` 快照，零改；§0 印证 bool/符号类逐位精确）。是 [[Golden and precision standard come only from the task-doc-specified method]] 的**生成侧**具体化——任务书指定的测试方法（opbase §1）定生成规则、ascendoptest 定阈值。→ 新 dossier「Case generation follows opbase ecosystem precision standard §1」。
- **数量以用户为准、默认 50、运行时问用户**（覆盖 §1.1「不设固定下限、覆盖优先、非机械凑数」——用户明示以其为准）：`spec.precision.case_target`，acc-spec `AskUserQuestion` 问（先 gen_cases `--dry-run` 拿 [强制下限 S, pool_max] 区间呈现）。50 封顶下 §1.1「100% 正交联合」**不可达**——采「白名单强制必覆盖组合（key dtype×attr×大shape、§1.4 特殊场景）+ 常规联合 1-wise 边际采样」、导出覆盖账本（coverage_strength/dropped_combo_classes）。
- **精度门前置 + fail-fast（跑完再判）**：精度非全过（pass/passed_with_risk 之外）→ 跳 Task3 性能、整体 FAIL(精度)、非零退出。**不 early-return**——先跑 gate/runner_source 分支、复用主 overall 路径（评审 #4）。是 [[Task spec is authoritative over PR]] 验收纪律的流程化。
- **性能同输入 + trivial-met**（用户明示 perf 判全部相同输入）：不另造大 shape 性能用例，同一套用例既判精度又测性能；退化 case（numel<4096，broadcast 输出算）标 trivial-met（达标免测、不失败），perf 达标由代表性大 shape 主导。trivial-met 做成贯穿 perf_compare + gate_task3 + GPU 对齐的一等公民。强化 [[A gate must validate the object that actually takes effect]]（trivial/na 门豁免均据 caseset 真实对象复核、防伪造）。
- **空 Tensor 实现**（用户定「现在就实现」）：validator 判 na（严格真空判定，拒 `shape:[false]/[0.0]` 伪造）、三门豁免 + 独立复核、真机 runner 已处理 numel=0。
- **bf16 扩 runner**（用户定「本轮就扩」）：runner.cpp×3 加 `ACL_BF16` dispatch（uint16 存储）+ repo_adapter `_NP`/storage-aware readback。→ 关联 [[Real-NPU runner supports only float32 and float16]]（runner **代码**已加入 bf16 dispatch + codec、mock 验；**真机支持集尚不能扩表**——bf16 kernel 支持未证实、真机验收阻塞 op-build，该页 claim 暂不改、待真机恢复+验证）。

**Changes（未 commit、未 push；均在 `fix/q2q3-transport`）.** gen_cases（§1 覆盖-预算重写，fork 落地 + 我 review + 补回 finding #12 防伪造校验）· validator（na/严格真空/nan-EXACT）· perf_compare（trivial-met/broadcast numel/固定 4096）· run_workflow（fail-fast）· validate_acceptance_state（na/trivial 门豁免+防伪造）· repo_adapter（bf16/空Tensor/readback/修二次-encode）· precision_policy（EXACT nan 对齐）· gpu_baseline（trivial 豁免）· runner.cpp×3（bf16）· acc-spec（case_target）· specs/schema · 22 文件。

**Verification.** a3 真 torch 2.13.0+cpu：mock e2e 全绿（50 用例、精度 pass、**perf 48 条全达标**——含 inf/nan/scalar 等 trivial-met 退化 case，48=2-dtype 那次的性能 case 数、非全 trivial；达标由大 shape 主导——三门 PASSED、总体 PASS、exit 0）+ **fail-fast 验**（注入精度缺陷→跳性能→FAIL(精度)、exit 1）+ bf16 **生成**验（三 dtype 均衡+codec+golden，mock 层）+ **274 单测全绿**。**codex 源码门一轮**抓 4 高全修（广播 numel 蒙混/inf·nan 补性能/固定 4096/严格真空），fail-fast·bf16·nan 判无问题。

**Open threads.**
- ⛔ **真机验收阻塞（环境、非代码）**：a3 `build.sh --ops=isclose` 失败（ops-math/clean 都 --ops not found/CMake error），prior session op-build 配置从当前 a3 复现不出，**挡所有 dtype 真机跑**。bf16 代码 ready+mock 全验+管道验到真 NPU op-build 步；bf16 真机验收（含 kernel bf16 支持性）+ fp16/fp32 真机回归待 op-build 环境重建。isclose spec 里 bf16 现诚实 deferred。
- bureau：本续段待 compile→review；「Real-NPU runner supports only fp32/fp16」页 claim 待更新（runner 已支持 bf16、但真机验收 pending）。
- 小尾巴：equal_nan 有效性未经 aligned-NaN 交集证明（结构覆盖，minor）。

**Lessons.**
- **先审地基**：ultracode 蓝图评审（4 lens）在动土前抓 12 必修（空Tensor 三道硬拒、1-wise vs 100% 正交、全 case 贴性能 tag、越权重判门…），省下一路错到底。
- **mock e2e 抓集成缺口**：门的 na/trivial 豁免、perf_cases 计数、evidence 契约——都是 mock 真跑才现（读代码看不全）。
- **codex 门抓真安全洞**：广播 numel 蒙混 trivial、`shape:[false]` 伪造真空、阈值 compare↔gate 不一致——fan-out 评审 + codex 门各抓不同层。
- **真机失败 2-3 次即停**：op-build 环境坑不钻，如实记「代码 ready、环境阻塞」，别耗在不可复现的 prior 配置上。
