---
title: session 9f5c778e-cdf9-4c84-bb4f-f0ab8c49a99d · 2026-07-24
updated: 2026-07-24
status: logbook
session: 9f5c778e-cdf9-4c84-bb4f-f0ab8c49a99d
transcript: "/Users/ll/.claude/projects/-Users-ll-Desktop-workspace-ascend-OpRunway/9f5c778e-cdf9-4c84-bb4f-f0ab8c49a99d.jsonl"
---

## [2026-07-24T01:35:32.000Z] session 9f5c778e — U7 泛化：正交契约 IR + Schema v1 落地；泛化优先定为最高原则

主题：接 U7「把用例生成/runner 泛化到所有算子形态」。本 session 从设计走到落地首步（版本化契约 Schema），并把「泛化优先」升为最高原则。本 checkpoint 记决策，多为 proposed / 待 review。

### 1. 🔴 最重磅决策 —— 泛化优先 · 绝不针对具体算子做优化/特判（本次 bureau:note 的主因）
- **用户 2026-07-24 明定为最高原则**，已写入 `CLAUDE.md` 最高优先级规则 **#0**（位列 #1 之前）。
- **内容**：OpRunway 是**通用算子验收工具**——一切设计与改进必须泛化：接口/算子名/目标目录/形状/dtype 一律从
  「任务书 × op_def × example」运行时**通用探测**；代码里**绝不**出现按算子名分支（`if op=="名"`）、**绝不**为某一类
  算子裁一套专属机制（把并集叫「通用」= 违规）。
- **关键区分**：具体算子只作**见证/测试输入**验证通用通路、绝不是优化目标（见证挑最压满结构轴的、不挑最简单的）；
  per-op 的 spec/IR/gap/目标机是通用工具消费的**数据**（核实数据不违规，为某算子改工具代码才违规）。
- **判据**：换任意域内算子工具零改即可跑，跑不了 = 通用性缺口、去补**通用机制**、绝不开特例。域外算子（稀疏库 API/
  有状态容器/无张量时序）命中即 **fail-closed 标「不支持的接口能力」**，绝不硬塞、绝不归某类 adapter。
- provenance：用户 2026-07-24 明示。**tier=proposed**，走 compile→review，不手改 cabinet。请人门把它 promote 成 canonical 架构/决策页。

### 2. U7 泛化设计全链（多轮 ultracode fanout + codex 审）
- 分类学 grounding → 现状勘察（**修正 2026-07-22「七道硬闸」快照部分 stale**：闸1 输出形状已由 C1 解耦、闸5 attr 已抬 list[int]、`run_on_npu.sh` `_math` 硬编码已删、`verify_mode=exact⇒bool` 已由 compare_dtype 修）→ codex review 逮 14 条结构性问题 → **正交契约重设计（9 个 IR 元素）** → 对最硬 4 见证（foreach_add_list/InplaceSigmoid/bincount/ArgMax）实测：**方向坐实但没零缺口站住**，5 缺口 2 关键（G1 shape_materialization 复合化、G2 constraint_graph zip+intra_list 边）已补。
- **纠正的重大 stale（须 bureau 更正）**：① `repos/ops-nn` **其实早已 clone**（285M 完整可读，「未 clone」是分类学 2026-07-22 stale、fanout 照搬没鲜核）；② taxonomy §5:145/:591「MinDim/MaxDim 仓内无实现·单源」错（arg_min/max_with_value 实现齐全）；③ 上条七道闸快照。
- **锚定铁证**：im2col aclIntArray 真序 `kernelSize→dilation→padding→stride`（stride 末位）≠ 派单；aclnn 参 `const` 不可信（foreach x1 是 `const aclTensorList*` 却被写）→ 方向唯一 ground truth = example 的 D2H 源 buffer。

### 3. 用户拍板（本 session）
- **F3 生成式 binding 架构批**：探测器→规范化 IR→唯一 codegen 模板→机械生成 per-entry 薄 binding→编译链接（模板/引擎零改、binding 机械生成、禁手写、无 op 名分支；编译期强制 ABI）。
- **目标机 a3/a5 两台**（逐算子由双源核定）。
- **先补 G3–G5 再真机四见证**。
- **300V Pro 优先级降低、往后放**（Cast&EmbeddingDenseGrad + SyncBatchNormGatherStats 两份挂起）。
- **旧「A→B 按类分期」作废**（承 #0 泛化优先——不按类一类类长，先一份通用契约打满结构包络）。

### 4. 落地首步（真代码进 plugin/）
- `plugin/acc-common/contract_ir/contract_ir.schema.v1.json` —— 版本化契约 IR Schema（draft 2020-12 合法）+ README + `examples/foreach_add_list.ir.json`（jsonschema 校验通过）。**G3/G4/G5 全补进 Schema**：G3 `dtype_selector` 三键源含 `platform_predicate`（承 a3/a5 SocVersion 分支）· G4 `acceptance_predicate` 跨输出引用 + 阈值 tier · G5 输入侧反向映射 + 被抑制参 + `allow_empty` 拆 list/element。

### 5. 开放线程
- 12 个 task 已建（见 session task 板）：#2 witness IRs · #3 prober · #4 codegen · #5 真机四见证 · #6 泛化验证 · #7 目标机冲突人核 · #8 inferred 算子 · #9 Foreach 语义 · #10 bureau 更正+promote · #11 commit 门 · #12 旧 follow-up。
- **未上真机**（covered≠真机绿）；prober/codegen 未建；本批改动未 commit（须先过 codex audit-fix 门）。
- 设计详见 `doc/oprunway-u7-generalization-design.md`（§3.5 契约 / §3.6 实测 / §3.7 Schema+G3–G5）+ 简表 2026-07-23/24 条。
