---
title: session 106cca26-c27e-4f76-8624-ee678b6aea61 · 2026-07-23
updated: 2026-07-23
status: logbook
session: 106cca26-c27e-4f76-8624-ee678b6aea61
transcript: "/Users/ll/.claude/projects/-Users-ll-Desktop-workspace-ascend-OpRunway/106cca26-c27e-4f76-8624-ee678b6aea61.jsonl"
---

## [2026-07-23T06:15:00Z] session 106cca26 — 批 6b 真机验收：Elu + Silu 在真 A5-950 NPU 端到端过精度

**Intent.** 承接 golden 去引擎化批次（工作分支 `fix/pdist-usertest-gaps`），把批 6b 单张量 elementwise 通路从「gen_cases 层通」推进到「真 NPU 端到端通」。用户目标：ultracode 完成必要 todo，尽量快、避免过度设计、快速验证。

**Decisions.**
- 批 6b 单张量 elementwise 通路真机成立，且**不只对某一种 runner 形态**：Elu 证「带 3 属性 runner」、Silu 证「零属性 runner + aclnn 派发到别名 kernel（Silu→Swish）」。此为 ADR 0011（golden 去引擎化，proposed）在真机上的正向证据 — 关联 dossier **golden decoupling**。
- 空张量用例判定：`bad_count=None` / `metrics={}` 是「无元素可比」的 vacuous 结果、**非精度失配**，不做特殊「空张量当 PASS」处理（避免重蹈已被用户叫停的 degenerate 特判）。
- 期3 D（多输入 runner 一般化：tensorlist / 多张量 / 独立形状）确认**非必要**（用户明示），放行 6 里剩 5 个（foreach_abs/acos、bce、rope×2）卡此、暂不推。

**Changes.**
- `doc/oprunway-changes-brief.md` (updated) — 07-23 节顶追加真机验收里程碑条目。
- `scratchpad/realverify_ops/{Elu,Silu,Sigmoid}/{golden.py,oprunway_*_runner.cpp}` (new, **不入库**) — 真机验收产物；Silu/Sigmoid 由 ultracode fanout（workflow `wf_389cde54-f8c`）趁 Elu 真机构建时并行备料。
- `scratchpad/{silu.spec.json,run_verify*.py,run_*.sh}` (new, **不入库**) — 真机 gen_cases + run_new_example 驱动脚本（脚本文件方式避开 `python -c` 引号坑）。
- a5 容器 `oprunway_elu_verify`（pta 镜像 `950-pta-triton-claude`，自带 torch/torch_npu）内：build elu/swish opp + 编 runner + 跑；mac 零操作。
- 无仓内代码改动 → 本次不涉及 commit。

**证据（真机实测）.**
- Elu（`--soc ascend950`，20 例）：**18/18 非空例 bad_count=0**（含 inf/-inf/nan/边界全1维/1024²网格/scalar），2 例空张量 vacuous。
- Silu（`--ops swish`，20 例）：**一次过没撞坑**，18/18 非空例 bad_count=0，2 空 vacuous。

**Open threads.**
- Sigmoid 已备（golden+runner+check_golden 退出码 0）未跑真机 — 单张量零属性、同 950，可续（一次 build+run）。
- 放行 6 里剩 5 个多输入算子的真机验收 = 期3 D，用户已定非必要、宜单独立项。
- 批 7（报告 + canon 收口）、ADR 0011 promote：卡 `bureau:review` 人门裁决，agent 不可越门。
- 本轮记账（changes-brief + logbook）与分支 `fix/pdist-usertest-gaps` 上已有的提交均**未 push**（等用户明示）；本轮真机验收本身无仓内代码改动。

**Source.** transcript `/Users/ll/.claude/projects/-Users-ll-Desktop-workspace-ascend-OpRunway/106cca26-c27e-4f76-8624-ee678b6aea61.jsonl`
