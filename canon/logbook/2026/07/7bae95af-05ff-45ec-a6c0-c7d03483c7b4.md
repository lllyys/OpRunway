---
title: session 7bae95af-05ff-45ec-a6c0-c7d03483c7b4 · 2026-07-09
updated: 2026-07-09
status: logbook
session: 7bae95af-05ff-45ec-a6c0-c7d03483c7b4
transcript: "/Users/ll/.claude/projects/-Users-ll-Desktop-workspace-ascend-OpRunway/7bae95af-05ff-45ec-a6c0-c7d03483c7b4"
---

## [2026-07-09T04:08:35Z] session 7bae95af — 完成所有 todo（每条 plan + codex 审）· Wave 1 落地 + Equal review 议程 capture

**Intent.** 用户设目标「完成所有 todo」+「每条 todo 制定 plan、每份 plan 过 codex audit」。通读全 canon（33 页 dossier + lint findings）grounding 后，为 11 条 todo 各产一份 codex 审过并修订的实施 plan（落 `doc/oprunway-todo-plans.md`）；经用户批准从 Wave 1（本地低风险）起做，P1 拆分口径定为「按原子能力拆 + 走 bureau 门更正 canon」。

**Decisions / 本 note 主体 —— Equal 翻案 canon 更正 review 议程（bureau 门 capture；低权威、非事实）.**
- 只读核验已过：4 张 Equal 页在库 `proposed`、`37223d6d` 在 `_compile-state.json` compiled、cabinet（architecture+decisions）grep 全关键词无未作废残留。compile 半程已由 minute 37223d6d（commit 419b5d4）落地，本会话不重推，只做「核验 + capture 议程 + 交人门」。
- **块 A · 4 张 Equal 更正页待 promote（proposed→canonical 或 hold；内容已 compile，agent 不手编 prose、只交人裁）**：
  1. `verify-spec-pr-correspondence-before-acceptance`【proposed·新建】→ 确认表述与 37223d6d 一致后 promote。
  2. `root-cause-decoupling-before-attribution`【proposed·改】→ 确认「解耦必要但不充分」+ Equal 动因案例已作废后 promote。
  3. `task-spec-authoritative-over-pr`【proposed·改】→ 确认 Equal 例已撤、前置「先确认对应」已加后 promote。
  4. `perf-baseline-by-reference-source`【proposed·Equal 项作废】→ **须先裁 `perf_baseline_source` 张力**（GPU 对比层可选/非必需是否成 canon）再升，否则 hold（前 3 页可按 checklist 直接 promote，本页不同等可升）。
- **块 B · 2 条 lint survivor（findings.md 已记，均「非功能 bug、只是误导性化石」）**：
  - Survivor#1（Superseded/medium）落 `0002-acceptance-grounded-in-catlass`【canonical】：括注「msTuner」→「msprof op」（ADR 实际决策不受影响，仅括注化石）。
  - Survivor#2（Drift/medium）落 5 页「1.2×」→ `target_ratio`：canonical 3 页（`acceptance-contract-evidence-chain`/`oprunway-acceptance-pipeline`/`generated-harness-responsibilities`）+ proposed 2 页（`0006-performance-timing-scope`/`0008-reuse-ascendoptest`）。
  - **可见性机制（精确）**：Survivor 里 3 张 canonical 页不会因本 note 自动进 review 视图；ADR0006/0008 虽已 proposed（本在 pending review），但其 drift 修法不会被单独标亮。要真显眼须走 tool 车道之一：`bureau:lint --apply`（改 status/结构标记，消耗性）或 `bureau:compile`（产 proposed 修订、仍待 review）。**默认不自动跑、待用户批**（改 canonical status 消耗性，CLAUDE.md #3）。
  - **review 操作护栏（建议，非 canon 硬规则）**：ADR0006/0008 若未同步处理「1.2×→target_ratio」，不宜单独 promote，以免固化 drift；5 页宜一次处置。
- **交人门 bureau:review 的事项**（agent 只走 tool 车道产 proposed、不手升 canonical、不手编 cabinet prose）：promote/hold 块 A 4 页（perf-baseline 连带裁张力）；块 B 2 survivor 先由 tool 车道送进 review 视图（待用户批）再由 review 促进。

**Changes（Wave 1，详见 `doc/oprunway-changes-brief.md`）.**
- **T1**：修 4 处文档一致性漂移（op-acceptance 补硬门节镜像 AGENTS.md、acc-runner 与 op-acceptance「验证-才-信」从「硬门」改回「纪律·sidecar 待补」、acc-casegen 补诚实 SKILL.md、rule-catalog 悬挂链接）。
- **T2 · P1 编排升级**：`op-acceptance` 改薄为 `mode:primary` 编排器 + 3 单轮 subagent（acc-spec-extractor/acc-runner-dev/acc-verify-rootcause，带 dispatch_mode、不自行判定）+ `acceptance-workflow` skill（CP-A..E 状态机 + correspondence.json 对应校验前置）+ `check_agent_frontmatter.py` meta-lint；AGENTS.md/plugin.json 同步 4 agent/3 skill。过 codex 代码门（HIGH：agents set 判 bug 已修）+ codex 散文门（HIGH：command CP-A primary NL durable 越界已修）+ 机器门全绿（SYNCED/frontmatter PASS/单测/mock）。
- **T10**：本 note（review 议程 capture）；changes-brief 加 capture 条 + 对账 T11b「待批（外发）」stale 子句为如实状态（push 已完成、PR#2 body 待在线复核）。议程与 changes-brief 文本均过 codex 散文门。

**Open threads.**
- 待人门 `bureau:review`：promote 块 A 4 页 + 处置块 B 2 survivor（见上）。
- open decision：是否跑 `bureau:lint --apply` 让 survivors 进 review 视图（默认不跑、待用户批）。
- T11 外发（公开台账 push 已完成、PR#2 body 在线复核）卡 infra（隧道 down、gh token 失效）+ 用户授权。
- Wave 2+ 未开工：T5/T6/T7/T8（精度双标准/性能小 shape/dtype 扩面/GPU consumer，本地逻辑+mock）、T3/T4（P2 原子化 + catlass adapter，真跑待 950）、T9（发布形态定稿，需用户决策）。

## [2026-07-09T07:29:08Z] checkpoint — Wave 2 落地 + 对抗式门坐实假通过 + T9 发布形态拍板（proposed）

**Intent.** 并行推进 Wave 2/3（T5 精度 / T6+T8 性能 / T7 dtype / T3 P2 / T4 catlass），每批过 codex 代码门与散文门；用户要求 fan-out 并行。

**Decisions / 本 checkpoint 主体 —— T9 发布形态定稿（用户 2026-07-09 逐条拍板；status: proposed，待 `bureau:review` promote，本条为低权威 logbook capture、非事实）.**
> 术语澄清：下文「插件仓」指 **OpRunway 主仓内 `plugin/` 目录作为自维护插件发行单元**，不另建独立 repo。
1. **仓位置——保持现状**：插件继续作为 OpRunway 主仓 `plugin/` 子目录，不拆独立 repo；远端维持 GitHub `lllyys/OpRunway` + GitCode `brian66237/OpRunway` 双镜像。
2. **插件名——保持现状**：`.claude-plugin/plugin.json` 的 `name` 维持 `oprunway`。
3. **skills 向 `awesome-ascend-skills` 的 external-sync —— 「很久以后」**。⚠ 出处更正（codex 散文门查出）：`ADR 0003`【canonical】原文**并无**「接口稳定前不 external-sync」，其 Consequences 只把「是否即刻登记同步」列为**待定**；该约束句实出自 `doc/oprunway-changes-brief.md` 与 2026-06 logbook minute（**低权威**）。故用户本次「很久以后」是**首次对此事作出有约束力的决定**，而非重述既有 canonical 约束。**约束范围（此刻具约束力）**：不得修改 `external-sources`、不得提交/创建相关 PR、不得 push、不得运行任何 sync 脚本；本轮只允许记录 proposed note / changes-brief。
4. **`init.sh` 跨 CLI 扇出——保留**，作为 `/plugin install` 之外的第二条分发路径。⚠ **现状更正（codex 查出）**：`plugin/init.sh` **尚未在主线**，仅存在于 T3-P2 的**未合并 worktree 分支**；本决定是「保留该路线、待合并落地」，不是「保留一条既有路径」。

**「很久以后」的判据（agent 建议，未经用户批准、不作事实）**：① 对抗式代码门坐实的假通过路径全部堵死（含 evidence↔产物 provenance 绑定）；② 至少一个算子在**真 NPU** 上端到端验收通过（当前判定逻辑均只有本地 + mock 绿）；③ `bureau:review` 把相关 canon 页 promote 到 `canonical`。

**与现状的一致性（诚实记录）.**
- 主线 `plugin/skills/` 现仅有 `acc-casegen`（草案、未接入 live 流、未登记 `AGENTS.md`）；`acc-precision` / `acc-perf` / `acc-rootcause` 在 T3-P2 未合并分支。决定 3「很久以后」与该现状自洽——现在同步出去会发布一批未接线的 skill。
- `init.sh` 的扇出遍历 `skills/` 全部目录、不读 `AGENTS.md` 的 `skills:` 列表，故「不登记」不影响决定 4 的分发能力（T3 已在其分支 fixture 实测）。其 opencode/trae/cursor/copilot 分支为自动干跑/静态审、**未在目标 CLI 真机验证**，如实记录、不声称已验证。

**Decisions（工程）.**
- **对抗式代码门坐实验收工具自身可被绕过**：三组 exploit 实跑成功（带反事实对照）。核心结构性缺陷 = **validator 用 caseset 自声明字段决定「怎么判」**。T5 曾据 spec 复算 policy 夺回权威；T7 引入 per-case `effective_standard` 后，选哪套标准取决于 caseset 的 `compare_dtype` → 权威复失。已复现：谎报 `compare_dtype` 放宽阈值 / 绕过整型强制 EXACT；`spec` 未 pin 时注入 `acceptance_policy` 攻破放行层；`dims=[]` 使坏点从不被裁；伪造 `storage_dtype` 令值 cast 污染送真机的 `x1.bin`。另坐实 `op` 身份三处无锚点。**修复原则**：凡决定「怎么判」者一律从 `spec` 派生，caseset/evidence 声明只作待核对断言。
- **evidence↔产物无 provenance 绑定（架构级）**：validator 全信自报 metrics，与 `out.bin`/`golden.npy` 无 hash 绑定 → 伪造 `bad_count=0` 即 pass。**用户选定方案 A**：产物落盘 + evidence 记 sha256 + 门内用 `precision_policy.compute_metrics` 重算比对。硬纪律：numpy 缺失或产物缺失一律 FAILED，mock 不放宽。**诚实边界**：A 只证「metrics 由这两个文件算出」，**不证「文件来自真 NPU 跑测」**（后者需绑 `OPRUNWAY_DONE` 哨兵 / raw log hash / msprof 输出），此边界须写入 canon 与 docstring，不假装已防。
- gate_task3 坐实 7 条绕过并已修实（零真实性能证据放行 / wait 态豁免 NPU 证据 / 「有图强制」空心——现改为门内据 `simulation` 确定性重算 SVG 比对字节）。

**Changes.** 主线已提交 6 个 commit：Wave 1 → T5 精度包 → T6+T8 性能包合并 → T7 dtype/attr → gate_task3 修复。未合并分支：perf-fix（修 20 条 CONFIRMED）、T3-P2、T4-catlass。

**Open threads.**
- 待人门 `bureau:review`：promote 块 A 4 页 + 处置块 B 2 survivor + 本 T9 决定 + 门职责扩展（「重算比对」属证据可信、非重判 verdict）。
- T4 偏离 canonical：未走 `catlass-to-aclnn-bridge`【canonical】的路线 A/B，自选「复用 catlass example + staging 换入 bin-IO runner」第三路径，需人裁。
- T4 分支基点为 `4dcd355`（不含 T5），其 catlass evidence 仍旧口径，合并后必被新 validator 判契约 fail，需适配。
- 真机验证（NPU/VPN）、真 GPU 基线数据、T11a 外发授权与 infra（隧道 down、gh token 失效）均未解。
