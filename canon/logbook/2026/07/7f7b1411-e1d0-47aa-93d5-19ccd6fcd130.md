---
title: session 7f7b1411-e1d0-47aa-93d5-19ccd6fcd130 · 2026-07-08
updated: 2026-07-08
status: logbook
session: 7f7b1411-e1d0-47aa-93d5-19ccd6fcd130
transcript: "/Users/ll/.claude/projects/-Users-ll-Desktop-workspace-ascend-OpRunway/7f7b1411-e1d0-47aa-93d5-19ccd6fcd130"
---

## [2026-07-08] session 7f7b1411 — Equal A3 验收失败解耦定论（op 真阳性、runner 清白）+ 拉 ops-math + bureau inspect/lint

**Intent.** 从「看当前 TODO」起步，实际展开为：拉齐缺的算子仓、跑 bureau inspect/lint，并把最紧的 Equal 真机失败**逐级解耦、正确归因**（纠正前一版把锅安在自己 runner 上的误判），产本地 bug 报告；附带把 `spec/` 移出 git 跟踪。

**Decisions.**
- **Equal 归因翻案：技术上是被测 PR（ops-math!2890）在 A3 上的真阳性、OpRunway harness/runner 清白** —— 靠真机解耦坐实（同一 runner 换内置 TBE Equal→输出正确、失配 0/N；换 custom PR→全 0）。推翻本会话之前「全 0=harness/我们 runner 的问题」的结论；那次误判源于把「内置 TBE 的混合输出」当成了「custom op 自己 build 的输出」。归因链经历 op-bug→harness-bug→op-bug(实锤) 的反复。— implies dossier **Root-cause decoupling before attribution（换内置 op 对照 + 跑被测自带 example + 查已编 vendor 包 + dtype 逐个 op 级测）**
- **平台/dtype 口径从任务书推、不猜；A3 是 Equal 的硬要求（四重锚定）** —— 任务书 `Equal_task_doc.md` 写「适配 A2/A3、支持原 TBE 所有 dtype」（非模板：同批 SlidingTileAttention 单写 A2）；作者 `tasklist/04-7-Equal/.../design.md` 自写「目标 A2/A3 √」；Sign/IsClose 同要求已注册并交付 A3；内置 TBE 在 A3 上 fp32/fp16 正常。— implies dossier **task-spec authoritative over PR**
- **两层缺陷，且「一行修好」被证伪** —— ① `equal_def.cpp` 漏 `AddConfig("ascend910_93")` → build（`gen_ops_info.cmake` grep 该字符串）静默丢 A3 kernel（`[INFO] not supported` 不报错；vendor `config/ascend910_93/` 空）→ device 无 kernel、输出停零初始化 → 全 0、aclnn 却返 ACL_SUCCESS；② 本地补上该行重编后 double 通、但 fp32/fp16 仍 `GetWorkspaceSize=561103`。教训：源码「一行诊断」须经真机重编坐实范围。— implies dossier **generated_harness responsibilities**
- **验收职责边界：定性缺陷即可，不替 PR 作者修算子** —— 561103 更深根因不深挖。
- **技术判定 vs 官方验收口径分离** —— 「真阳性」是实测技术判定；「是否算官方验收通过」是程序问题、待用户向组织方确认，结论留口、暂缓外发/上报。
- **bureau:lint 语义扫描：2 个 medium survivor** —— ADR 0002 仍称 msTuner 为 catlass 性能后端（已被次日 catlass-acceptance-mechanics 改为 msprof op，superseded）；perf 门比值「1.2×」死值 vs 机制/spec 的 `target_ratio`（drift）。— implies dossiers **catlass acceptance mechanics** / **Performance baseline follows the reference source**
- **仓与素材归置** —— `ops-math` 是唯一缺的仓，clone `--depth 1`；`spec/`（任务书素材）移出 git 跟踪。

**Changes.**
- `repos/ops-math/` (new, `git clone --depth 1`) — 缺的唯一仓，为 Equal root-cause 提供源码/example/build。
- `repos/ops-math/experimental/math/equal/op_host/equal_def.cpp` (updated, 本地 + a3 clone 均 patch) — 加 `AddConfig("ascend910_93")` 做去风险验证；**未 push、纯本地/真机实验**。
- `doc/equal-a3-defect-report.md` (new) — 本地 bug 报告（含 PR/任务书链接 + 作者 design.md 铁证 + 验收口径待确认注脚），**未上报**。
- `doc/oprunway-todo.md` (updated) — Equal 归因翻案（真阳性/两层缺陷）+ 待办收窄（不深挖 561103）+ ⏳验收口径待确认。
- `doc/oprunway-acceptance-evidence.md` (updated) — 台账 Equal 表格行 + 解释段：旧「归因指向我们 runner」→ 真阳性·A3 未达标 + runner 清白 + ⏳口径待确认。
- `doc/oprunway-changes-brief.md` (updated) — 追当日首条，记本次纠正。
- `canon/lint/findings.md` (new) — bureau:lint 报告（2 survivor / 2 discarded，report-only）。
- `.gitignore` (updated) + `spec/` (git untracked via `rm --cached`；文件保留)。
- `gazette/` (rebuilt) — bureau:inspect 渲染（37 dossier、结构 health 全绿）。

**Open threads.**
- ⏳ **PR #2890 是否算官方验收通过** —— 用户正在向组织方询问；决定程序性结论与是否上报。
- **上报 ops-math** —— 上报稿备好、**未发**，待批准 + 口径结果。
- **codex 散文门未过** —— Equal 翻案文本经用户「改过来」直接落地，未经 codex verify；可补跑。
- **bureau:lint 2 survivor 待人裁** —— 改 ADR 0002（msTuner→msprof op）、1.2×→target_ratio 全局统一；`bureau:lint --apply` 未跑。
- **a3/本地 clone 的 equal_def.cpp 仍留 patch** —— 作实验记录，未还原 pristine。

**Source.** transcript `/Users/ll/.claude/projects/-Users-ll-Desktop-workspace-ascend-OpRunway/7f7b1411-e1d0-47aa-93d5-19ccd6fcd130`
