---
title: session 019fa119-cf94-7993-bd18-dae28be83cf8 · 2026-07-27
updated: 2026-07-27
status: logbook
session: 019fa119-cf94-7993-bd18-dae28be83cf8
transcript: ""
---

## [2026-07-27T06:32:17Z] session 019fa119 — Median 性能用例规则与真机优化

**Intent.** 按任务书直接完成 Median PR6429 的性能验收：性能 case 从精度 caseset 选择，A3 以全部输入物理字节 256 KiB 区分小/大 shape，并消除真实性能劣化。

**Decisions.**
- 性能用例只能从已经通过精度裁决的精度用例中选取；同一 DUT、同一输入不应在性能阶段重新出现功能或精度执行失败。性能 ratio 不达标与 baseline 或 profiler 证据缺失仍分别按性能 FAIL 或 BLOCKED 处理 — implies dossier Performance cases are precision-pass subsets
- Median 任务的小算子拼接版本按用户确认等价于 Torch 对应接口，性能 baseline 使用同机 torch_npu 的 torch.median，不追加包装等价性证明 — implies dossier Performance baseline by task requirement
- A3 小 shape 以全部输入物理字节之和不超过 256 KiB 判定，大于该值为大 shape；分类只用于报告，不免测、不改变 ratio 阈值 — implies dossier Performance shape classification
- profiler 偶发失败允许相同口径定向重采，但只能用有效双边记录填补主采集中的无效项，禁止覆盖已有有效数据或择优挑数 — implies dossier Performance retry merge discipline

**Changes.**
- A3 隔离 PR6429 源码检查点 4fbaa74f7 (new) — global value-only 跳过 index 计算并优化小 shape 与大 FP 路径
- A3 隔离 PR6429 源码检查点 e215fa176 (new) — INT64 全局大 shape 改为多核精确整数二分
- plugin/acc-common/aclnn_runtime/perf_msprof.py (updated) — torch baseline 增加 spec 驱动的 keyword_groups 变体绑定
- plugin/acc-common/perf_retry_merge.py (new) — 严格合并同口径子集重采并记录输入哈希
- reports/Median-perf-rule-20260726-v2 (updated) — 精度 60/60 PASS；48 个可评分性能 case 全部达标

**Open threads.**
- 当前 torch_npu 2.10.0 与 CANN 9.0.1 的全局 BF16 aclnnMedian 对两个小 shape 返回 161001，缺 baseline 耗时；正式性能裁决仍为 48/48 达标、2 blocked。
- 若要消除最后两个 blocked，需要获得升级或替换隔离 baseline 环境的明确授权；不能通过换 dtype、伪造耗时或复用 DUT 实现绕过。

**Source.** transcript ``
