---
title: session d31ea446-dec3-479f-a7b3-d6c1dec4f611 · 2026-07-02
updated: 2026-07-06
status: logbook
session: d31ea446-dec3-479f-a7b3-d6c1dec4f611
transcript: "/Users/ll/.claude/projects/-Users-ll-Desktop-workspace-ascend-OpRunway/d31ea446-dec3-479f-a7b3-d6c1dec4f611.jsonl"
---

## [2026-07-02T07:33:40.000Z] session d31ea446 — AscendOpTest 桥真机验证：方案研究 + Codex 审计 + baseline 更正

主题：把 canon 里 proposed 的「catlass→aclnn 桥」第一次在真机去风险（桥目前**零代码**，仅设计）。本 checkpoint 记三件事，均**未落地、未拍板**。

### 1. 用户更正 — acceptance-contract 的性能「标杆」不必然是 GPU（本次 bureau:note 的主因）
- **对象**：`canon/architecture/acceptance-contract-evidence-chain.md` 里「GPU 仅作性能标杆、非精度 oracle」这句措辞。
- **更正**：性能验收基线要拆成两个角色——
  1. **性能验收基线（1.2× 的分母）**：默认 = **NPU 侧 torch 未融合小算子拼接链**（`torch.layer_norm + npu_grouped_matmul + bias add + silu`，跑 torch_npu），由 generated_harness 自测。与 ADR 0006 及 `generated_harness responsibilities` 页已有口径一致。
  2. **GPU 标杆**：Task 3 的对比标准（外部给数据、出 NPU↔GPU 对比报告），**非** Task 2 验收门槛分母。
- **建议落地**：给统一契约加枚举 `perf_baseline_source ∈ {npu_torch_unfused_chain(默认) | task_spec_target | npu_existing_op | gpu_external}`，与精度侧 `oracle_source` 对称；强约束「分母与分子同 timing_scope」。acceptance-contract 页那句应改写、不把 GPU 当默认性能基线。
- **影响**：收窄真机方案开放项②（1.2× 分母口径）——分母默认即 NPU torch 未融合链，不依赖 GPU。
- provenance：用户 2026-07-02 口头更正 + 我的归位。tier=proposed，走 compile/review，不手改 cabinet。

### 2. Codex(gpt-5.5) 对抗审计「catlass-to-aclnn-bridge」（只读，回源码 file:line）
- 断言判定：框架硬绑 aclnn 两段式 API=**真**；「只接受 custom aclnn 算子」=**过强**（也支持 built-in `opapi` 链）；catlass 裸 kernel 不能原生直测=**真**。
- 🔴 阻断 1：**路线 A 默认写共享 CANN `opp/vendors/`** → 违反共享机铁律。须走用户态 OPP（`--op-path $USER_OPP/op_api` + 用户 set_env + LD_LIBRARY_PATH）。
- 🔴 阻断 2：**路线 B 无「编译自造 exe」的闭环** → exe 不存在框架就触发 aclnngen；generated_harness 必须自带 CMakeLists、自编、把产物放进 `aclnn_dir/build/<exe>`、全程禁 `--build`。
- 🟠 exe 名解析脆弱（`run_test.py:298-299` 只取 `line.split("(")[1]`）→ CMakeLists 第一条 `add_executable` 须复刻生成器换行格式。
- 🟠 perf `-k` 命中裸模板符号 `KernelAdapter<GemmKernel>` 不可预设 → 先 msprof 实测符号，或用 `extern "C"` 包装。
- 🟠 路线 A 工作量远超「group+动态 M」（LN/grouped mm/bias/SiLU 全套 op_host+op_kernel 注册）。
- 🟡 `run_test.py:477-482` 会自动 `pip install` aclnngen（共享机副作用），应预装用户 venv。
- 表述性纠错：输出路径是 `output/<output_desc.name>.bin` 非 `output.bin`。
- 修订建议已就绪，待并入 bridge 页（走 gate）。

### 3. 重磅发现 — catlass 自带完整 ACLNN 桥参考（已亲核）
`repos/catlass/examples/advanced/basic_matmul_aclnn/`：`catlass_basic_matmul.json`（`"op":"CatlassBasicMatmul"`）+ `op_host/*.cpp/_tiling.h` + `op_kernel/catlass_basic_matmul.cpp`（**有 `extern "C" __global__ __aicore__` 包装**转调模板 `CatlassBasicMatmulTemplate`）+ `basic_matmul_aclnn.cpp` + CMake。
- 含义：路线 A 对 basic matmul **非 greenfield**（catlass 给了现成范式，aclnn 符号即 `aclnnCatlassBasicMatmul`）；`extern "C"` 包装恰好解掉 perf `-k` 命中风险。

### 4. 真机验证方案草案（workflow 产出，未拍板）
- 命题分两层：**本轮只证机制**（AscendOpTest 能驱动一个 catlass kernel 走完 data_gen→自造 exe→compare 精度闭环 + msprof kernel-only perf），**不碰目标算子**。
- 目标算子 **LayerNormGroupedMatmulBiasSilu 的 catlass kernel 仓里不存在**（grep 无命中）→ 须由算子 PR 提供，第二层推迟到 PR 到位。
- 推荐：**路线 B 先行**（不写共享 vendor）；载体 **`43_ascend950_basic_matmul`**（arch 3510，固定 m==n 钉单符号）；机器 **ascend-a5**（真 950，arch 匹配是刚需）。
- 真机就绪度（只读实测）：a5 reachable、msprof 在、**缺 numpy**（需 `pip3 install --user numpy`）、catlass 无 `.git`；a3 reachable、conda `cann-ops` 有 numpy 1.26.4、catlass 预编在位。
- 4 决策点待用户拍板：机器(a5/a3)、载体、范围(精度-only / +perf)、路线深度(B-only / B后接A)。推荐 a5 / 43 / 精度+perf / B-only。
- 前置缺口（诚实标注）：AscendOpTest 的 gitcode org/URL 未确认；a5 缺 numpy；catlass 无 .git 无法版本对账；catlass 是否暴露标准 aclnn 符号未知（只影响路线 A）；43 真机 demangled 符号未实测；exe 的 CWD 对齐未实证。

**开放线程**：① 用户对真机方案 4 决策点拍板；② 上述审计修订 + baseline 更正待 compile/review 并入 canon；③ 目标算子 PR 未到（第二层验收前置）。

## [2026-07-02T08:10:54.000Z] session d31ea446 — canon 内部冲突：ADR 0008 的「optest 性能只有 kernel-only」与 ADR 0006 打架

**决策/进展**：用户选定真机验证 = **a5 + 43_basic_matmul + 精度闭环+perf + 路线 B only**；依赖用**用户态 miniconda**（用户指示「没有 conda 就装一个」；a5 无 conda 但直连 pypi 200）。a5 装 miniconda 踩两坑后走通路径：默认渠道要接受商业 ToS → 改 **conda-forge `--override-channels`**；conda-forge python 不带 pip → 补 `conda install pip` 再 `pip install numpy ml_dtypes build`。路线 B 桥制品已全部落 `plugin/bridge/route_b/`，本地 DRY_RUN 通过（build/stage/precision/perf/perf_e2e）。

**冲突（本 checkpoint 主因）**：回源码复核「optest 能否测 e2e」时发现 **canon 内部不一致**——
- `run_test.py:335` `--msprof --op` → `msprof op`（kernel-only，OpBasicInfo.csv）；`run_test.py:341` `--msprof`（**不带 --op**）→ `msprof --application`（**e2e，含 H2D/D2H**）。`scripts/get_prof.py:63-72` 只解析 OpBasicInfo.csv（kernel-only）、不解析 application 产物，且只并排 custom+built-in、不算 speedup。`run_test.py` 跑完 msprof **不解析任何性能数**（直接 compare）。
- **ADR 0006（proposed）** 已含「更正(2026-07-02)：optest 并非不能测 e2e……device_e2e 由我们驱动 --application + 自解析」→ **正确**。
- **ADR 0008（proposed）** 仍写「性能只有 kernel-only 单口径」「只用它的 kernel-only 采集」→ **与 0006 矛盾**。根因：07-01 的 e2e 更正只落进 0006 + `generated_harness responsibilities`(职责#4)，**漏改 0008**；且 0008 Context 称该结论「经对抗验证」——当初对抗验证**漏看了 `--application` 分支**（失误记录在案）。
- 无 canonical 页受影响：`catlass acceptance mechanics`(canonical) 讲 catlass 自身 perf=msprof op kernel-only（默认口径），非 optest 能力，不冲突。

**修正建议（走 compile/review，勿手改 cabinet）**：改 ADR 0008 与 0006 对齐——
- Context：「性能采集**有两口径**：kernel-only(`msprof op`) 与 e2e(`msprof --application`,含 H2D/D2H)；但自带解析 `get_prof.py` 只认 kernel-only、不算 speedup、无 torch 通路」。
- Decision·性能：「**复用其采集**(kernel-only→msprof op；device-e2e→msprof --application)；**e2e 解析/裁窗 + 1.2× 基线(torch 未融合链) + 比值** 由 generated_harness + validator」。scope 选择归 ADR 0006（默认 kernel-only，融合算子可 device-e2e，两侧必同 scope）。
- 制品侧已对齐：`run_derisk.sh` 补 `perf_e2e`（`--msprof` 不带 --op）验 optest 的 e2e 采集通路。

**开放线程（累积）**：① 真机 deploy→build→precision→perf/perf_e2e 待跑（等 conda env 收尾 + 用户 go）；② 待 compile/review：Codex 审计修订 + baseline 更正(perf_baseline_source 枚举) + **本条 ADR 0008↔0006 对齐**；③ 目标算子 PR 未到。

## [2026-07-02T08:15:47.000Z] session d31ea446 — 顶层两页现状评估：pipeline 一处措辞跟改，component-breakdown 不动

对现状复核 `OpRunway acceptance pipeline` 与 `OpRunway component breakdown`（均 proposed）是否需改：

- **`OpRunway acceptance pipeline`**：三段式/脊柱/catlass 打底不变（本轮工作是印证 Task 2 可落地，非改流水线）。Task 2「跑出精度+性能」不变——「怎么跑」（optest 桥、kernel-only+e2e）是 ADR 0008 / catlass mechanics 的细节，不在此页高度。**唯一要动**：句「GPU 仅作性能标杆，不是 Task 2 的精度 oracle」与 acceptance-contract 那句**同源同病**，应按 baseline 更正顺成「GPU = Task 3 对比标准；Task 2 的 1.2× 验收基线 = NPU torch 未融合链」。→ **把 baseline 更正的适用页从 acceptance-contract 扩到本页**（review 一并改）。
- **`OpRunway component breakdown`**：**无需改**。1 workflow + acc-common + 3 skill + 子 agent + acc-npu-run(仓适配器/判定器) 结构全 hold；本轮建的路线 B 桥 = acc-npu-run 仓适配器→generated_harness→aclnn 桥的**实现**（已被 repo-adapter / generated_harness responsibilities / catlass-to-aclnn-bridge 命名），是落地/印证非改结构。「acc-npu-run 包 optest 做精度+perf(kernel-only+e2e)、validator 算比值」属 ADR 0008（正在修），不落此页。真机跑通后信心可 proposed→verified，文字不变。

**结论**：本轮实质变化都沉在更低层（acceptance-contract / ADR 0006 / 0008 / catlass-to-aclnn-bridge）；两张顶层页几乎不动，仅 pipeline 那句 GPU 措辞随 baseline 更正一起顺——改动落在对的高度，未冲击顶层架构。

## [2026-07-02T08:32:54.000Z] session d31ea446 — 新流程规则：Codex audit-fix 双门（拟入 ADR 0010）

用户定标准流程规则并已落 `CLAUDE.md` 最高优先级规则 #5：**每次 bureau 新增/变更之前、每次 md/代码生成之后，必须过一次 Codex audit-fix**。分工经澄清（用户追问「走哪个」+ 实际加载 cc-suite 后据实修正）：**代码/脚本 → `cc-suite:audit-fix`（9 维代码审→修→验循环）；散文（CLAUDE.md/bureau 决策/设计 md）→ 底层 `mcp__plugin_nlpm_codex-cli__codex` 定制审**（cc-suite 是 per-file 代码审计、套不上散文且过重）。`nlpm`（check/score/fix）**非本门**——NL 制品确定性 lint，另线，仅用于打磨已发布 skills。已存 auto-memory `codex-audit-fix-gate.md`（指针，权威在 CLAUDE.md #5）。本门已自我应用：CLAUDE.md #5 与下述 ADR 文本均经 Codex 审过（散文门）。

**拟写入 canon 的 ADR 0010（已过 bureau 写入前的门 = Codex 审校；待 compile→review 提升，勿手改 cabinet）**：

> # ADR 0010 — Codex audit-fix double gate
>
> Context: OpRunway 会产出三类高代价制品：bureau canon/logbook（会被当作事实）、上真机的代码（本地无法完整编译验证）、md 文档。为降低错误落盘和错误交付的成本，需要独立第二双眼在关键点拦截。
>
> Decision: 采纳 Codex audit-fix 双门：(1) bureau 新增/变更之前，审「拟写入文本」，确认后才写入 / compile / review；(2) md/代码生成之后，审+修产物，再交付、引用或记录改动。分工：代码/脚本 → cc-suite:audit-fix（9 维代码审→修→验循环）；散文（CLAUDE.md 规则 / bureau 决策文本 / 设计 md）→ 底层 `mcp__plugin_nlpm_codex-cli__codex` 定制审。nlpm 非本门：`nlpm:check/score/fix` 是 NL 制品确定性 lint，另线，仅用于打磨已发布 skills。执行点 = CLAUDE.md 最高优先级规则 #5。
>
> Consequences: 每次变更多一道门，可按逻辑制品批量执行。已有一次具体收益：发现 `aclnn_op/CMakeLists.txt` 注释里的 `add_executable` 串会被 `run_test.py:get_exe_name()` 误抠的真实 bug。递归：关于本门自身的 bureau 写入也须过本门，即先审拟写文本，不先落盘。关联 [[ADR 0001 — Adopt bureau]]（capture → compile → review 写门）。
>
> Provenance: session d31ea446 · 用户指示 2026-07-02。

**开放线程（累积）**：① 真机 deploy→build→precision→perf/perf_e2e 待跑（等 conda env 收尾 + 用户 go）；② 待 compile/review：Codex 审计修订 + baseline 更正(perf_baseline_source 枚举) + ADR 0008↔0006 对齐 + pipeline GPU 措辞 + **本条 ADR 0010（新决策，compile 时建 decisions/0010）**；③ 目标算子 PR 未到。

## [2026-07-06T08:13:32.000Z] session d31ea446 — 社区竞赛任务书↔PR 全量对应（第一步）+ 散文门改走 codex CLI + review 收尾

本段跨到 2026-07-06，三块：① review 收尾 + baseline 定论（含对 07-02 note 的反转）；② 散文门工具从 MCP 改 codex CLI；③ 新工作——把社区竞赛任务书和真实 PR 全量对应上，为设计工作流备真数据。

### 1. review 收尾 + baseline **反转**（相对 07-02 checkpoint）
- **baseline 定论并反转了 07-02 note**：性能默认基线 = **`gpu_external`（GPU 标杆，走 Task 3）**；可选 `npu_torch_unfused_chain` / `task_spec_target` / `npu_existing_op`；1.2× 分母 = `perf_baseline_source`。07-02 note 曾写「默认 = NPU torch 未融合链」，用户两次更正（「gpu 参与性能验收，但不必然有 gpu」「默认看 gpu，也可看 torch 小算子拼接」）后改为 **GPU 默认**——此为**反转**，旧 note 不改、以此条为准。已落 acceptance-contract / acceptance-pipeline / ADR 0006 / 0008。
- 审定为 canonical：repo-adapter、catlass-to-aclnn-bridge（加白话 + vendor=自定义算子标准包澄清 + 路线 A/B 白话，路线 A 共享机走用户态 OPP）、ADR 0007/0009/0010、component-breakdown、pipeline、rule-library、task3-state-machine、precision-thresholds 等——**canonical 共 19 篇**。**剩 ADR 0006、ADR 0008 待审**。

### 2. 散文门：MCP → `codex exec` CLI（provenance 更正）
- 查明 **nlpm 1.1.0 用 `.mcp.json` 起 codex MCP（`mcp__plugin_nlpm_codex-cli__codex` = `codex mcp-server`），nlpm 1.1.1/1.1.2 已删除该 `.mcp.json`** → 新版不再提供该 MCP。故散文门改为**底层 `codex exec`（Codex CLI）**。
- 改三处引用：CLAUDE.md #5、ADR 0010、auto-memory `codex-audit-fix-gate`。ADR 0010 因内容变更 canonical→proposed→（散文门过）→重新 canonical。**实测 `codex exec --skip-git-repo-check -C <dir> -s read-only` 散文门可用**，不依赖 MCP（codex 二进制健康即可）。⚠ codex 沙箱 read-only **禁网**，故 codex 内部无法 curl gitcode（我方/agent 的 Bash 无此限制）。

### 3. 社区竞赛任务书 ↔ PR 全量对应（第一步 · `doc/oprunway-task-pr-map.md`）
- clone `cann/cann-ops-competitions`；任务书按月放 `04_tasks/01_community-task-2026/docs/{202604,202605,202607}`，**7 月前 = 41 份任务书**（8 仓：ops-math 18 / ops-nn 16 / 其余 6 仓各 1-2）。任务书格式即 M1 缺的那套（开源仓 + 参数表 dtype/format/shape/非连续 + 验收标准）。
- **匹配方法定论**：纯脚本按标题匹配**不可靠**（算子名是常见子串、真开发 PR 淹没在每仓数百 PR、标题格式五花八门；脚本仅 7~19/41 且多误配）→ 改为 **3 个 agent 逐仓 file-level 匹配**（`/pulls/<n>/files` 核改动落在算子目录 + 社区任务 issue 号）。结果 **34 找到 / 7 未找到**。
- 收口核验：codex 复审（查出计数不自洽、`</content>` 残留、未找到清单错放 Gcd、过度概括）→ 已修复；再用 **gitcode API 全量重核 36 个主开发 PR 状态**，修出 2 个 agent 报错状态（ops-cv #1012 merged→closed、ops-blas #243 merged→open）。状态列现已 API 核过。
- **关键发现（= 第二步设计工作流的输入）**：(a) 社区任务多是「给已有算子加 dtype」(int8/uint8/int16/bf16)；(b) **一任务对多 PR 是常态**（设计文档 + 主开发 + 后续修复 + 多人并行提交）；(c) 源码 aclnn 原生示例仓（experimental/<类>/<算子>/…），**new_example 模式、不用桥**（与 catlass 模板库两码事）；(d) **主开发 PR 多为 open**（验收发生在 open PR，不能只认 merged）；(e) 有「未开发/复用已有/仅设计」的空任务，工作流须识别跳过。

**开放线程（更新）**：① **第二步「据此设计工作流」待启动**——用真实 41 任务书+PR 校准 acceptance-contract 字段 / rule-catalog 覆盖 / repo-adapter 模式 / 精度性能口径；② 剩 **ADR 0006、ADR 0008 待 review approve**；③ 真机 deploy→build→precision→perf 待跑（a5 conda numpy 卡下载，回退清华 pip 镜像）；④ **ops-cv upsample uint8（PR #1012/#1048/#1070）是候选首个真实验收目标**（aclnn 原生、不用桥、a3 有 numpy、issue #561 = 任务书）。

## [2026-07-06T09:18:25.000Z] session d31ea446 — workflow 设计方向锁定（A 跨运行时可移植）+ 任务书规格&PR 内容规律总结（设计地基）

本段为「据全部任务书 + PR 设计 workflow」做地基。两块：① 设计方向拍板；② 规格 + 交付规律总结。**均未开始设计本身**（用户明确「先总结、之后才设计」）。

### 1. workflow 设计方向 = A（跨运行时可移植）
- 用户在「参考 Claude Code 实现方式、兼容所有 agent/skill 体系」问题上选 **A = 跨运行时（Claude Code / Codex / Antigravity 都能跑）**；另一读 B（复用现有 bureau/cc-suite 生态）未选。
- 定的铁律：**价值压进「工具中立核心」**（JSON schema 契约 + 确定性脚本 validator + 脚本化 repo-adapter/机制 + 数据化用例），**每家运行时只是可替换的薄壳；stage 间只传 JSON/数据、不传 agent 记忆**。与 canon `component-breakdown` 一致，只是把「中立核心 vs 薄壳」这条缝正式划出（nlpm overlay 式）。落法：照 Claude Code 制品结构设计 + 核心中立 + **先 CC 落地、接缝留干净**，不预建所有 per-tool 适配器（YAGNI）。

### 2. 任务书规格 + PR 内容 规律总结 → `doc/oprunway-spec-pr-analysis.md`
- 方法：扫全 **41 份任务书结构（41/41 模板统一）** + **5 个 agent 深读 18 个代表 PR**（跨新算子/加dtype/移植 + 6 仓、file-level）。codex 散文门**两轮**（收紧 7 处过度概括；再用本地 clone 真货核对、校准 4 点）。
- **关键结论（设计地基）**：
  1. 任务书 41/41 **统一模板**（基础信息/功能/测试/性能/精度/验收规则/TBE 参考路径）→ Task 1 输入契约天然清晰、可直接定 schema。
  2. **任务书是权威，PR 不逐项对齐** → Task 1 以任务书为准、落差标待确认（实证：Fmod 任务 INT16/PR INT32；im2col 任务 A2/A3、PR 主攻 950；RightShift 定 10×、PR 零性能证据）。
  3. **证据得 OpRunway 自己产** → 仓内性能证据基本缺席、精度证据强弱不一（零测试 → 自验证报告进 PR 都有）→ **坐实 OpRunway 价值**；且「PR 有测试」≠「验收过了」。
  4. **工程范式三分**（① 标准 GE/aclnn ② experimental 库式 `aclsparse*`/`aclblas*` C API ③ 纯头文件模板库）+ 各仓 build/run/golden/perf 不同 → repo-adapter 三模式（existing/new/generated）要全吃。
  5. **契约多样性维度**：验证模式（数值型 / 行为型如 Sleep 返回码+计时 / 精确型二进制一致）× 精度口径（AscendOpTest / MERE·MARE 按 dtype 分级 / torch 对拍 / np.isclose 按 dtype 容差 / 整数精确）× 性能基线 × 整型语义**逐算子**（饱和截断 vs wraparound，casegen 不能假设统一）。
  6. **「加 dtype」影响面常见 4 处**（op_def 白名单 / op_kernel dtype 分支 `if constexpr(sizeof(T))` / op_host tiling 内存倍增 / 该 dtype 测试）；但 diff 大小≠影响面（改已有 vs 新建目录）。
- **对 canon 的印证/修正**：`perf_baseline_source` 枚举得印证，但**基线随任务书/参考源变**——移植类=GPU-A100(0.5–0.8×，对标 cuSPARSE/cuBLAS/cuCollections)、重写与多数加 dtype 类=TBE(95%/无劣化)、部分加 dtype=同 op 其他 dtype。**这批社区任务默认多是 TBE，不是 gpu_external**——与 07-06 note 定的「默认 gpu_external」不冲突（gpu_external 是 OpRunway 额外加的 Task 3 对比层、非任务本身线），但**设计须按任务书优先**。
- **upsample uint8 交付存疑**：本地 `ops-cv` master 未见 uint8（`image/upsample_nearest` 仅 fp32/fp16/bf16）、主 PR #1012 已 closed → 之前「首个真实目标」候选**要重估**，很可能是「任务书要了但没落地」的样本（反而是 OpRunway 该识别的情形之一）。

**开放线程（更新）**：① **workflow 设计本身待启动**（方向 A 锁定 + 地基已成，用户点头即开工）；② 剩 **ADR 0006、ADR 0008 待 review approve**；③ 真机跑测待（a5 conda numpy 卡下载、回退清华镜像）；④ **首个真实验收目标重估**——upsample uint8 存疑，宜换 merged 干净的（如 Sign / IsClose / SPMV）。

## [2026-07-06T14:48:02.000Z] session d31ea446 — workflow 设计 v1（三层架构）+ Layer 0 spec.json 坐实 + 精度标准/性能口径一手坐实

接 09:18 之后的实质产出。以下结论**拟 compile 进 cabinet**（每条点名目标页；判断项 proposed、可核项 verified）。

### 决策 / 结论
1. **workflow 三层架构（约束 A 跨运行时可移植）** → 目标页「**OpRunway workflow three-layer architecture**」（新建 architecture）。`Layer 0 数据契约(6 JSON: spec/caseset/evidence/verdict/baseline/perf_report) — Layer 1 确定性脚本(gen_cases/repo_adapter/validator/perf_compare，核心脑子) — Layer 2 per-tool 薄壳(CC 编排+parse agent+3 skill+eval)`；**stage 间只传 JSON、核心工具中立、Layer 0/1 无 Claude-Code 依赖（codex 核过）**。是 [[OpRunway component breakdown]] 的落地细化。doc: `oprunway-workflow-design.md`。
2. **spec.json 入口契约坐实** → 目标页「**Acceptance case spec contract**」（新建，或并入 [[Acceptance contract and evidence chain]]）。字段照任务书定；3 实例（Sign/IsClose/SPMV）验证兜住「参考三类·改动·精度分层·性能基线」多样性；精化出 `params_source`（**仅约 17/41 任务书有参数表**，无表须从参考推、可能不准→待确认）/`dtype_combinations`（SPMV 5 组，禁非法并集）/`precision.fallback`（分层精度）/`perf.reference_cases`（具体用例基线 us）/`verify_mode` 推导（数值/行为/精确）。doc: `oprunway-spec-schema.md`。
3. **精度标准一手坐实（含更正）** → 目标页 [[AscendOpTest precision thresholds]]（已 canonical，本条坐实来源 + 更正指标定义）。一手 = **`cann/opbase` 仓 `docs/zh/ops_precision_standard/experimental_standard.md`**，明确管「贡献在 **experimental 目录**下的计算类算子」= 这批社区任务。**`MERE`=平均相对误差(Mean)、`MARE`=最大相对误差(Max)**（早先对话我口误把两者对调过，以一手原文为准）；**通过 = MERE<Threshold 且 MARE<10×Threshold**；Threshold 按 dtype：FP16 2⁻¹⁰ / BF16 2⁻⁷ / FP32 2⁻¹³ / HiFLOAT32 2⁻¹¹ / FP8 E4M3 2⁻³ / E5M2 2⁻²；相对误差公式含 +1e-7 防除零；**单标杆 = 对比高精度 CPU 或昇腾小算子拼接**。分工：AscendOpTest（`HIT1920/AscendOpTest`）=跑比对的**工具**（实现该口径，未核源码）、ATK `cv_fused_double_benchmark`（`AscendTest/ATK`，SPMV 引）=单标杆不足时的**双标杆**兜底。
4. **ADR 0006 kernel-only 双边同口径坐实** → 目标页 [[ADR 0006 — Compare performance at a matched timing scope]]（proposed）。对比双方 `scope` 必须一致、**默认 kernel-only**（msprof/msTuner）；**torch 小算子拼接基线 `us` = Σ(链各小算子 kernel-only us)、非整条链 e2e 墙钟**；取舍 = kernel-only 会**低估融合省下的 launch/调度/同步**（但中间 device 访存的减少仍反映在各 kernel duration 差值里）→ 报告可附 e2e 参考、判定用 matched kernel-only。
5. **性能基线随参考源/改动目标（非单一 gpu_external）** → 目标页 [[Acceptance contract and evidence chain]]（canonical）。移植类=GPU-A100(0.5–0.8×)、重写与多数加 dtype=TBE(95%/无劣化)、部分加 dtype=同 op 其他 dtype、可选 torch 小算子拼接；**均按任务书优先**。（gpu_external 是 OpRunway 额外加的 Task 3 对比层、非任务本身线，不冲突。）
6. **工程范式三分 & 三模式≠三范式** → 目标页 [[Repo adapter interface and modes]]（canonical）。范式三分 = 标准 GE/aclnn ／ experimental 库式（`aclsparse*`/`aclblas*` C API）／ 纯头文件模板库；**三种都优先 `existing_example`/`new_example`**，只有无现成可运行壳或 catlass 桥才 `generated_harness`；各仓 build/run/golden/perf 各不同。
7. **任务书是权威、PR 不逐项对齐** → 目标页「**Task spec is authoritative over PR**」（新建 decisions）。Task 1 以任务书生 spec、落差标待确认（实证 Fmod 任务 INT16/PR INT32、im2col 任务 A2/A3/PR 950、RightShift 10×无证据）；**PR 自带 UT ≠ 精度验收、证据 OpRunway 自产**。

### 变更（doc）
新增 4 份 doc：`oprunway-task-pr-map.md`（任务书↔PR 全量对应）、`oprunway-spec-pr-analysis.md`（规格+PR 规律）、`oprunway-workflow-design.md`（设计 v1）、`oprunway-spec-schema.md`（spec.json 坐实）。均经 codex 散文门。

**开放线程**：① 本条 compile 入 cabinet(proposed)→review 升 canonical；② `caseset.json` 坐实待做；③ ADR 0006 / 0008 待 approve。
