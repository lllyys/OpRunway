---
title: session f3bb5155-2e5e-4185-8b52-11d9e935a76f · 2026-08-12
updated: 2026-08-12
status: logbook
session: f3bb5155-2e5e-4185-8b52-11d9e935a76f
transcript: ""
---

## [2026-08-12T02:47:40Z] session f3bb5155 — 定下派发验收 subagent 的提示词模版

**Intent.** 端到端跑通 Roll 验收；过程中确定「主 session 如何把一次验收交给 subagent」的稳定形态。

**Decisions.**

- **提示词模版定为「算子 + 两个已拷出路径 + 目标机环境」，不含任何 how。** 完整形状：算子名；目标机上的
  任务书路径；目标机上的被测源码路径；随后是目标机环境——SSH 命令、容器名与 `bash -lc`（CANN 环境由
  login profile 加载，`sh` 加载不了）、SoC token、ATK 可执行绝对路径、plugin 部署目录、设备锁目录、
  执行目录。不提 SKILL、不复述 AGENTS.md、不规定 spec 来源、不规定汇报格式。
- **输入必须先由主 session 从保护根拷到非保护位置再交给 subagent。** subagent 全程接触不到保护根，
  安全边界因此不依赖它的自觉。
- 在用户给定的四项（SSH、容器名、依赖路径、执行目录）之外补了三项，理由是缺了就无法照 AGENTS.md 执行：
  SoC token（`--target-soc` 必填，否则要它自行从「A3」推断）；设备锁目录（§6 要求在预置机器共享路径上
  加 `flock`，不给路径它只能自己编）；「CANN 由 login profile 加载」半句（解释为何必须 `bash -lc`）。
- **不得把 `plugin/tests/witnesses/<op>/` 当作验收输入。** 那四套 spec/design/generator/execution_plugin
  是 `test_witness_inputs.py` 在 mock 下使用的单测夹具。它们并未过期（`roll/spec.json` 是现行
  `oprunway.acceptance_spec` v1），但直接复用会跳过 SKILL 步骤 2、3——而那两步恰是全链上唯一无工具、
  无校验、纯模型手写的环节。跑完后可拿来对照，差异按发现处理。
- 相应地撤回早先「把 `witnesses/` 提升到 `plugin/witnesses/`」的提议：既然是夹具且不应被当生产输入，
  留在 `tests/` 下位置本就正确，也不应在 SKILL 里为其指路，否则等于鼓励绕过。

**Changes.**

- 未落盘代码改动。模版本身记录于本 minute。
- 同一 session 内已提交两个 commit：`1f0f5e7` 插件收敛到单一 skill 并按官方 best practices 重排
  SKILL.md；`7958809` 清理 marketplace.json 与 dev-doc 的失效描述、并在 AGENTS.md §6 增补
  `real-machine.env` 只在主 checkout 维护一份的定位规则。canon 另做过一轮剪枝（`359f485`）。
- ignored 的 `real-machine.env` 增记 Roll 在 A3 上的只读输入位置（任务书与被测源码分别标注），
  并把其所在根加入 `OPRUNWAY_MACHINE_PROTECTED_ROOTS`，条目数 8 → 9。

**Evidence.**

- Roll 输入双向核实：任务书 SHA-256 `1088ffd3af097d450de6e25667c34afb4fe5f8e539ab7ca4418a4d79d5bd3f06`
  与调用方给定 URL 抓取所得逐字节相同；源码 content_anchor
  `d36c4c9cd88ca3f6c44cca98766a594f7beb0f694ba628c90693bae34926fd77`，用插件自身
  `oprunway.source.content_anchor` 对本地 `repos/ops-math-roll` 的 `experimental/math/roll`（25 文件）
  算得，与 A3 上一轮收据一致。
- 任务书与 op_def 交叉核验一致：SoC 侧任务书「Atlas A2、A3、A5」↔ op_def
  `AddConfig("ascend910b"/"ascend910_93"/"ascend950")`；dtype 侧 op_def 声明
  `BOOL/UINT8/INT8/BF16/FLOAT16/FLOAT/INT32/UINT32/COMPLEX64`。任务书性能要求为「无」。
- A3 环境实测：ATK 在 `/tmp/oprunway-atk-oprunway22-7220f27/venv/bin/atk`，版本 26.5.14，**不在 PATH**；
  该 venv 的 `python3` 是指向 `/usr/local/python3.12.13/bin/python3` 的符号链接，与容器默认解释器同一个，
  venv 只改 `site-packages`；以 `env -i` 剥离环境后 ATK 在 `torch/__init__.py` 的 `_import_device_backends`
  崩溃，说明它依赖 login profile 加载的 CANN 变量。插件 `_execution_environment` 以 `os.environ.copy()`
  全量继承、只删 `ASCEND_RT_VISIBLE_DEVICES` 与 `OPRUNWAY_DEVICE_*`，与该依赖相容。

**Open threads.**

- **任务书→spec 无生成能力。** CLI 只有 `accept` 子命令且 `--spec` required；任务书在代码中仅被校验哈希
  格式、复制进 session、参与锚定，无一处读其内容；`extract`/`parse.*doc`/`spec_from`/`generate_spec`
  在 `plugin/oprunway/*.py` 零命中。因此 spec 是否忠实于任务书**无任何机制校验**——写错一个 dtype，
  格式校验照过、哈希链照闭、PASS 照出。对已有 witness 的算子不构成阻塞，对新算子是硬缺口。
- 采用该模版的代价：拷贝输入、部署 plugin、判定 ATK 路径这三步移出端到端测试范围，由主 session 承担。
- A3 上 `/work/run/oprw_v2`（配置里的 `OPRUNWAY_MACHINE_PLUGIN_DIR`）不存在，本轮插件尚未部署。
- ATK 装在 `/tmp` 下，容器重启即失；`/tmp/oprunway-atk-7220f27.tar` 可重新解包。不应假定其长期存在。
- 两次派出 subagent 均被主动中止（第一次提示词含过多 how，第二次用户叫停），均未触及远端，A3 状态未变。

**Source.** transcript ``

## [2026-08-12T03:17:57.358Z] context checkpoint (compaction)

_Context was compacted here. Full transcript on disk. Run `bureau:note` to capture live minutes._

## [2026-08-13T01:13:55Z] session f3bb5155 — Roll 四轮隔离验收，与规则归位模型

**Intent.** 端到端跑通 Roll 隔离验收；从跑出来的事故反推「主干之外的规则如何在运行时生效」的机制。

**Decisions.**

- **`plugin/tests/` 移出 plugin。** 第一轮 PASS 20/20，但事后哈希比对确认它把 `tests/witnesses/roll/` 的
  `design.yaml`、`generator.py`、`execution_plugin.py` 逐字节复用为验收输入，等于跳过「从任务书推导 spec
  与 design」——全链上唯一无工具、无校验的环节。夹具随 plugin 部署到目标机且带哈希绑定，看起来比现推更
  可信；这是结构缺陷，不是会话的错。
- **`--plugin-dir` 必须指向仓外中性副本。** 第二轮实测：指向仓内 `plugin/` 等于把仓库地图交给会话，它据此
  读到夹具、ignored 的私有配置，还在仓根 `ls`，并开始拿上一轮的 PASS 当依据。隔离只挡住 `CLAUDE.md` 自动
  加载，挡不住路径遍历。
- **accept 的等待方式定为「脱离驱动进程 + 反复有界检查」。** 实测三条：无头模式下后台任务不能保活会话
  （45 秒任务、会话 28 秒退出）；本地后台进程随会话一起被杀；单次命令上限远小于 accept 的 7200 秒预算。
  因此「必须前台阻塞」是错的。第四轮能跑完是侥幸——accept 经 SSH 在容器内运行，不是本地子进程。
- **三条 ATK 能力边界不管，如实保留 UNVALIDATED**（非连续 Tensor、dims 空数组、uint32 与 argtypes 缓存）。
  按仓规，工具够不着的东西记账不伪造即为正确处置；不在 witness 里绕，绕了等于把 ATK 的坑抄一份进来。
- **性能验收三项裁定：** 做 op_def 与性能双非回归；有同口径基线时才判，无基线只测不判；先写设计代码不动。
  GPU 标杆永远 UNVALIDATED（仓规禁止碰 GPU）。
- **状态机保留不动。** 奥卡姆剃刀复查未发现明显赘肉：performance 205 行被 4 个 witness 中的 3 个使用；
  手写 XLSX 解析器 88 行是唯一有逐 case 判定的来源；`task_case_bundle` 62 行是仓规要求。可删的只有两段
  近乎重复的 ERROR 收据约 20 行。

**Reversals.**

- **撤回「把 accept 劈成 run + verify」。** Codex 评审指出 `finalize()` 不是独立证据验证器，而是建立在
  「前序收据由受控代码诚实产生」前提上的一致性检查器。最锋利的反例已自行复核：`expected_error_case_ids`
  只在 `atk.py:1242` 从 caseset 推导，`verdict.py` 只做类型校验、从不重新推导；执行权若交给 agent，
  声称全部 case 为 expected-error 即可让 `required_output_ids` 变空集。
- **撤回「generator 缺口是 ATK 缺陷」。** design 语言表达不了 shifts/dims/rank 耦合是声明式语言的固有
  边界，正是仓规 §5 落点矩阵指定的 generator plugin 用途。只有 execution 侧那两条是真缺陷。
- **撤回规则归位模型第一版（五层 + 尽量下沉）。** 五层混了四个维度；`reference` 只是文档装载方式不是执行
  层；一条规则是穿过多层而不是归入某层。「尽量下沉」也错——下沉提高执行力的同时降低知晓度。
- **撤回「主干会变短」。** 实算净 −8 行；规则体都在三行以内时反而增长 5 至 7 行。
- **撤回「报假阳性率」这条纪律的充分性。** 永远回答「没有原算子」的检测器假阳性率为零却漏掉 Roll；
  危险方向是假阴性，要报完整混淆矩阵。

**Changes.**

- `e6288b3` `plugin/tests/` git mv 到仓根 `tests/`，修 `test_plugin_manifest_and_active_entry_inventory`
  的过期期望值（`agents/`、`commands/` 早已删除）。真机 81 passed / 0 failed。已推。
- `9827271` 新增 `.claude/skills/isolated-acceptance/`，并修真机跑出的三处错：`--plugin-dir` 改指中性
  目录、提示词模版变量加花括号（全角逗号会被 shell 连字节吞掉）、步骤 9 的「`-p` 只在结束时输出」前提
  在加了 `--output-format stream-json --verbose` 后不成立。已推。
- `2a2e9bd` `acceptance-workflow` 步骤 8 新增「怎么等它跑完」（措辞不假设具体驱动环境）；
  `isolated-acceptance` 步骤 9 新增「会话结束不等于验收结束」；`--atk-bin` 触发条件两处均改。未推。
- `3216cd9` 新增 `dev-doc/oprunway-original-operator-baseline-design.md`，169 行，代码零改动。未推。

**Evidence.**

- 第四轮终态 `PASS` / `ALL_REQUIRED_EVIDENCE_PASSED` / `225/225`，`spec_sha256` `12db9a5e…c887c`，
  物理卡 davinci6，主动耗时 588.9 秒 / 预算 7200，端到端 49 分 23 秒，$20.47。拿掉参考答案后用例数
  从 20 涨到 225，未验证条款从 5 条增到 7 条且具体得多。
- 状态机零门触发：全机只有一个 `workflow.json`，`status=COMPLETE`、`failed_stage=None`。真正的诊断价值
  全部产生在绕过路径——agent 在 `work/` 下手工重现了每一阶段（`build.sh` 215KB 日志、`ops_custom`
  安装器、atk casegen、atk 执行），`probe-log` 里没有任何一处调用 `oprunway_cli.py`。它在那里发现两个
  ATK 缺陷、避免了一次 `DUT_FAIL` 误判。
- 功能非回归缺口实证：`conversion/roll`（原算子）支持 INT64，`experimental/math/roll`（目标）不支持，
  任务书参数表也没列，而任务书第 22 与 86 行要求「保持原有 dtype 功能不变」。三方不一致，第四轮照常
  PASS——工作流只做「任务书 ↔ 新算子 op_def」交叉验证，从不看原算子。
- 规则增长曲线：`SKILL.md` 从 2026-07-09 的 167 行 2 处条件，长到 2026-08-10 的 793 行 29 处条件，
  随后被 `904cf32` 重置为 115 行 3 处条件，两天后回到 264 行 7 处条件。
- `904cf32` 的真实规模：301 files changed，6,562 insertions，**129,125 deletions**，删除 260 个文件
  （整个 `acc-common/`：自研 aclnn runtime、golden 引擎、msprof 性能采集、catlass 支持）。
  `AGENTS.md` 同批从 266 行掉到 110 行（−88%），`DUT_FAIL` 与 `UNSUPPORTED` 是重写后才有的词表。
  **没有任何载体活下来，活下来的只有问题域。**
- 对「重写后增速更快是旧债浮出」的核实：只对一半。`a47c484` 加的 73 行里，`bundle` 与「落点」在 793 行
  版本中确有（各 3 处命中），但 `flock`、`npu-smi`、`TARGET_DELIVERY_MISSING` 重写前均 0 命中，是真新增。

**规则归位模型（本轮主要产出，经两轮 Codex 评审与用户两次纠偏）。**

三部分。一、**归位**：每条规则占两个坐标——执行位（`E1` spec 契约 / `E2` 运行前置 / `E3` 证据门 /
`E4` 控制面 / `E5` 无）与知晓位（`K1` 注册表条目 / `K2` 主干正文 / `K3` reference / `K4` 无）。两条硬
约束：知晓位不得为 `K4`，至少 `K1`；执行位为 `E5` 时知晓位必须 `K1` 加 `K2`。核心原则由用户提出：
**执行可以沉，存在必须浮。**

二、**完整性**：最小注册表五字段（`rule_id` / `applies_if` / `decide_by` / `outcome_action` /
`evidence_validator`），只登记会改变执行路径、caseset 分母、证据要求、终态声称或阶段放行的逐轮决策。
清单由注册表机器生成，manifest 逐条 `yes`/`no`/`unknown` 加依据，accept 校验 ID 集合相等、拒绝逾期
`unknown`，`registry_sha256` 进哈希链。否定结论也要给依据。

三、**存续**：目标改写为「长久不等于载体不变，长久等于每次换载体都不丢东西」。三样——能力回归账本
（注册表防不住 torch-baseline 那类损失，它会老实回答「无基线 → UNVALIDATED」，形式完整却看不出该能力
曾经有过）、迁移映射四态（`preserved` / `replaced_by` / `intentionally_retired` /
`temporarily_unsupported`，无去向即迁移未完成）、减少载体数（规则现散在六处）。

三条纪律：语义触发不下沉；下沉前报完整混淆矩阵；不引入排他的任务类型。

Codex 第二轮评审结论：核心闭环不过度、应当现在做，七字段与通用基础设施过度；盈亏平衡点 10 至 12 条
活跃规则，风险调整后降到 6 至 8，当前正处该区间；触发门槛六项中本仓已满足至少三项（30 天高频增长、
九文件三轮散文同步、重写丢能力）；明确反对把推倒重写当周期性策略。落地顺序按 `904cf32` 的数据重排为：
能力回归账本 → 迁移映射规矩 → 注册表。

**Open threads.**

- `SKILL.md` 步骤 9 把 `UNSUPPORTED` 与流程错误并列、要求「修复流程后回到步骤 6 重跑」，而
  `workflow.py:26` 的 `_FORMAL_VERDICTS` 明确把它当正式终态。目标 SoC 不在任务书硬件集合时没有流程
  可修。真 bug，未修。
- 规则归位模型的文档已起草未落盘（`dev-doc/oprunway-rule-placement-model.md`），等结论稳定后再写。
- `task.baseline`、`performance_tolerance` 在 plugin 与 SKILL 里 0 命中，`PERFORMANCE_MODES` 仍是
  `{"none","measure"}`，行为与第四轮完全一致——设计已定，代码未动。
- `.claude/settings.json` 被 `/permissions` 写成了共享配置，含 `Bash(claude --plugin-dir:*)` 前缀放行；
  未提交，建议挪进 `settings.local.json`。
- spec 原文不进 session 已查实（`spec_digest` 记规范化 dict 哈希，`materialize_inputs` 的五个
  `_copy_input` 里没有 spec），暂不改。
- `2a2e9bd` 与 `3216cd9` 未推。

**Source.** transcript ``

## [2026-08-13T01:55:09Z] session f3bb5155 — ATK 自带文档、上下文增长，与两份 skill 的分发边界

**Intent.** 查第四轮上下文为何涨到 326k；由此发现 ATK 自带文档，重判「ATK 事实要不要沉淀进仓库」。

**Decisions.**

- **`isolated-acceptance` 是开发期工具，不随 plugin 分发；真实用户只加载 `plugin/`。** 因此任何面向用户的
  能力都不得依赖主机侧 skill。据此推翻了同一轮里刚提的方案——原打算让主机侧探测 ATK 文档路径、经提示词
  传给隔离会话，那样真实用户永远拿不到。探测必须落在 `plugin/skills/acceptance-workflow/SKILL.md`
  步骤 6。自查今天放进主机侧 skill 的内容（启动机制、提示词模版、「会话结束不等于验收结束」）均为驱动
  无头会话的事，位置正确。
- **ATK 文档指路，不复制。** 复制副本正是本轮反复抓到的漂移病（`--atk-bin` 两处皆错、已删的
  `plugin/AGENTS.md` 抄设备协议抄丢一句、`marketplace.json` 整段过时）。不复制则版本天然对齐：安装目录名
  里带 commit（`oprunway-atk-oprunway22-7220f27`），文档与可执行文件必为同一版。
- **撤回同一轮里提出的 `reference/atk-facts.md`。** 那就是复制。ATK 官方文档存在时应当指向它；不存在时
  agent 仍需逆向，但逆向结论属于「本轮事实、绑 ATK 版本」，写进本轮记录，不进仓库知识。
- **规则要 `E ∧ T`，事实只要 `K`。** 之前把两者混谈了。ATK 的行为约束是 agent 需要知道的事实，不是要它
  遵守的规则，所以不需要强制与留痕，只需要可知晓。
- **按 1M 上下文设计。** 主干长度、渐进披露省 token、多 agent 分上下文这三条顾虑作废。第四轮峰值 326k
  仅占三分之一。渐进披露保留，但理由从「省 token」换成「事实绑外部工具版本，生命周期与 SKILL 不同」。

**Evidence.**

- 第四轮上下文实测：起始 33,142，结束即峰值 326,326，287 个助手轮次单调增长，中途无回落。换成 200k
  上下文的模型约在第 130 轮爆掉，那时才走到步骤 5 至 6。
- 增长来源：工具结果 149 条合计 346,624 字符（约 11.5 万 token），其中**读 ATK 源码 49 次、200,470 字符、
  占 57%**；被测源码 6%，session 与日志 6%，`npu-smi` 4%，plugin 源码 1%。agent 自身输出另有 126,407
  token。大致一半来自读，一半来自自己推理。
- ATK 仓（`repos/ATK`，HEAD `7220f27`，与 A3 安装目录同一 commit）自带两个 skill
  （`atk-quality-guard`、`atk-operator-onboarding`）与 27 篇中文文档，覆盖用例设计文件说明、用例生成、
  任务执行参数、结果分析、自定义执行方式、自定义参数约束。
- **但这些不随安装分发。** `MANIFEST.in` 只有 `include requirements.txt`；`setup.py` 用
  `packages=find_packages()`；实测 `venv/lib/python3.12/site-packages/atk` 下 md 文件数为 **0**。A3 上之所以
  可见，是因为 ATK 从源码 tarball 安装，`src/` 是解包留下的源码树而非安装产物。`pip install` 的环境不会有。
- `03 扩展开发/自定义参数约束.md` 给出的官方机制正是继承 `CaseGenerator` 重写 `after_case_config`，示例是
  `torch.max` 的 `dim` 需落在输入 rank 范围内——与 Roll 的 shifts/dims/rank 耦合同形。因此第四轮那个
  generator 插件是照官方推荐做法做的，不是绕缺口。此前「那是声明式语言的固有边界」的判断方向对但说轻了。
- `02 参考资料/精度标准说明.md` 只讲如何配置 `single_bm`，明说「具体阈值和实验标准更新以外部文档为准」。
  因此第四轮逆向逐 dtype 阈值这部分是必要的，其余大部分是白读。
- 另一处已知张力：逐 dtype 阈值所依据的 opbase 生态精度标准，其整理成果在 canon 内，而 `AGENTS.md` §8
  规定验收流程不得读取 canon。该知识存在却对验收 agent 不可见。未处理。

**Changes.**

- `bfacdb8` 修 `SKILL.md` 步骤 9：`UNSUPPORTED` 与 `PASS`、`DUT_FAIL` 一样是正式终态，不再与
  `PLUGIN_ERROR`/`NEEDS_INPUT`/`BLOCKED` 并列要求「修复流程后重跑」，并明确禁止为绕开该终态去改
  `task.hardware`（它来自任务书）。未推。

**Open threads.**

- ATK 文档探测尚未落地。形状已定：步骤 6 尝试定位 ATK 源码树内的 `docs/`，找到记路径、找不到记不可用；
  步骤 2 与步骤 3 的「先读文档不要读源码」以该探测结果为条件。降级优雅——没有文档时退回现有行为。
- 规则归位模型的文档仍未落盘，且需并入本轮两处修正（规则与事实的区分、1M 上下文前提）。
- 待推：`2a2e9bd`、`3216cd9`、`bfacdb8`。

**Source.** transcript ``

## [2026-08-13T03:42:18Z] session f3bb5155 — plugin 边界总纲：内部只服务验收

**Intent.** 从「`real-machine.env` 用户没有」这一个具体问题出发，发现它是一类病，立总纲并清理全部实例。

**Decisions.**

- **`plugin/` 的全部内容只服务于执行一次验收，不承载开发期的设计、取舍与维护判断。** 判据：一个只拿到
  `plugin/`、要验收一个算子的执行者，需不需要读这条内容？不需要就不该放在里面。已写入 `AGENTS.md` §2。
  据此排除的典型内容：何时把 witness 提升为通用能力、如何新增 build profile、如何运行本仓测试、开发期
  私有配置的位置与文件名、具体算子的一次性输入绑定。
- **验收运行时不得修改 `plugin/` 下的通用代码。** 两个理由都在运行时成立：条件无从求值——「是否已有第二个
  独立算子撞过同一缺口」需要跨 session 的项目历史，而每轮都是全新 session，且隔离修复之后会话根本够不着
  仓库；动作也留不下痕迹——收据绑定了 taskdoc、source anchor、design、generator、execution_plugin，唯独
  **不绑定 plugin 自身身份**。
- **两份 skill 的受众不同，规则位置也不同。** `AGENTS.md` 不随 plugin 分发、隔离会话刻意不加载它，所以
  只写在那里的规则对验收 agent 是不可见的（`K4`）。给维护者的总纲放 `AGENTS.md`，给 agent 的行动约束
  必须放 `SKILL.md`。
- **canon 页 `claude-code-plugin-agent-discovery` 退役。** plugin 已收敛到单一 skill，该页描述的机制在本仓
  没有作用对象；status 一直是 proposed，从未被批准。由用户裁定删除。

**Reversals.**

- 我曾把该 canon 页误判为「意外删除」并擅自 `git checkout` 恢复。实为用户有意删除，已撤销恢复。教训：
  工作树里出现未预期的删除时，先问，不要先恢复。
- 我在同一轮里往步骤 5 补了三句解释性文字（向执行者说明「提升为通用能力是维护者的判断」「运行时为何
  判断不了」「正式范围只覆盖 atk_aclnn + cann_ops_package_v1」），随后用刚立的判据检验自己这段，三句
  全部不合格——它们在向执行者解释维护者的推理，读了不改变任何行为——已删除，只留行动约束。

**Changes.**

- `a617f11` 立总纲并清理七处实例：步骤 6 点名 `real-machine.env` 改为普适表述；
  `reference/witnesses.md`（GaussianBlur 的算子名与上游 URL）移到 `dev-doc/oprunway-operator-bindings.md`
  并删掉步骤 4 的链接；`plugin/README` 里失效且带算子专属环境变量的测试说明整节删除、修正后移到仓根
  README；`--atk-bin` 触发条件补齐剩余三处；`runner.device` 的「隔离容器内」改为「运行环境内」；步骤 5
  落点矩阵删掉 core adapter 一行、「新增 build profile」改为停在 `BLOCKED`；价值顺序第 5 条并入第 4 条，
  六条减为五条。**`plugin/` 现在不含任何算子名，也不含开发期私有假设。**
- `b797448` 退役上述 canon 页。

**Evidence.**

- **一条规则，五个副本。** `--atk-bin` 的触发条件原先在 SKILL 步骤 6、SKILL 步骤 8 表格、仓根 `README.md`、
  `plugin/README.md`、`dev-doc/oprunway-real-machine-environment.md` 五处各有一份，**全部写成「多版本并存时
  才用」，全部是错的**——真机上 ATK 装在虚拟环境目录里，既不在 `PATH` 也不存在第二个版本。今早修了前两处
  并以为修完，随后在同一天内又陆续发现三处，其中一处随产品分发。这五个副本是本仓「同一条规则写多处必然
  分叉」最硬的实证，也是规则注册表机制最直接的论据：不是「将来可能漂」，是现在就有五份、追了一整天还在冒。
- 收据 `inputs` 绑定六项（`taskdoc_sha256`、`source_content_anchor`、`design_sha256`、`generator_sha256`、
  `execution_plugin_sha256`、`task_case_bundle`），`grep plugin_sha\|plugin_version\|self_digest` 在
  `plugin/oprunway/*.py` 零命中——`execution_plugin_sha256` 绑的是调用方给的 witness 插件，不是 `oprunway/`
  本身。

**Open threads.**

- **收据应绑定 plugin 自身身份。** 启动时算 `oprunway/` 的聚合摘要写进 `workflow.json`，「本轮没改通用
  代码」即从一句自觉变成可核验事实（按本轮模型从 `T0` 抬到 `T2`）。属代码改动，未做。
- `atk-source-findings` 十条终稿已产出（200 行，逐条回源码核过，核出草稿一处错误：默认不注入离群值，
  配比末位为 0），尚未入仓。落点定为 `plugin/skills/acceptance-workflow/reference/atk-source-findings.md`，
  与 `reference/atk/` 平级——后者是上游逐字副本用于 diff，前者是我们的派生结论，必须物理分开。
- 其中两条是会造成假 PASS 的上游 bug，均已独立复核属实：`single_bm` 的五个 `*_thd` 阈值覆盖键因
  `setdefault` 作用于已存在的键而静默无效，只有 `mare_ratio` 生效；CPU 真值含 nan/inf 时该 case 无条件
  判通过、不做任何比较，而 `has_infnan` 默认为 `True`。第四轮 Roll 未被这两条影响——它没用阈值覆盖，且
  在 design 里主动写了 `has_infnan: false` 并注明理由，生成的 225 个 case 中 inf/nan 命中数为 0。
- `.claude/settings.json` 仍未提交。`/permissions` 把 `Bash(claude --plugin-dir:*)` 写进了项目共享配置，
  建议挪进 `settings.local.json`。
- 待推 commit 增至九个。

**Source.** transcript ``

## [2026-08-13T07:00:34.209Z] context checkpoint (compaction)

_Context was compacted here. Full transcript on disk. Run `bureau:note` to capture live minutes._
