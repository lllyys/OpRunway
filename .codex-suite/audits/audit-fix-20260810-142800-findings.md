# OpRunway full audit findings — 2026-08-10

Scope: all 40 active implementation, test, agent, skill, command, manifest and current implementation-document files named by the acceptance-plugin refactor. Historical/archived material, `canon/`, bureau records, the ATK checkout, caller DUT repositories and generated dependency/output trees were excluded.

Mode: full nine-dimension read-only audit, severity `all`, intended fixer `codex`.

## Degraded-audit record

Grok completed 32 file audits. Eight read-only calls stopped with `stopReason=cancelled` after trying to inspect dependencies outside the exact workspace; they were not retried or worked around. Grok preflight immediately afterwards was healthy (`grok 1.0.0`, session auth, default `deepseek-v4-flash`; models `grok-4.5`, `deepseek-v4-pro`, `deepseek-v4-flash`). Per the audit skill, those files received a fresh manual Codex fallback and the automatic fix loop was not started from this degraded pass.

| File | Job | Thread | Fallback result |
|---|---|---|---|
| `plugin/oprunway/contract.py` | `audit-fix-audit-msnb1cp6-bka5gz` | `019febfe-e4d9-7521-82cb-0aee7a44c04b` | one verified finding below |
| `plugin/.claude-plugin/plugin.json` | `audit-fix-audit-msnb59p2-e5knxk` | `019fec01-ae02-7760-9b32-5d506cc94682` | clean |
| `plugin/tests/witnesses/gaussian_blur/generator.py` | `audit-fix-audit-msnbm81o-ijufxt` | `019fec0d-bf7f-7e83-8586-3594ddbe50e7` | shared generator-bound finding below |
| `plugin/tests/witnesses/remainder/generator.py` | `audit-fix-audit-msnbmbh0-sq1dnf` | `019fec0d-d00b-7cb1-bff5-177ea7f47e06` | shared generator-bound finding below |
| `plugin/tests/witnesses/roll/generator.py` | `audit-fix-audit-msnbmd77-x7riw1` | `019fec0d-d9ac-7d12-adfc-10c00ab1073c` | shared generator-bound finding below |
| `plugin/tests/witnesses/gaussian_blur/spec.json` | `audit-fix-audit-msnbmaft-wvrme9` | `019fec0d-cca4-73e2-8cfa-fa98681ade68` | clean against the current contract/task document |
| `plugin/tests/witnesses/remainder/spec.json` | `audit-fix-audit-msnbm5uv-4nyz45` | `019fec0d-b57a-7d92-a0eb-464ac4a85047` | clean against the current contract/task document |
| `plugin/tests/witnesses/roll/spec.json` | `audit-fix-audit-msnbmg0m-nqt0s7` | `019fec0d-e757-7781-98d0-8b8ae84bbf42` | clean against the current contract/task document |

Unsupported/speculative Grok claims were excluded, notably proposals to re-authenticate caller-trusted input pairs, restore the removed multi-agent architecture, treat generator-overridden ATK design sampling ranges as the formal acceptance contract, remove task-required fp64 merely from a different-machine torch warning, or rewrite historical change-log entries.

## Verified findings

| ID | Severity | File(s) | Finding | Intended minimal fix | Status |
|---|---|---|---|---|---|
| F01 | High | `plugin/oprunway/source.py`, `build.py`, `workflow.py`, `verdict.py` | Source facts bind only the operator subtree although `build.sh` and shared build files outside it determine the vendor package. The build command/ELF chain therefore lacks a complete build-input anchor. | Add one shared full staged-build-tree anchor with the same transient exclusions as staging; reject symlinks; bind facts, staging, build receipt and verdict. | fixed; verified on A3 and A5 |
| F02 | Medium | `plugin/oprunway/build.py` | `_find_cache` accepts arbitrary substring occurrences of SoC/operator token, so a changed unrelated cache can prove the target accidentally. | Parse exact CMake cache assignments for compute unit, op cache and vendor. | fixed; verified on A3 and A5 |
| F03 | Medium | `plugin/oprunway/atk.py` | Accuracy columns with a mix of `True` and `None` are filtered before normalization and can become a false pass. | Treat every matching accuracy column as required boolean evidence. | fixed; verified on A3 and A5 |
| F04 | Medium | `plugin/oprunway/atk.py` | Profiler evidence checks only headers plus one row; duplicate kind/case CSVs and non-numeric/non-positive timing cells can pass. | Require exactly one CSV per kind/case and at least one finite positive required timing value. | fixed; verified on A3 and A5 |
| F05 | Medium | `plugin/oprunway/atk.py`, `verdict.py` | Zero-byte saved outputs are accepted as CPU/DUT evidence. | Reject empty output evidence at collection and deterministic finalization. | fixed; verified on A3 and A5 |
| F06 | Medium | `plugin/oprunway/verdict.py` | Malformed receipt/vendor/report objects can raise raw `AttributeError`, `KeyError` or `TypeError`; performance row IDs are sorted before type validation. | Validate object shapes and IDs before dereference/sort, returning typed evidence errors. | fixed; verified on A3 and A5 |
| F07 | Medium | `plugin/oprunway/workflow.py` | The total budget is checked before long stages but not again around finalization; copied design/generator/plugin time is missing from stage timings. | Time all input materialization and check remaining budget immediately before and after finalization. | fixed; verified on A3 and A5 |
| F08 | Medium | `plugin/oprunway/util.py` | A second `wait()` timeout after `SIGKILL` escapes as raw `TimeoutExpired`. | Convert it to a typed workflow termination error. | fixed; verified on A3 and A5 |
| F09 | Medium | `plugin/oprunway/contract.py`, `util.py` | Python JSON accepts `NaN`/infinity; canonical hashing can therefore serialize non-standard values in comparator options or fail with an untyped exception. | Make canonical JSON reject non-finite/non-serializable values with a typed input error. | fixed; verified on A3 and A5 |
| F10 | Medium | `plugin/oprunway/cli.py` | Failure receipt generation re-reads the spec after execution, creating a time-of-check/time-of-use digest and an unnecessary second input read. | Carry the once-validated digest from `main`; never touch dirty sessions. | fixed; verified on A3 and A5 |
| F11 | Medium | `plugin/oprunway/atk.py` | Read-only workbooks are not closed on success or error; malformed UTF-8/shape errors can escape untyped. | Close in `finally` and normalize parse failures to `ATK_REPORT_INVALID`. | fixed; verified on A3 and A5 |
| F12 | Medium | `plugin/oprunway/build.py` | `nm` timeout is not converted to a workflow error. | Catch `TimeoutExpired` as `SYMBOL_PROBE_FAILED`. | fixed; verified on A3 and A5 |
| F13 | Low | `plugin/oprunway/atk.py`, `verdict.py` | `performance_skipped_reason` is dead schema left from a removed skip path; current policy always attempts declared performance while budget remains. | Remove the field and its validation branches. | fixed; verified on A3 and A5 |
| F14 | Low | `plugin/oprunway/__init__.py`, manifest | Package `__version__` says `0.2.0` while the only public manifest says `1.0.0`; nothing consumes the package constant. | Delete the redundant version constant. | fixed; verified on A3 and A5 |
| F15 | Medium | all four witness generators | Out-of-range ATK generator indices silently repeat or cycle scenarios, inflating denominator/time after an ATK design/version drift instead of failing closed. | Pin each versioned witness to its expected generated-case count and reject extra indices. | fixed; verified on A3 and A5 |
| F16 | Medium | Bernoulli witness | The statistical comparator cannot meaningfully adjudicate a one-element `p=0.5` scalar case; empty/scalar boundary semantics should be deterministic. | Use a boundary probability for the scalar witness while retaining large-sample non-boundary cases. | fixed; verified on A3 and A5 |
| F17 | Low | GaussianBlur execution witness | OpenCV squeezes `[H,W,1]` to `[H,W]`, contrary to the task's channel-preserving result shape. | Restore the trailing singleton channel in the CPU oracle. | fixed; verified on A3 and A5 |
| F18 | Medium | `README.md`, `plugin/README.md` | Documentation presents all six workflow/attempt statuses as possible `acceptance.json` verdicts; only `PASS`, `DUT_FAIL`, `UNSUPPORTED` are formal acceptance verdicts, while setup/process errors are attempt/workflow statuses. | Separate formal verdicts from non-verdict attempt statuses. | fixed; verified on A3 and A5 |
| F19 | Low | plugin docs/skill/current machine doc | The quick start omits `OPRUNWAY_PLUGIN_ROOT`, the case receipt description omits the ATK executable hash, and the machine doc contains a stale section citation/path duplication. | Correct only current entry documentation. | fixed; verified on A3 and A5 |
| F20 | Medium | `plugin/tests/test_core.py`, `test_witness_inputs.py` | Tests do not pin mixed `None` accuracy, profile timing/duplicates, zero-byte output, build-tree staging-anchor parity, exact ATK probe argv, or all witness-file private-path leakage. | Add focused regression cases without restoring broad legacy suites. | fixed; verified on A3 and A5 |
| F21 | Medium | `dev-doc/oprunway-todo.md` | Hypothetical P1 failures are listed without observed evidence or completion criteria, conflicting with the document's own evidence-first rule. | Keep only observed blockers and give each an explicit completion condition. | fixed; verified on A3 and A5 |
| F22 | High | `plugin/oprunway/source.py`, `workflow.py` | Post-fix audit proved that the operator anchor retained `__pycache__`/`.pytest_cache`, while clean staging drops them at every depth, causing false `SOURCE_DRIFT`. | Give the operator anchor exactly the same anywhere-transient exclusions while reserving root build-output exclusions for the full build-input anchor. | fixed; verified on A3 and A5 |
| F23 | Medium | `plugin/oprunway/workflow.py` | Publishing JSON then Markdown with two renames can expose a formal JSON beside a missing/pending report when the second rename fails. | Promote Markdown first, JSON last, and roll Markdown back if JSON promotion fails. | fixed; verified on A3 and A5 |
| F24 | Low | `plugin/oprunway/workflow.py` | Session creation races/permission failures can escape as raw OS exceptions, and publish failures inherit the previous stage name. | Normalize session-creation failures and name the publish stage explicitly. | fixed; verified on A3 and A5 |
| F25 | Medium | `plugin/oprunway/util.py` | `run_command` does not validate a positive timeout and lets command-start `OSError` escape its typed error contract. | Validate timeout and convert start failure to `COMMAND_START_FAILED`. | fixed; verified on A3 and A5 |
| F26 | Medium | `plugin/oprunway/util.py`, `cli.py` | Output-write OS failures can escape while writing the attempt receipt, replacing the original typed failure with a traceback. | Normalize atomic-write OS failures and keep CLI error reporting best-effort. | fixed; verified on A3 and A5 |
| F27 | Medium | `plugin/oprunway/atk.py` | Empty saved files for expected-error or already-failed cases are rejected before the required-output filter is applied. | Reject empty files only for case IDs whose outputs are required; ignore non-required placeholders. | fixed; verified on A3 and A5 |
| F28 | Medium | `plugin/oprunway/atk.py` | Accuracy normalization validates incidental device-time columns even when performance is not requested. | Parse device timing only for a performance run. | fixed; verified on A3 and A5 |
| F29 | Medium | `plugin/oprunway/atk.py` | The ATK executable is probed before its receipt hash is captured, leaving a probe/hash swap window. | Hash before the probe, re-hash after it, and verify again immediately before every ATK command. | fixed; verified on A3 and A5 |
| F30 | Medium | `plugin/oprunway/workflow.py` | A workflow-receipt write failure after acceptance publication can leave a formal acceptance artifact beside an ERROR/incomplete workflow. | Roll the final report pair back to pending names before recording any post-publication error. | fixed; verified on A3 and A5 |
| F31 | Medium | `plugin/oprunway/build.py`, `plugin/tests/test_core.py` | First formal Bernoulli build proved that CANN records the requested package vendor in canonical `VENDOR_NAME`, while lowercase `vendor_name` retains an unrelated default; checking the lowercase key rejected a valid fresh build. | Match exact `VENDOR_NAME` together with the existing exact SoC and op-cache keys; pin the real dual-key cache shape in the target regression. | fixed; verified on A3 and A5 |
| F32 | High | `plugin/oprunway/source.py`, `build.py`, `atk.py`, `verdict.py`, tests | The next formal build materialized a new root `third_party/` tree without changing any caller input, so comparing the complete post-build tree directly to the pre-build anchor caused a false `SOURCE_MUTATED`; merely allowing additions would leave generated dependencies unbound. | Preserve the strict pre-anchor and pre-existing-file mutation gate; bind the complete post-build tree in a second deterministic anchor and replay it before execution and finalization. | fixed; verified on A3 and A5 |
| F33 | High | `plugin/oprunway/util.py`, `atk.py`, `verdict.py`, tests | ATK workers can detach into a new process group, survive the parent timeout, continue writing command logs and contaminate the independently scheduled performance phase; the original command hashes can then become stale. | Give every command a unique inherited token; require Linux pidfd isolation before launch; drain exact token-bearing processes before return; settle final log hashes; replay casegen, ATK probes, build/install and execution command drain/log evidence in the finalizer. | fixed; verified on A3 and A5 |
| F34 | High | Roll execution witness and target test | ATK cannot natively encode an empty `attr_tuple`; the marker initially reached execution as an omitted argument, and later a valid empty `aclIntArray` pointer was still routed through ATK's generic list converter before the DUT ABI. | Restrict the marker to the exact bound empty-dims cases and append a direct empty `aclIntArray` pointer at the final Roll ABI slot; preserve the generic path for non-empty dims and prove cleanup ownership. | fixed; verified on A3 and A5; formal Roll 20/20 PASS |
| F35 | Medium | Bernoulli execution witness and target test | ATK promotes FP16/BF16 CPU benchmark inputs to FP32 before invoking the reference plugin, so deriving output dtype from the runtime tensor produced a false two-case numerical mismatch. | Restore reference output dtype from the immutable declared case config and pin promoted-runtime FP16/BF16 regressions. | fixed; verified on A3 and A5; formal Bernoulli 19/19 PASS |
| F36 | Medium | `plugin/oprunway/atk.py`, GaussianBlur/Roll witnesses and tests | The workflow depended on `openpyxl`, selected a globally empty ATK accuracy column, and represented structured tuple attributes as scalar `attr`; these caused missing precision and malformed execution inputs. | Use a dependency-free strict OOXML reader, ignore only globally empty accuracy columns, model structured attributes as `attr_tuple`, and fail closed on malformed tuple/sentinel evidence. | fixed; verified on A3 and A5 |

The findings section originated in the degraded first audit; the status column and resolution section below record their later closure.

## Resolution and independent verification

All F01–F36 rows above were fixed with the smallest general change and were rechecked against the final active implementation. Later independent read-only passes also checked non-formal result publication, complete fresh-build evidence, post-build full-input re-anchoring, same-vendor delivery, the ATK casegen re-hash window, one shared build deadline, exact SoC ops-info naming, per-target installation evidence, duplicate profiler identities, dependency-free OOXML parsing, structured attribute transport, direct empty-array ABI ownership, detached-process isolation and final command-log replay. The final pass reported no remaining actionable finding.

The frozen plugin manifest contains 33 files and has aggregate digest
`ef38cc13e4264a9476d053fab52118418a763c41a5634728ba172fcdc37368e1`.
The same bytes passed the target regression on both machines:

- A3: 33/33 manifest files; 73 passed, 0 failed, 0 errors, 0 skipped; unittest time 64.063 seconds; log SHA-256 `b4306fe1d2ea31c3aeceefc0537ded56f8cd5b89f480829d779f113f1be51e25`.
- A5: 33/33 manifest files; 73 passed, 0 failed, 0 errors, 0 skipped; unittest time 39.080 seconds; log SHA-256 `0eadbb0aeeb6712e278954e91fe78f7a6ecc9f8b6781d9f7c8a53ab6f658127d`.

Both target suites included all four real ATK 26.5.14 witness case-generation runs, the Roll direct empty-`aclIntArray` ABI route and the detached-worker pidfd drain. The same frozen digest then produced final isolated formal sessions: Bernoulli A3 `PASS` (19/19, active 233.857614s), Roll A3 `PASS` (20/20, active 196.140172s), RemainderTensorTensor A3 fail-closed `PLUGIN_ERROR / BUILD_TARGET_UNPROVEN` with independently proven missing DUT A3 delivery (active 125.072402s), and GaussianBlur A5 `BLOCKED / ATK_EXECUTION_TIMEOUT` with independently proven DUT K13 capability defect and unvalidated timed-out performance (active 3244.620814s). The final verification and target regressions were read-only with respect to the shared worktree and preserved earlier formal evidence.
